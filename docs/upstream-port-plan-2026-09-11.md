# Upstream port check — 2026-09-11

Upstream (`NuvioMedia/NuvioMobile`, branch `cmp-rewrite`) moved: `e3779428` → `465266d7`, 25 commits (9 non-merge "real" commits with code/config changes, 4 merge commits, 5 i18n-only commits, 2 version/store-publish commits, 2 MPVKit submodule-pointer chore/revert commits, 1 CI-only fix). All non-merge commits read in full via `git show` (not `--stat`, not commit titles).

## Zero action items this run

Every commit in the range is either i18n-only, a version/store-publish bump, CI-only, an MPVKit submodule-pointer chore, or scoped entirely to `composeApp/`/`androidApp/`/mobile's own `iosMain`/`androidMain` — a full `git diff --name-only e3779428..465266d7` across every non-merge commit (excluding `strings.xml` and `store.json`) shows **zero paths under `shared/`**. Breakdown:

### Compose-UI-only navigation/animation batch (no `shared/` touch, no tvOS relevance)

A connected set of 8 commits (`8c71a6c9` "update bottom tab animation", `bb1a5ffa` "add root navigation baseline profile", `24920167` "lift original cards in zoom overlay", `b1f22de3` "add shared blur transition", `7b0ecf81` "refine poster transition and detail reveal", `f9d0c143` "refine poster transition and add toggle", `4aa62916` "add reactive bottom bar glow", `ab32ed3d` "refine tablet bar and transition defaults") build a jelly-physics animated bottom tab bar (new `composeApp/src/androidMain/.../jelly/JellyTabs.kt`, `JellyMotion.kt`, `JellySpring.kt`, `JellyDrawing.kt` — vendored from `react-native-jelly-tabs`, licensed accordingly), a glass-shader reactive glow on the bar (`GlassBarShader.kt`, `GlassBarSurface.kt`), and a poster-to-detail shared-element blur/lift transition (`PosterNavigationTransition.kt`, `PosterNavigationMotion.kt`, `PosterNavigationArtwork.kt`, `PosterLift.kt`, `PosterOpenMotion.kt`, `DetailsContentReveal.kt`). Every file touched lives under `composeApp/src/{commonMain,androidMain,androidHostTest,iosMain}/` or `androidApp/` (a `baseline-prof.txt` ART profile, Android-only). `find shared -iname "Jelly*" -o -iname "PosterNavigation*" -o -iname "PosterLift*" -o -iname "GlassBar*"` returns nothing — none of this has a `shared/` extraction, because it's Jetpack Compose animation/rendering code (custom `Canvas`/shader draws, `AnimatedVisibility`, shared-element `SharedTransitionLayout`) with no tvOS equivalent; tvOS's tab bar and poster→detail navigation are wholly bespoke native SwiftUI (already covered by the fork's own BUG-93/BUG-96/BUG-99 detail-anchor and reveal work in the beta.18 batches). Not a port candidate. **Product-idea note, not a task:** the reactive glow-on-focus bottom bar and the poster-lift-into-detail transition are visually interesting ideas tvOS could someday build natively (its own tab bar and detail-reveal animation are already under active bespoke iteration via the Steven beta.18 feedback loop), but this is a FEAT-level design idea, not anything with shared logic to port — log it as low-priority/optional if Christian wants a "watch this for inspiration" note, not a build item.
- `4b0ebd92` "perf(ui): replace compottie spinner with native drawing" — swaps a Lottie-based loading spinner (`composeApp/.../core/ui/LoadingIndicator.kt`, deletes `ResourceLottieCompositionSpec.kt` + its test + the bundled `nuvio_loading_indicator.json`, drops the `compottie` Gradle dependency) for a hand-drawn Canvas spinner. Pure Compose rendering change, `composeApp/` only, no `shared/` touch. tvOS has its own native loading-indicator treatment; not applicable.
- `38bd90f0` "chore(mpvkit): update Xcode scheme order" and `8d8e4cf2` "revert(mpvkit): restore published submodule revision" — a same-session bump-then-revert pair on the `MPVKit` git submodule pointer (`69eee035` → `d5cf091c` → back to `69eee035`), net no-op. This is upstream's own vendored MPVKit fork used by mobile's iOS player bridge — a different integration from tvOS's own MPV player wiring (`MPVPlayerView.swift`) — and the two commits cancel out regardless.

### Settings/home fixes (composeApp-only, confirmed no `shared/` logic)

- `db3da891` "fix(settings): require Trakt progress for continue watching window" (fixes upstream #1903) — `composeApp/.../settings/TrackingSettingsPage.kt` + one new string. Greys out and disables the "Continue Watching window" row unless Trakt is both connected and the active `WatchProgressSource`. Pure Compose UI row-enablement logic reading existing `SettingsUiState`; no `shared/` file touched, no new repository/business logic. tvOS's own Settings (native `List`/`Toggle`/`Menu{Picker}` since beta.15, `2026-08-23`) would need its own equivalent guard if this scenario matters there — worth a light spot-check next time anyone is in tvOS Settings/Tracking code, but not a mechanical port (there's no shared logic to port, only a UI-state conditional that would need re-deriving for SwiftUI).
- `8f0bfc8e` "fix(home): settle hero carousel when returning" — one 4-line `LaunchedEffect(pagerState) { pagerState.scrollToPage(pagerState.currentPage) }` added to `composeApp/.../home/components/HomeHeroSection.kt`, forcing the Compose `PagerState` to re-settle on its current page when the composable re-enters composition (e.g. returning to Home). Compose-`Pager`-specific mechanism; tvOS's Home hero has no `PagerState` (its own bespoke `HeroCommitGate`/`PinnedRowGeometry` system, already the subject of extensive fork-native work). Not applicable.
- `4f814036` "fix(settings): defer search indexing until needed" — `composeApp/.../settings/SettingsScreen.kt` + `SettingsSearch.kt`, changes `settingsSearchRootContent`'s `entries` param from an eagerly-built `List<SettingsSearchEntry>` to a `@Composable () -> List<SettingsSearchEntry>` lambda evaluated lazily inside the results `item {}` block, avoiding building the full settings-search index on every Settings screen composition. Pure Compose-recomposition perf fix, `composeApp/` only. tvOS's Settings has no equivalent eager-search-index-on-every-composition pattern to fix (it's native `List`, not Compose).

### i18n / version / CI (not applicable by category)

- `f904b467`, `fe1c9730`, `a517a7cf`, `02abf2a2` — four Greek (`el`) locale commits, `values-el/strings.xml` only, no logic. **Special note on `f904b467`**: it adds Greek strings for `playback_unavailable`/`playback_unavailable_message` (and 17 custom-theme strings) — confirmed pure i18n via full diff, only touches `values-el/strings.xml`, no `.kt` files. This does **not** change the status of the carried open item below; it's just evidence upstream is still actively building out the `PlaybackAvailability` composeApp feature (adding missing localization for strings that already shipped in `values/strings.xml` when `972109f9` landed) rather than upstream reverting or extending the feature's logic.
- `c1358f5d` — Dutch (`nl`) locale merge/sync, `values-nl/strings.xml` only.
- `0ead9818`, `6da74323`, `98e21d6f` — "update latest strings" / Vietnamese localization, string-resource files only.
- `a3b99ca9` "bump version" — `iosApp/Configuration/Version.xcconfig` only (mobile's own CMP iOS app version config, not the fork's `iosApp/NuvioTV/`).
- `465266d7` "chore(store): publish 0.4.16" — `store.json` only.
- `1abca38f` "fix(release): target the workflow branch" — `.github/workflows/android-release.yml` only, replaces a `release_commit` output with `RELEASE_BRANCH: ${{ github.ref_name }}` for the `gh release create --target` flag. CI-only, no app code.
- Merge commits `ab750a7f`, `11c5e54b`, `9f5a909c`, `75c73809`, `62e20ab9`, `e4379328` carry no diff of their own beyond what's already covered by the commits above.

## Submodule pointer check

No drift. Outer repo's committed pointer, `origin/tvos-shared-extraction`'s freshly-fetched tip, and this sandbox's local submodule checkout all agree at `1f5121d0b15ffd8f6f506c1ba6774d30da1f8a04` (build 127, tag `tvos-v0.3.0-beta.18-rc10`).

## Carried open item (unchanged from 2026-09-09 / 2026-09-10)

### [MEDIUM] Disable Play button when no playback source is available

Still unbuilt as of this check — re-verified `grep -rn "canPlay\|hasCompatiblePlaybackSource\|PlaybackAvailability\|isPlayEnabled\|playDisabled\|canStream" iosApp/NuvioTV/` returns nothing, and `DetailView.swift`'s Play button (`actionLabel("Play", systemImage: "play.fill")` at line 1430) still has no `.disabled()` gating. Full writeup and port shape unchanged — see `docs/upstream-port-plan-2026-09-09.md`. Summary: upstream commit `972109f9` (landed before this run's window) added `PlaybackAvailability.kt` (composeApp) that pre-computes whether a title has any playable source (enabled addon manifest support, enabled JS plugin scraper, embedded stream, or matching download) and greys out/disables Play instead of letting the user land on an empty Streams screen. Every dependency (`AddonRepository`, `MetaDetailsRepository`, `DownloadsRepository`, tvOS's own `TvOsPluginRepository`) already exists in `shared/` — only the Compose `remember`/`collectAsStateWithLifecycle` wrapper has no tvOS equivalent needed. Ready for Claude Code to pick up; not built this run. This run's `f904b467` (Greek `playback_unavailable` strings) confirms upstream is still iterating on this feature's composeApp/localization surface, not that anything changed about tvOS's gap.

## Verification note

All 9 non-merge, non-i18n, non-version-bump commits were read via full `git show` diffs. Applicability was confirmed by running `git diff --name-only e3779428..465266d7` across the whole range (excluding `strings.xml`/`store.json`) and observing zero paths under `shared/`, then spot-checking `find shared -iname` for the new Jelly/PosterNavigation/GlassBar/PosterLift file names to rule out an extraction existing under a different path. The carried open item was re-verified fresh against current `iosApp/NuvioTV/` file contents, not assumed from the prior day's note. Submodule pointer drift was checked by fetching both `origin/tvos-shared-extraction` and `upstream/cmp-rewrite` fresh in this sandbox rather than trusting the session-start values.
