# Steven beta.18 batch — feedback record + fix plan (2026-09-05)

Status: **MERGED 2026-09-06 into `tvos-shared-extraction` (`8560d305`)** — device pass (Show Hero OFF) + rc2 + DM owed; see the outcome section. **rc5 CUT (build 121, `9b02bb16`) is latest** — rc4 (build 120, `a4dbb976`) was cut but its DM was NEVER sent and is superseded; rc5 = rc4 + the full rc2-feedback batch (sidebar hide fix, bridge caption, Saga names, Settings font, last-row inset floor, Row Settle Diagnostics); device pass still owed.

## 1. What Steven reported on build 117 (rc1, DM 2026-09-05 5:48 PM ET)

Photos: `docs/research/steven-beta18-photos/2026-09-05-hero-paint-diagnostics-build117.jpg`,
`…/2026-09-05-trailer-diagnostics-build117.jpg`; the two beta.17 photos from 09-04 are filed beside them.

**Fixed (his words):** the double hero ("finally fixed"); the poster ring now goes around the poster instead of on it;
title size and the text no longer appearing before the logo; zoom + card depth; the second-to-last collections row.

**Caveats on the last one:** switching Medium → Large in Settings brings the old symptom back until a restart; and the
correction is visible ("you first see part of the row above, then the app realises 'oops' and quickly hides it").

**Not fixed:**
- Catalog titles bouncing; on his first launch the title vanished entirely (relaunch fixed that); a glitch on the
  title whenever he scrolls a row horizontally.
- Description page stutter, with every trailer option disabled ("not caused by the trailer").
- Trailer zoomed in on the description page.
- Trailers overflowing a focused poster, now believed to happen only on the first poster of each row.
- Collection backdrop repositioning in the hero: the new image appears, then re-crops in front of him when the
  previous image had a different size.
- Top bar still visible (FEAT-30, other session).

His 09-05 4:18 AM message also asked for the trailer bridge animation (FEAT-32, built) and 60 fps collections (FEAT-33).

## 2. What the photos say

**Hero Paint Diagnostics (build 117):** no `publish` lines, no `commit` line.

```
1403ms vm acquire id=1 rc=1
1451..1539ms addonsChanged ready=1..5 catalogs=0..18 addonRoute=refresh bootstrap=settled
1700ms rows vm=1 n=2 first=collection_… settingsSig=7805ec5f heldRebuilds=3
1811ms present item=nuvio.folder:… backdrop=fetched logo=fetched waited=73 same=0
1831ms paint kind=seededPrimary first=1 … item=collection-hftcv0ta-community/folder-JRWZVYWG
2477ms rows vm=1 n=6 first=tmdb-addon:movie:… rowsHash=2d82fb78c925689c settingsSig=f2774137
2686ms present item=movie:tt37752275 backdrop=fetched logo=fetched waited=165 same=0
2703ms paint kind=image first=0 … same=0 item=movie:tt37752275
186661ms vm release / vm stop
```

Reading: Steven runs **Show Hero OFF**. With no hero items there is no `publish` and no `commit`, and the top of Home is
the FEAT-15 focus panel (`HomeView.focusHeroActive`), whose resting title is the first item of the first catalog row
(`heroPanelSeed`). `decideHeroGate` releases immediately for `heroOff` (`HeroCommitGate.kt:201`), the Swift route sends
that to `.noHero`, the rows gate opens on the first released publish at 1.7 s, and when the launch sync burst lands at
2.5 s (settings signature changes) the rows reorder, the first catalog row changes, and the panel repaints a different
title. The two beta.17 photos show the same profile (no `publish` lines, a paint every second for a minute as the rows
churned), so Wave G's rows gate is what collapsed that churn to one swap. The plan's "Steven's configuration" assumed
Show Hero ON, so the 09-05 device pass and the photo contract never covered his profile.

**Trailer Diagnostics:** inline poster surface (`movie:tt37752275`): cached zoom applied (token match), layer 1458x820
zoom 1.080, probe abandoned after 2 samples when focus left (verified-cached kept). Description page = full-screen
surface (`series:tt34771210`, FEAT-32 auto-enters the cover): cached zoom 1.125 applied, re-measured and
`verify-confirmed`. 1.125 = exactly a 2.00:1 picture inside a 16:9 frame (1080/960). The crop is the letterbox-fill
policy working as designed on a surface where Steven expects the official app's uncropped behaviour. Cache: 36 entries.

## 3. Decisions (Christian, 2026-09-05 evening)

- Full-screen trailer surface never crops (zoom 1.0, `.resizeAspect`, bars). Hero loop and inline poster unchanged.
- The structural Large-frame fix is allowed (the 09-04 plan had rejected it): make the focusable link frame fit the
  pinned rows viewport so the hardware focus engine has one rest.
- The parked features (FEAT-30 side/collapsible top bar, FEAT-31 Open Sans, FEAT-33 60 fps) belong to a second
  session. It already occupies the main `NuvioMobile/` checkout (branch `claude/steven-parked-feats`), so THIS batch
  works in a separate local clone: `/Users/christianturnbull/Claude/Projects/NuvioMobile-beta18`, branch
  `claude/steven-beta18` off `1d4d8f9d` (remotes: `origin` = GitHub, `local` = the main checkout for fetch-back).
  This batch merges into `tvos-shared-extraction` first; the feature branch rebases.

Item → tracker: hero-off rows gate = BUG-86 remnant; bounce / vanished title = BUG-87; scroll glitch = BUG-88;
second-to-last row + Medium→Large = BUG-89; detail stutter = BUG-41; first-poster bleed = BUG-92; detail trailer crop =
BUG-94 (new); folder backdrop reposition = BUG-95 (new, the BUG-86b video evidence).

## 4. The plan

The approved plan is `~/.claude/plans/lets-make-a-detailed-cozy-comet.md`; summary of the waves:

**Phase 1 (parallel, disjoint files):**
- **W1 hero-off ROWS gate** (Opus): Kotlin owns it. `HeroGateDecision.rowsReleased`, `HeroGateInputs.rowsElapsedMs`;
  `HERO_OFF`/`NO_SOURCES` set `rowsReleased = syncSettled || rowsElapsedMs ≥ 4000`; `HomeRepository` holds `rowsHeld`
  on that, arms a rows-timeout job, widens `republishForGate()`; `HomeUiState.rowsGateReleased/rowsGateReason`;
  `gate=` string gains `rowsWait=… rowsWaitMs=…`; Swift `HeroPublishRoute.decide(rowsReleased:)` holds; a `publish n=0`
  probe line for the hero-off profile; `-debug.homeHeroOff YES` non-persisting fixture flag; new test31 leg D in
  `HeroOffLaunchTests.swift`; Kotlin gate tests for the five shapes.
- **W2 full-screen no-crop + first-poster bleed** (Sonnet): pure `zoomPolicy(surface:cached:)`, full →
  `(1.0, .resizeAspect, measure:false, persist:false)`, `policy=uncropped` probe line, cache never written by the full
  surface; `RowLeadingEdgeClip` shape (attached by W5 at `BrowseComponents.swift:3107`), player layer corner radius +
  `masksToBounds`, `debug_trailerTile x=`, screenshot-diff UI test.
- **W3 folder backdrop** (Sonnet): `HeroCrossfadeImage` gets `Color.clear` + `.clipped()` so its size never depends on
  the bitmap; `HeroArtResolver.commit` assigns outside `withAnimation`; `.transaction { animation = nil }` on the
  images; `present … frame=WxH` probe; layout unit test.

**Phase 2 (parallel):**
- **W4 pinned geometry** (Opus): `PinnedRowGeometry.plan(...)` spends compression (link-frame keyed, cap moved behind
  `showsCTA`: 142 give in panel mode vs 70 in carousel mode), then bottomReach 44→24, then topReach 88→64, publishes
  `fits`/`restRange`; HomeView reads the plan for compression, reaches, shelf chrome, bottom inset and last-row height;
  `debug_env` gains `comp= topR= botR= fits= slack=`; `regimeKey` `.onChange` drives the re-reveal in the same
  transaction as the compression change. (rc4: order reversed, reaches first — see outcome 09-06 17:00)
- **W5 corrector + belt** (Opus): first Reading may not arm the belt (0.5 s grace, `remeasureTick`); bounded
  re-measure watchdog while faded; `noteRegimeChange(key:)` resets the session-wide disarm/pull-back/verify state and
  re-arms with `retryAfter: 0`; settle line gains `regime= fits=`; attaches W2's clip; new regime UI test.
- **W6 detail stutter** (Sonnet, after an A/B evidence run with `-debug.detailScrollProbe YES -debug.detailScrollAB
  <0..4>`): debounced `isScrolling` on `ScrollDimModel`, `chipGlassFlat` also true while scrolling; acceptance ≤ 2
  hitches / 10 s, `maxGap ≤ 40 ms`, within 20% of glass-off.

**Phase 3:** Codex loop to clean → docs/tracker/CLAUDE.md/memory → merge into `tvos-shared-extraction` + pointer bump →
device pass in Steven's REAL shape (Show Hero OFF, French, Large, No Zoom ON, ring OFF, Card Depth ON, Hide Labels ON)
→ rc2 (build 118) → DM through the SlopMonster loop with the hero-off photo contract → beta.18 public only after his
photo passes.

**Hero-off photo contract:** good = `publish … n=0 … gate=released:heroOff rowsWait=settled rowsWaitMs≤4000`, then one
`rows … rowsGate=open`, then `present … same=0` + `paint … first=1`; bad = a second `rows` line with a changed
`settingsSig` followed by a `present` with a different `item=`.

## 5. Outcome (append as it happens)

- 2026-09-05 ~20:15 ET: clone created, Phase 1 agents (W1 Opus, W2 Sonnet, W3 Sonnet) launched.
- 2026-09-05 ~21:00 ET: **Phase 1 COMMITTED** on `claude/steven-beta18` in the clone — `db2240bb` W1 hero-off rows
  gate (Kotlin `rowsReleased`/`rowsWait=`, Swift route hold, `publish n=0` probe, `-debug.homeHeroOff`, test31 leg D),
  `89006585` W2 full-screen never crops + `RowLeadingEdgeClip` (attachment owed to W5), `ecc00536` W3 crossfade
  container. Gate so far: jvmTest 744 green (+42); Debug sim build green; NuvioTVTests 126 → two findings: (1) W3's
  own layout probe showed a square bitmap still grew the stack to 1250×1250 with `Color.clear` alone — fixed with a
  GeometryReader-pinned frame, probe green; (2) `testPrepareTimesOutEvenWhenAFetchNeverReturnsAndIgnoresCancellation`
  is a pre-existing one-`Task.yield()` race (passed 1 of 2 re-runs; the other re-run was a sim host crash) — hardened
  to a bounded poll. Release build + K/N tests + UI legs pending. Agent deviations accepted: W3 kept the commit
  `withAnimation` (the foreground text/logo crossfade rides that transaction; geometry is now pinned so it cannot
  animate); W1 applies `debugForceHeroOff` in `publish()` too (HomeView follows `uiState`, not `snapshot()`); W2's
  cache-untouched test proves the policy is cache-inert (no DI seam for `TrailerZoomCache.shared`). Note: the
  `-debug.homeLaunchBurstSim` replay waits for a non-empty hero publish, so on a hero-off profile it bursts only after
  its own 6 s timeout — leg D's real reproducer is the fixture's genuine launch sync.
- 2026-09-05 ~21:40 ET: Phase 1 gate continued — Release sim build green (Debug + Release both from `ecc00536`), K/N
  `tvosSimulatorArm64Test` 764 green. UI legs (test31 A/B/C, HeroOffLaunchTests leg D, RowLeadingEdgeTests, test37/41,
  TrailerSoakTests) running from a detached worktree snapshot of `ecc00536` (`~/Claude/Projects/NuvioMobile-beta18-p1`,
  own DerivedData) against the spare simulator `F38F573A` that now carries a clone of the FA87 fixture (data container +
  app group + prefs; FA87 itself is booted by the parked-features session). Phase 2 agents launched in parallel: W4
  (Opus, PinnedRowGeometry + HomeView/Theme), W5 (Opus, BrowseComponents belt/regime + leading clip attach), W6
  (Sonnet, DetailView glass flatten while scrolling); W6's A/B evidence run moved to the Phase 2 gate.
- 2026-09-05 ~22:30 ET: **Phase 2 waves delivered (uncommitted, gate running).** Phase 1 UI-leg run 1 failed to
  compile the test bundle (`RowLeadingEdgeTests.swift:100` "failed to produce diagnostic" on
  `Double(...).map(CGFloat.init)`) — spelled out and committed as `fdd7442e`; re-run in progress from the worktree.
  **W4** (`PinnedRowGeometry.swift`, Theme, HomeView): Steven's shape (Large 403.33, Hide Labels, panel) now
  compression 112.33 / reaches 88/44 / viewport 567.33 / linkFrame 535.33 / `fits=1` / restRange 32 inside a 74 pt
  band; Small/Medium/landscape 0 (explicit gate: spend only where today's compression is > 0); Large+captions+carousel
  unsatisfiable → today's numbers + `fits=0 CAPPED` log. Panel give made real (`heroPinnedCompressionCap(showsCTA:)`
  = 142; `synopsisLineLimit` derived from the slot) — **visible trade-off: the focus panel synopsis at that shape
  drops from 3 lines to 1** (the alternative spend order, reaches to their 64/24 floors first, would keep 3 lines at
  the cost of both focus-reach cushions) — **Christian's call at the device pass**. `.animation(.easeInOut(0.28),
  value: pinnedPlan)` on the pinned container + `.onChange(of: regimeKey)` → `noteRegimeChange`. `debug_env` gains
  `comp= topR= botR= fits= slack=`. Collection last-row height corrected via `pinnedPlanReachDelta` (helper lives in
  CollectionsUI.swift, out of scope — fold in later). **W5** (BrowseComponents): first-reading grace (0.5 s
  `remeasureTick` + republish), faded recovery watchdog (1 s × 5), `noteRegimeChange(key:fits:)` resets every
  session-wide brake and re-arms with delay 0 (scheduler slot now `(Int, TimeInterval)`), settle line `regime= fits=`,
  `UNEXPECTED-WITH-FIT` on a correction with `fits=1`, `RowLeadingEdgeClip` attached on `CatalogRowView`'s LazyHStack
  before `.scrollClipDisabled()` (CollectionRowView's row in CollectionsUI.swift NOT clipped — folder tiles play no
  trailer, so no user-visible gap); new `PinnedRowSettleRegimeTests` (leg A drives the real Poster Size picker, gated
  on `debug_env`; leg B idles 5 s then focuses one row to read `beltFaded`). **W6** (DetailView): `ScrollingLatch`
  (150 ms) on `ScrollDimModel`, third `onScrollGeometryChange` on raw offset, `chipGlassFlat` = trailer ∨ scrolling ∨
  A/B, `detailChipBackground` swap without animation, `debug_ux6 scrolling=`; the action-row `GlassEffectContainer`
  stays on its own A/B leg-4 knob by design. Main-session fix: the clear task is one per scroll, not one per frame.
  Gate: Debug build green with all three waves; unit bundle running on a third simulator; Release, UI legs (test47/48,
  regime tests, test31 re-run), and the BUG-41 A/B evidence run pending.
- 2026-09-05 ~23:00 ET: **Phase 1 UI legs (worktree @ `fdd7442e`, spare sim with the cloned fixture): 9/10 passed** —
  test31 A/B/C, test37, test41, RowLeadingEdgeTests both legs, TrailerSoakTests all four (the soaks ran despite the
  YouTube LOGIN_REQUIRED note — the smoke video id path still works). **Leg D failed and found a second gap:** the
  probe dump shows the hold working (`gate=released:heroOff rowsWait=sync` at 5520 ms, rows open at 6014 ms with
  `rowsWait=settled rowsWaitMs=712`), but the focus panel painted at 5659 ms — BEFORE the rows opened — from the
  Continue Watching seed (`heroPanelSeed` falls through rows → CW → folder, and CW is not behind the rows gate), then
  repainted the first catalog row's title at 6507 ms: the same double paint by another door. Fix: `HomeViewModel.
  rowsGateOpen` (published mirror of `RowsGate.isOpen`) and `heroPanelSeed` returns nil until it opens, so the panel's
  first paint is the rows' first paint (the "Loading catalogs…" placeholder covers the hold). The 12449 ms move in the
  dump is the burst sim's reversed row order (first rows changed) — legitimate; leg D now forbids any `present` before
  the rows open and allows a later title move only after a `rows` line with a CHANGED `settingsSig` (the photo
  contract's own rule). Note for the record: on a hero-off profile `HomeLaunchBurstSim.runBurst` waits for a hero
  publish that never comes and bursts after its 6 s timeout (~12 s here), so it never lands inside the hold; the real
  launch sync is what leg D exercises, and that part passed.
- 2026-09-05 ~23:45 ET: **Codex rounds 1–2 + Phase 2 UI run.** Round 1 (branch `1d4d8f9d..6561c73c`): P1 the
  initial pinned regime was never registered (`onChange` skips the initial value, so the first Medium→Large switch
  was `noteRegimeChange`'s record-only first call) → `initial: true`; P2 `cancelScrollLatch()` left `isScrolling`
  stuck if the page left inside the 150 ms window → cleared on cancel. Both in `1a8ba8ab`. Round 2: P2 the
  `AddonBootstrapRoute.openRowsNoSources` path opened the Swift rows gate on its own, bypassing the Kotlin rows hold
  → it now honours Kotlin's decision, waits on `LaunchSyncSignal` (bounded 4 s) when the launch sync is running, and
  reads the current publish/sync state through new Kotlin accessors (`currentUiState`, `launchSyncRunning`; the first
  cut guessed at StateFlow/enum interop names and broke the Release build); P2 the leading clip sliced the 22 pt
  focused-card shadow → allowance includes it. Commits `b…` (round 2 fixes) + `9ff57411` (accessors). Round 3 pending.
  Phase 2 UI run at `6561c73c` (spare fixture, machine under three builds + a Codex Gradle probe): test37/41,
  RowLeadingEdge ×2, regime leg B PASSED; **test31 leg A FAILED on `gate=released:timeout gateWait=sources
  sources=0/2`** (both hero-source catalogs unfinished at 4 s — the contract forbids timeout on Wi-Fi; passed in Phase 1
  on the same fixture, so re-run quiet before reading anything into it); leg D died on an AX snapshot timeout at its
  first query (infrastructure); **test47/48 SKIPPED: the fixture was at Medium (`debug_env w=220`)** — set Large +
  Hide Labels on the spare fixture via `poster_card_style_payload_1` in user defaults (profile index 1 from
  `profile_payload`); regime leg A SKIPPED loudly (the Appearance → Size picker walk landed on "Import Badge Pack";
  no adjacency map for that pane — owed). Unit bundle 148 green at `6561c73c` (the prewarm-timeout test's earlier
  miss re-ran 53/53 twice in isolation: load). Gate on `9ff57411` running.
- 2026-09-05 ~22:35 ET: **Codex round 3** (branch at `9ff57411`): P2 a Show Hero toggle while the launch gate is
  armed inherited the launch-sync rows hold → `rowsHoldBaselineHeroEnabled` stamped on the first evaluation from the
  caller's snapshot; a `heroOff` decision whose baseline was `true` releases the rows at once with `rowsWait=toggle`
  (`dbc9570d`, then `591e1ed8` fixed my own arm-time reset that nulled the stamp, then `0817b2a4` dropped an
  under-lock `snapshot()` call the first cut had introduced); P2 the scroll latch could over-hold by a full 150 ms
  interval → the clear task now sleeps only the remaining window from the newest change (`dbc9570d`). **Codex round 4
  failed twice: usage limit until 23:09 ET** — round 3's commits were reviewed internally (debounce sound; the lock
  point above was the one finding). Tip `0817b2a4`. Spare fixture set to Large + Hide Labels (profile index 1) for
  test47/48 and the regime legs. Gate on the tip owed: JVM, Debug/unit/Release, UI legs (quiet machine), BUG-41
  evidence run; Codex round 4 after 23:09.
- 2026-09-05 ~23:40 ET: **UI run on `0817b2a4` (spare fixture, machine quieter): test31 leg D PASSED** (the hero-off
  contract end to end with the seed hold), test37/41/46, regime cold-launch leg, RowLeadingEdge ×2 PASSED; test31
  failed on leg C (folder hero committed with `pbd=0` — the 1.5 s folder-art deadline; overlapped Codex's Gradle
  probe, re-run quiet); test47/48 skipped (`debug_env w=220`: the `defaults write` of the poster payload is clobbered
  by the synced pull) → `9f6c70a8` makes test47/48 carry `-poster_card_style_payload_<0..3>` as launch arguments
  (the defaults ARGUMENT domain beats the persisted/pulled value for the process); regime leg A still skips (picker
  walk). **Codex round 4 (after the usage reset): P1 the bootstrap route checked `launchSyncRunning` only — `Idle`
  with an expected sync is unsettled → new Kotlin `launchSyncSettled` (the gate's own term); P1 a re-entry with the
  watcher installed opened the rows → idempotent; P2 `noteRegimeChange` re-armed on the previous regime's
  measurements → `latest`/`sample` cleared so the settle HOLDS until fresh geometry; P2 the leading clip's allowance
  used the resting poster width → sized off the expanded tile (`height × 16/9`).** Round 5 running; gate + quiet
  UI re-run on the tip owed after it.
- 2026-09-06 ~00:20 ET: **Codex loop ended at round 4 (reviewer unavailable after it).** Round 5 failed four ways:
  usage limit (reset 23:09), then `gpt-6-astra` (the account's configured default) "requires a newer version of
  Codex" even on CLI 0.153.4 (upgraded at 23:19 by the other session or Christian), and every explicit model tried
  (`gpt-5.3-codex`, `gpt-5.4-codex`) is "not supported with a ChatGPT account". Round 4's fixes (`cddd23f4` Kotlin
  `launchSyncSettled` + idempotent bootstrap wait; `d7bf763e` stale measurements cleared on regime re-arm + clip
  allowance off the expanded tile — the first attempt's text match had silently failed, caught and redone) were
  reviewed internally: the re-arm lands on `MeasurementReport.unmeasured`'s HOLD until fresh geometry; the bootstrap
  wait is bounded (4 s task) and returns on re-entry. **Owed: one more Codex round on the final tip when the CLI can
  talk to the account's model again.** Tip `d7bf763e`; the tip gate (JVM/Debug/unit/Release) + quiet UI re-run
  (test31 A/B/C, leg D, test47/48 at Large via launch args, regime leg B, RowLeadingEdge) running.
- 2026-09-06 ~01:15 ET: **MERGED.** `origin/tvos-shared-extraction` had moved to `9ffed8ee` (the parked-features
  session landed FEAT-30 sidebar / FEAT-31 Open Sans / FEAT-33 probe, 17 commits) — merged into `claude/steven-beta18`
  with no conflicts (Theme/BrowseComponents/HomeView overlapped in different regions). Merge commit `8560d305` gated:
  Debug + Release green; unit bundle green except `testPrepareFailedWhenArtworkFetchThrowsWithinBudget` (timing, passed
  in isolation); UI on the spare fixture: test31 A/B/C, leg D, test37, regime cold-launch leg, RowLeadingEdge ×2
  PASSED (test31 fails under a concurrent build lane: 4 s gate timeout — run it quiet). `tvos-shared-extraction`
  fast-forwarded to `8560d305` and pushed with `claude/steven-beta18`; outer pointer recorded via update-index (the
  main `NuvioMobile/` checkout belongs to the other session). test47/48 launch-arg payload reverted (`26e946ae`, the
  synced pull publishes over it). **Owed:** device pass in Steven's real shape (Show Hero OFF, Large, Hide Labels, No
  Zoom, ring OFF, Card Depth ON) incl. the synopsis 1-line trade-off call; BUG-41 evidence run (script drafted at the
  scratchpad, untested); one Codex round when the CLI can use the account's model; rc2 (build 118) + DM (SlopMonster).
- 2026-09-06 ~02:00 ET: **Codex round 5 ran** (via `codex exec` — the companion's review mode is what the account's model rejects on CLI 0.153.4; `exec` works with both the npm and the ChatGPT-app binaries). Two P2s, fixed in `ebe14a0f`: the releasing publish after a mid-launch Show Hero toggle used the raw decision (`rowsReleased=false`) so Swift kept holding — now the effective post-apply state; the no-sources bootstrap wait's completions could open the Swift gate after a sync pull enabled an add-on — the refresh path bumps a generation both completions check. Gate: JVM, Debug, unit; `tvos-shared-extraction` fast-forwarded + pushed, outer pointer bumped.
- 2026-09-06 ~01:30 ET: **rc2 CUT.** Build number 118 (`25e07e08`, tag `tvos-v0.3.0-beta.18-rc2`, on `tvos-shared-extraction`); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc2 NuvioCommitSHA=25e07e08`; IPA 25,818,355 bytes at https://litter.catbox.moe/js7mfl.ipa (litterbox 72 h, `content-length` verified; copy in `~/Downloads/NuvioTV-beta18-rc2.ipa`). DM draft at 5/5 with the link and commit filled: `docs/comms-dm-drafts-2026-09-06.md` — NOT sent; the optional synopsis paragraph is Christian's call. Device pass with Show Hero OFF still owed before the public beta.
- 2026-09-06 ~01:45 ET: **rc2 DM SENT** to Steven (without the synopsis trade-off paragraph; the sidebar early-stage ask included). Verified in-thread. The send also surfaced an unread edit from him (~23:45 ET): the second-to-last collections row "doesn't work" after further testing on rc1 — to be read and triaged.
- 2026-09-06 ~14:30 ET: **BUG-96 FIXED** (`46ab30f0` + review fixes, tip `69ebad3a`, pushed). Mechanism found by the fixture: `ScrollViewProxy.scrollTo(id:)` is ignored by the focus-driven vertical scroll; `ScrollPosition.scrollTo(y:)` (Home's corrector) works, lands at `y − contentInsets.top` (inset 157 on the fixture), and the engine's own reveal overrides anything issued before it settles (0.35 s). Rest = 108 pt on screen under a 96 pt top scrim. Codex exec review: 5 findings fixed (comments unanchored, geometry read at fire time, cancel on leave/bridge, Reduce Motion, probe published at rest). Side fix: the trailer cover's dim-overlay copy carried a second `debug_ux6` probe (test17 broken since FEAT-32). NOTE for background chains: `codex exec` must run with `</dev/null` or it waits on stdin forever (10 h stall on 09-06).
- 2026-09-06 ~15:30 ET: BUG-96 Codex rounds 2–3 (exec, stdin closed): r2 fixed cancellation honoured in the anchor pass + the comments row genuinely unanchored (`0e68e254`); r3 P3 fixed (the UI leg requires three DISTINCT anchored rows); r3 P2 DECLINED with reason: the Episodes section rests as one row (season chips anchored at 108, the episode shelf fully visible under them); a per-shelf anchor needs EpisodesSection internals and adds a second move inside one section, the land-then-nudge class this fix avoids. Fixture-found follow-up in `072dfa63`: late layout under the focused row (cast photos) moved it 470 pt after the pass and the engine re-revealed; the row's measured offset now re-triggers one debounced pass per layout change. Housekeeping: the morning sweep renumbered the trailer-crop row BUG-94 → BUG-97 (ID collision); earlier BUG-94 mentions in this doc mean BUG-97. Tip `072dfa63` on `tvos-shared-extraction`.
- 2026-09-06 ~15:50 ET: **rc3 CUT.** Build 119 (`9ddc4104`, tag `tvos-v0.3.0-beta.18-rc3`, on `tvos-shared-extraction`) = rc2 + BUG-96; unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc3 NuvioCommitSHA=9ddc4104`; IPA 25,855,404 bytes at https://litter.catbox.moe/2cljii.ipa (litterbox 72 h, size verified; copy in `~/Downloads/NuvioTV-beta18-rc3.ipa`). Follow-up DM draft 5/5 with the link filled: `docs/comms-dm-drafts-2026-09-06-rc3.md` — NOT sent, awaiting Christian's go.
- 2026-09-06 16:05 ET: **Steven's rc2 feedback (DM 08:34 AM ET, read after the rc3 follow-up went out; video https://fromsmash.com/OstKtGR5AA-ct, Large + Medium, hero ON).** FIXED per him: full-screen trailer (BUG-97); choppy description (BUG-41); floating bar hides on Home (FEAT-30); Open Sans works (FEAT-31) — but not applied to the Settings categories. NOT FIXED: the second-to-last row at Large "still always visible" (BUG-89); title bounce persists — Medium: titles bounce; Large: no bounce on the first row but titles DISAPPEAR on the other rows and stay hidden (BUG-87/88, "whether the Hero is enabled or not"); the Home focus-panel synopsis at Large shows one line (the W4 compression trade-off — he objects: "one line is not enough"; he sees one line sometimes and several other times); NEW: floating bar flickers for a second when pressing Up while scrolling a catalog, then settles (FEAT-30); the floating bar should sit higher and further left (FEAT-30); trailer bridge (FEAT-32) "looks really clean" but the title resizes with the still (odd) and the logo is missing (text only); saga/franchise titles missing in the description (only images). No hero photo yet. TRIAGE OWED — the Large title-hiding and the still-visible second-to-last row say W4/W5's structural fit did not land the way the sim predicted on hardware (the corrector/belt regime is still live at Large); the one-line synopsis is the W4 spend order (reaches-first would keep 3 lines) — Christian's call, now with Steven's vote against one line.
- 2026-09-06 16:40 ET: Steven's rc2 video fetched via Smash: `~/Downloads/IMG_8477.mov` (589 MB, 3840×2160, 30 fps, 186.6 s; contact sheets in the session scratchpad). Sheets confirm the shapes in his text: Large + Nuvio-style hero ON on Home ("Nouveaux films", "Top 10", "Genres"/"Services de Streaming" folder rows), the FEAT-32 bridge (Avatar), the sidebar and the Navigation/Typeface settings, then Medium. Frame-level triage of the Large title-hiding, the still-visible second-to-last row and the Medium bounce is the next session's job (needs the device, or at least a Large synced profile on a sim, which the fixture is not). Decisions Christian owns before that work: the W4 spend order (reaches-first keeps a 3-line synopsis; Steven objects to one line) and whether rc3/beta.18 waits for it.
- 2026-09-06 ~16:00–17:00 ET: **W4 SPEND ORDER CHANGED (reaches first).** Christian's decision on Steven's rc2 feedback: `PinnedRowGeometry.plan` now spends the reach cushions (bottomReach 44→floor 24, topReach 88→floor 64) and only then hero compression, instead of compression first — his shape (Large 403.33, Hide Labels ON, focus panel = Show Hero OFF) had compression-first spend 112.33 and drop the panel synopsis to one line ("one line is not enough"). At his shape under the new order: compression 68.33 (Wave 10's own Large number), reaches 64/24, linkFrame 491.33 fits the 523.33 viewport, restRange 32, panel synopsis keeps 3 lines. Second half: the hero's compression split (`synopsisSlotGive`/`logoSlotGive`) extracted from `HomeHeroForeground` into a pure, unit-tested `PinnedRowGeometry.HeroSlotGive.split`, with a three-tier spend in panel mode (synopsis 36, logo 32, then the panel's extra 72 only beyond those plus the 2 pt frame slack) — needed because the prior panel-mode split drained the synopsis first (68.33 → 75.67 pt slot → 2 lines, not 3). Small/Medium/landscape unchanged (compression 0, reaches 88/44); Large + captions + carousel still unsatisfiable → `fits=0 CAPPED`. Commits on `claude/steven-beta18` (clone `~/Claude/Projects/NuvioMobile-beta18`): `0d449346` ("home: PinnedRowGeometry spends the reach cushions before hero compression (rc2 feedback)") and `a4dbb976` ("release: bump build number to 120 for beta.18-rc4"); both pushed, `tvos-shared-extraction` fast-forwarded to `a4dbb976` and pushed, outer pointer recorded via `git update-index --cacheinfo`. Gates (scratchpad DerivedData): Debug sim green; NuvioTVTests 170/0 failures (22 in PinnedRowGeometryTests incl. 9 new HeroSlotGive tests + a spend-order invariant); Release sim green; UI legs on the spare fixture F38F573A: test31HeroCommitsOnce PASSED (306 s, all legs incl. D), HeroOffLaunchTests test31DHeroOffRowsWaitForSync PASSED, PinnedRowSettleRegimeTests testW5bColdLaunchLeavesNoTitleHidden PASSED, RowLeadingEdgeTests ×2 PASSED, test47/test48 SKIPPED as expected (fixture's synced profile is Medium). Codex review round 1 via `codex exec -s read-only` (`</dev/null`): CLEAN for material bugs, 2 P3 doc nits (a wrong "31.7 pt" rationale figure — the old slot was 36 pt = 144 − min(112.33, 108); a stale BrowseComponents line reference) fixed before the commit. No JVM/K-N run needed (no Kotlin change). **⚠️ The reach floors 64/24 are UNPROVEN ON HARDWARE** (72 was the long-proven reach, 88 the BUG-53 title cushion, 100 kills focus; at reach 64 the settled title clearance is 2 pt vs 26 at 88, so No Zoom OFF users at Large may see the focus lift clip a title) — device pass owns the verdict; revert = `topReachFloor` 72 (Steven's shape then compression 76.33 → 2 lines) or the compression-first order.
- 2026-09-06 ~17:30 ET: **rc4 CUT.** Build number 120 (`a4dbb976`, tag `tvos-v0.3.0-beta.18-rc4`, on `tvos-shared-extraction`); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc4 NuvioCommitSHA=a4dbb976`; IPA 25,853,147 bytes at https://litter.catbox.moe/77tezp.ipa (litterbox 72 h ≈ 2026-09-09 ~16:30 ET; copy in `~/Downloads/NuvioTV-beta18-rc4.ipa`). DM draft at 5/5: `docs/comms-dm-drafts-2026-09-06-rc4.md` — NOT sent, awaiting Christian's go. Still owed: device pass with Show Hero OFF (now also the reach-64/24 focus check, and a No Zoom OFF Large title-clip check), BUG-41 evidence, Steven's hero photo, the rc2 remaining-items batch (Large title hiding, second-to-last row at Large, Medium bounce, floating bar flicker/placement, bridge title/logo, saga titles) working from `~/Downloads/IMG_8477.mov`.
- 2026-09-06 ~18:00 ET: **rc4 DM HELD; rc2 video triaged frame by frame.** Christian decided not to send the rc4 DM and instead worked through the rest of Steven's rc2 feedback list from `~/Downloads/IMG_8477.mov`. Frame-level triage record: `docs/research/steven-rc2-video-triage-2026-09-06.md`, evidence crops in `docs/research/steven-rc2-video-evidence/`. Findings: at Large the row label survives a landing from above and vanishes on the first horizontal move (belt), entering from below it fades; the last row rests ≈90 pt deeper than a middle row at both sizes; at Medium every landing is followed by a ≈50 pt upward slide (the corrector's second move = "bounce"); the sidebar pill stays painted over detail pages opened from Search; the bridge caption scales in 1.25→1 and is text where the header is a logo; the Saga row shows bare posters (Hide Labels ON reaches it); Settings categories render in the system face. Sidebar root cause found: `MainTabView` applies `.environment(\.tabBarVisibility)` on the TabView and attaches the sidebar as an `.overlay` OUTSIDE it, so the overlay's environment read resolved to the unconnected default instance and the immersive push never reached it (the pill only hid on Home's detail because the page was scrolled down). Key diagnostics reading on the fixture (Medium profile with captions): `plan M330c1p1r0 vh=455 link=506 rest=0 … fits=0` — 51 pt over-tall, matching the ≈50 pt Medium land-then-slide; extending the fit to Medium+captions is Christian's call (a default layout change).
- 2026-09-06 ~18:10 ET: **rc2 FEEDBACK BATCH BUILT + MERGED.** Submodule clone `~/Claude/Projects/NuvioMobile-beta18`, branch `claude/steven-beta18`: commit `3de9c5ce` "rc2 feedback batch: …" + `9b02bb16` "release: bump build number to 121 for beta.18-rc5"; both pushed; `tvos-shared-extraction` fast-forwarded to `9b02bb16` and pushed; outer pointer recorded at `9b02bb16`. Waves: **W1** sidebar (explicit `TabBarVisibility` pass-through; pill in the corner 60 pt from the left / 40 pt from the top, safe area ignored; 0.35 s show-edge settle, hide/focus/reveal/immersive immediate; `sidebar_state` gains `immersive= resting=`). **W2** bridge caption (no scale-in — `captionScale` removed; title logo via `CachedAsyncImage` with text fallback; one `placedBottomLeading()` helper for both copies; note: an Extras-row clip's caption now shows the title logo instead of the clip name) + Saga row `PosterCard(showTitle: true)`. **W4** Settings categories `Theme.Font.body`. **W5** last-row inset floor `Spacing.screen + plan.restRange + Theme.Size.heroPinnedRowsDeviceParkSlack (96)` + NEW "Row Settle Diagnostics" About toggle (`debug.pinnedRowSettleProbe`, lines persisted under `debug.pinnedRowSettleProbe.lines`, head 12 / tail 28; lines: `regime`, `plan … vh= link= rest= comp= topR= botR= fits= botInset= lastRowH=`, `lastRow`, `settle <report>`, `belt <event>`; strings fr/de/es/it/vi added to Localizable.xcstrings by hand). Codex exec round 1: CLEAN for material bugs; 1 P2 (the plan line only fired on a regime change → companion `lastRow` line on the last row's height change) + 1 P3 (buffer test needed `resetForTesting()`), both fixed. Gates: Debug + Release sim green; NuvioTVTests 171/0 (25/0 on the three touched classes after the fixes); UI legs on the spare fixture F38F573A: test31HeroCommitsOnce, HeroOffLaunchTests, PinnedRowSettleRegimeTests cold-launch leg, RowLeadingEdgeTests ×2, test52SidebarOverlay, test53TypefaceOpenSans, DetailRowAnchorTests, test51TrailerBridgeAutoPlay (PASSED end to end — YouTube extraction works again from this Mac, the proof owed since FEAT-32) all passed; test47/48 skipped as expected. Visual pass with a scratch XCUITest (deleted, not committed): pill at the corner; sidebar element ABSENT on detail pages pushed from an unscrolled Home AND from Search; Settings categories in Open Sans; About shows the toggle with live lines; bridge caption = logo + hint, no resize, return lands on the enlarged still; Saga row names under the posters. Gotchas: osascript key input does not reach a headless-booted simulator even after opening its window (use an XCUITest driver + `xcrun xcresulttool export attachments`); `-ui_font` value is `openSans` (case); detail auto-play trailer is `-detail_trailer_autoplay NO`; the fixture's Home row 4 is the Netflix folder.
- 2026-09-06 ~18:40 ET: **rc5 CUT.** Build number 121 (`9b02bb16`, tag `tvos-v0.3.0-beta.18-rc5`, on `tvos-shared-extraction`); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc5 NuvioCommitSHA=9b02bb16`; IPA 25,868,367 bytes at https://litter.catbox.moe/dm1noh.ipa (litterbox 72 h ≈ 2026-09-09 ~18:30 ET; copy `~/Downloads/NuvioTV-beta18-rc5.ipa`). DM draft at 5/5: `docs/comms-dm-drafts-2026-09-06-rc5.md` — NOT sent. rc4 (build 120) was cut and uploaded but its DM was never sent; it is superseded by rc5. Still owed: device pass (Show Hero OFF, reach 64/24 focus walk, No Zoom OFF Large title-clip check, last row rest on hardware), Steven's two photos (Hero Paint Diagnostics + Row Settle Diagnostics at Large), the Medium+captions fit decision, BUG-41 evidence.
- 2026-09-06 18:35 ET: **rc5 DM SENT** to Steven (Reddit chat, Christian's go): link + About check `9b02bb16`, the six fixed items, the two deliberately unfixed ones with the Row Settle Diagnostics photo ask (Large walk), the hero photo ask. Verified in-thread (1,817 chars, composer empty). Next: his two photos → the Large title-hiding / Medium fit decision; device pass (Show Hero OFF) still owed.
- 2026-09-06 ~23:30 ET: **Steven's rc5 feedback (DM 7:16 PM ET + three photos; he skipped rc3, never got an rc4 DM, went rc2 → rc5).** FIXED per him: floating bar higher (FEAT-30; suggests icons only, no text, "like the movie descriptions"); bridge caption shows the logo (FEAT-32) — but the leave/return now reads as a plain fade, he misses the "Nuvio-style" transition rc2 had; Open Sans on Settings (FEAT-31); bar hides on description pages (visible on Search's first result and Home's first page, which he assumes is intended). NOT FIXED per him: Home description still ONE line at Large; second-to-last Home row still always visible (BUG-89); Saga row still bare posters (rc5 `DetailView.collectionRow` passes `showTitle: true`, sim showed names — unexplained; check whether his "Saga" row is `collectionRow` or another row); catalogue title issue (BUG-87/88, warned); collection background resizing (BUG-95, W3 was meant to fix this). NEW: on the description page one Down press scrolls TWO steps (BUG-96's rest anchor + re-anchor is the suspect — his first build with it). **Photos** (`docs/research/steven-beta18-photos/2026-09-06-*-rc5-*.jpg`): the Hero Paint pane (two parts) shows `heroOff=0` — **Show Hero was ON for this photo**, contradicting the 09-05 "he runs Show Hero OFF" reading (his rc2 text already said the bugs happen with the hero on or off; the video was hero ON). Hero-ON contract PASSES on his AppleTV11,1: `publish n=0 heroOff=0 resets=1 rowsWait=n/a` ×3 (1292–1350 ms), `publish n=8 first=1` 2480 ms, `rows n=6` 2508 ms, ONE `commit head=movie:tt10375624 art=ready waited=24 ms` 2509 ms, `first_hero n=8`, ONE `present first=1` 2549 ms, ONE `paint kind=seededPrimary` 2587 ms, later publishes (2648, 3602 ms) keep the same head with no second commit; the next `present item=movie:tt29355505 first=0` is at 233.4 s, one second before `vm release/stop` (he left Home for Settings), so it is remote input, not a repaint. The third photo is the WRONG pane: "Diagnostic de la Barre d'Onglets" (Tab Bar Diagnostics: `Home f=602 t=17 086 y=0 i=0 r=0`, others 0, `depth=0 cycles=0`) — the Row Settle toggle ("Diagnostic de position des rangées") sits lower in About, after the BUG-89/66 A/B legs, and he did not find it. So the Row Settle photo is still owed, and the hero-OFF contract photo (`rowsWait=settled`) is still untaken. **Why the synopsis is one line:** `PinnedRowGeometry.HeroSlotGive` keeps the carousel bit-identical (tier-3 ceiling 0), so with Show Hero ON at Large + Hide Labels the 68.33 compression takes 36 from the carousel's 72 pt synopsis slot ⇒ `floor(36/36)` = 1 line; only the hero-OFF focus panel gets the 3-line result. If he keeps the hero on, the 3-line ask needs a carousel-side decision. Reply not yet drafted.
- 2026-09-06 11:41 PM ET: **REPLY DM SENT** for his rc5 feedback (`docs/comms-dm-drafts-2026-09-06-rc5-reply.md`, 5/5). Says: hero photo passes with Show Hero ON; asks for the hero-OFF photo only if the double hero persists that way; explains the one-line synopsis is the hero-on carousel and offers three lines there if that's how he runs Home (Christian's call, conditional); points him to "Diagnostic de position des rangées" for the Row Settle photo; asks for the 9b02bb16 commit check + a Saga-row photo; asks which rows double-step on Down and what the trailer transition looks like now; icons-only bar noted; bar on first result/page confirmed intended. Owed from him: hero-OFF photo (conditional), Row Settle photo, commit check, Saga photo, answers.
- 2026-09-07 9:19 PM ET: **Steven's REPLY to the rc5 reply DM (text + three photos; read 9:26 PM ET, photos at `docs/research/steven-beta18-photos/2026-09-07-*.jpg`).** Text: (1) with Show Hero OFF the Home description shows TWO lines (not the three the panel plan promised) and he says the text is too large — two lines is "not enough"; (2) with Show Hero OFF the row titles no longer disappear (so the BUG-87/88 title loss is hero-ON/carousel-regime only); (3) he took the Row Settle pane with the hero ON and again OFF "because the behaviour differs"; (4) build confirmed `9b02bb16`; (5) Saga photo attached; (6) trailer transition: rc2 had a zoom-in/"approaching" effect closer to his Nuvio video, rc5 has "only a fade" — he wants the zoom back. NOT answered: which detail rows double-step on Down; whether the double hero still shows with the hero off (no hero-OFF Hero Paint photo). **Saga photo (`…detail-saga-row-rc5-names-ok.jpg`):** names render under every poster (Avatar, Avatar : La Vo…, Avatar : De fe…, Avatar 4, Avatar 5) and the "Bandes-annonces et extras" row below carries names + "Trailer • 2022" — the Saga-names item is FIXED on rc5; his rc5 "still bare" note predates this check. **Row Settle photos, decoded** (regime key = Large 403 / no captions / p1 = panel hero (hero OFF), p0 = carousel hero (hero ON) / portrait rows; the pane shows the head only, the tail is below the photo edge): *photo-a = Show Hero ON* — `0ms regime L403c0p1r0 fits=1`, `0ms plan vh=523 link=491 rest=32 topR=64 botR=24 fits=1 botInset=60 lastRowH=0`, `2753ms regime L403c0p0r0 fits=1 from=L403c0p1r0` (the carousel replaced the panel when the hero committed, matching the 2.5 s paint), `2754ms plan … botInset=190 lastRowH=393`, `2754ms lastRow collection_collection-erfs5gwk-community-2 lastRowH=393 botInset=190 rest=32`, `4752ms settle row=tmdb-addon:movie:tmdb.lates… nudge=-12 bound=44 n=1 UNEXPECTED-WITH-FIT`. *photo-b = Show Hero OFF* — same `L403c0p1r0 fits=1` plan throughout, `392ms lastRow … lastRowH=393 botInset=190 rest=32`, `963ms settle row=collection-hftcv0ta-communit… nudge=-12 bound=62 n=1 UNEXPECTED-WITH-FIT`, `3482ms settle row=collection-hftcv0ta-communi… nudge=-44 bound=30 n=1 UNEXPECTED-WITH-FIT`, `3999ms settle … deficit=0 bandLo=0 bandHi=48 inBand=1 nudge=0`. **Reading:** the W4 fit holds on his AppleTV11,1 (`fits=1`, `link=491` inside `vh=523`, 32 pt rest range, reach floors 64/24 confirmed on hardware), yet the corrector still fires — `UNEXPECTED-WITH-FIT` in BOTH hero modes, negative nudges of 12 and 44 pt (the engine rests the focused row 12–44 pt too LOW, the corrector pulls it up, `n=1` so each lands with no oscillation, then `inBand=1`). That is exactly the "third anchoring nobody has measured" case the `UNEXPECTED-WITH-FIT` branch in `BrowseComponents.swift` was written to catch: hardware rests a FITTING frame out of band, so the visible land-then-slide he calls the bounce is the correction itself, not a missing fit. The 44 pt one is on a collection row (`collection-hftcv0ta-…`, the row after `lastRow`'s `erfs5gwk` folder) — collection rows and the plain tmdb row both miss. Next: reproduce the hardware rest offset (the sim top-anchors; the ATV bottom-anchors an over-tall frame, and now apparently also a fitting one), then either move the band or pre-position the rest so no correction is needed; the tail of both panes (28 lines) is still unread — a second photo scrolled down would show the walk back up and the last-row lines (`endOfContent=1 room=`). Also new: the hero-OFF panel synopsis shows 2 lines on hardware where the plan expected 3 (check the panel's synopsis slot at his shape before promising three), and he asks for a smaller synopsis font. Reply not yet drafted.
- 2026-09-07 9:59 PM ET: **REPLY DM SENT** for his 09-07 photos (`docs/comms-dm-drafts-2026-09-07-reply.md`, 5/5, 211 words; Christian cut the simulator sentence and the "I'll bring it back" promise before the go). Says: Saga confirmed fixed; the row photos show the frame fits but the TV rests rows 12–44 pt low and the pull-up is the bounce, next fix is to measure the hardware rest and aim there; asks for the Row Settle pane scrolled to its tail; two-line synopsis with the hero off is unexpected (plan said three), will check; text size noted; the trailer "zoom" was the caption scale-in removed with the logo caption (no promise to restore); re-asks which detail rows double-step on Down and whether the double hero persists with the hero off. Owed from him: tail photo, the two answers.
- 2026-09-07 10:09 PM ET: **Steven's REPLY to the 9:59 PM DM (text only, no photos; read 2026-09-08 morning via ego task space `reddit chat steven`, id 11, tab reused without a wedge).** Six points: (1) **Saga row, misunderstanding cleared up:** the film names ARE shown above/under the posters; what he means is that the poster ARTWORK itself is textless — "inside the poster itself, there is only the image and not the title" — so rc5's `showTitle: true` fixed the wrong thing; the ask is about which poster variant the Saga collection cards use (a textless/no-title poster vs. the titled one official Nuvio shows). (2) **Double hero: FIXED in both hero modes** per him — that is why it sat in his rc5 "fixed" list; the hero-OFF photo is no longer owed. (3) **Only remaining hero item: collection images resize live** (the folder/collection backdrop growing into place, BUG-95 class, "as I showed you in my videos"). (4) **Row Settle pane tail: he says he already did the full walk** (top → bottom → top) for the two photos he sent; he read the "scrolled to the end of the list" ask as a walk ask, not a "scroll the diagnostics list itself to its last lines" ask — the tail photo is still missing and the ask needs rewording (scroll the About pane's own list down until the last `settle`/`lastRow` lines show). (5) **Trailer bridge: explicit ask to restore the rc2 choreography** — "the logo growing while the page fades to dark" (the `captionScale` 1.25→1 scale-in removed in rc5 W2; the 09-07 reply deliberately did not promise it — Christian's call). (6) **Two-step Down on the detail page happens on every movie** (4–5 tested), so BUG-96's row anchor is the suspect on every title, not a per-layout case. Reply not drafted; decisions owed from Christian: bridge scale-in restore (yes/no), Saga poster variant, Row Settle tail re-ask wording.
- 2026-09-08 (afternoon–evening ET): **THREE-ITEM FOLLOW-UP BATCH BUILT on `tvos-shared-extraction` (model-delegated: 3 Sonnet implementation agents + 4 Sonnet/Haiku fix agents, Codex 2 rounds, main session verified).** Plan: `~/.claude/plans/lets-work-on-getting-polished-naur.md`. (1) **Bridge zoom restored** (`bf02fd30`): `TrailerBridgeChoreography.captionScale` back verbatim from rc2 (1.25→1 entering `.leaving`, description-side caption only, applied to the logo caption; cover-side copy never scaled), `TrailerBridgeTests` pins the scale terms again. (2) **BUG-95 hardening + rig** (`e3f281d5`): frame-by-frame reads of Steven's rc2 video showed every folder→folder hero swap already crossfading with zero geometry motion (W3 closed that class; sheets in `docs/research/steven-beta18-video-evidence/bug95-rc2-*.jpg`); the two `HeroCrossfadeImage` leaves are now UIKit-backed (`HeroBitmapLayer`, UIImageView scaleAspectFill + clipsToBounds) so no SwiftUI transaction can ever interpolate an aspect-fill crop; `HeroCrossfadeLayoutTests` + a bounds test for 16:9 and 1:1 bitmaps; new rig `HeroFolderSwapTests.test54FolderHeroSwapGeometry` (self-locating: presses Down until the hero probe's `pitem=` contains `nuvio-folder://`, 37–41 Downs on the FA87 fixture, whose synced profile carries Steven's own Fusion collections with hero art on all 16 folders) + `scripts/dev/hero-swap-sheets.sh` (simulator recording → per-swap contact sheets). **THE RIG'S REAL FINDING:** on a cold cache the folder mosaic misses the resolver's 1.5 s `folderDeadline` and the probe logs `present item=nuvio.folder:… backdrop=none logo=text waited=1509`; the hero commits with NO backdrop (`fadeToEmpty`) and the late-arriving image was never presented, so the folder hero sat BLANK until focus reached a folder whose art was already cached (`paint first=1 hadArt=0`). Two causes stacked: the Services folders' art is on i.postimg.cc, which answered 32 KB in 30 s from this Mac at the time (transient; normal minutes later), and the artwork session's default 60 s request timeout let hung fetches hold the six fetch slots so even the GitHub-hosted Genres mosaics starved behind them. **Fixes (uncommitted at the time of this entry, landing as one commit):** `HeroArtResolver.adoptLateBackdrop` — a backdrop that lands after the deadline re-commits ONLY when the same folder is still targeted and presented with `backdrop == nil` (pure `shouldAdoptLateBackdrop` predicate, 6 unit tests; `HeroPresentArtWait.lateBackdrop` keeps an image that lands during the deadline hand-off; adoption commits the LIVE `presented.item` so a same-identity gap-fill is never rolled back); probe token `backdrop=late` (append-only); `CachedAsyncImage` session `timeoutIntervalForRequest = 20`; DEBUG-only `-debug.heroFolderDeadlineMs` knob; `test55FolderHeroLateBackdropAdopts` (deadline 1 ms, cold cache) — PROVEN on the fixture: `present … backdrop=none waited=2` then `present … backdrop=late waited=138` for the same folder. (3) **BUG-96 two-step Down** (`b067fcba`): the rc3 anchor waited 0.35 s for the engine's reveal and then slid the row as a second move; now `anchorPass` fires from the per-frame scroll-geometry callback the instant the engine's reveal moves the page (replacing it mid-flight), 0.05 s fallback for rows the engine never scrolls; `DetailScrollMotion.segments` (time-based after Codex r1: `MotionSample(time, offset)`, a ≥0.10 s gap between moving samples splits) feeds `moves=N` into the `debug_ux6` probe; `DetailRowAnchorTests` asserts `moves ≤ 1` — fixture: `moves=1`, residual 0 on every step (blend and fallback paths). Codex r1: 4 P2 (late adopt rolled back metadata; hand-off race lost the image; knob read the launch arg as the wrong type; count-based `moves=` blind to pauses) — all fixed; r2: app code CLEAN, 1 test-only P2 (test55 vs the probe ring buffer's elision) — fixed. Steven's rc5 report used the phrase "resize live"; nothing in the rc2 footage or the fixture shows a crop moving, but the blank-then-pop of a cold folder hero is real and is now closed. Gates + landing: see the next entry.
- 2026-09-08 (~18:10 ET): **LANDED — `tvos-shared-extraction` tip `760c0fac` (5 commits: `bf02fd30` bridge zoom, `e3f281d5` UIKit hero layers + rig, `b067fcba` BUG-96 blend, `a09b5a9e` time-based `moves=`, `760c0fac` late backdrop + 20 s artwork timeout), pushed; outer pointer bumped.** Gates: `NuvioTVTests` 194/0 (was 179); Debug + Release simulator builds green; UI on FA87: `test31HeroCommitsOnce` PASS, `RowLeadingEdgeTests` ×2 PASS, `PinnedRowSettleRegimeTests.testW5bColdLaunchLeavesNoTitleHidden` PASS, `test51TrailerBridgeAutoPlay` PASS end to end (bridge with the restored scale-in), `DetailRowAnchorTests` PASS with `moves=1` on all five anchored steps, `test54FolderHeroSwapGeometry` PASS (39–41 Downs to the folder row); `HeroOffLaunchTests.test31DHeroOffRowsWaitForSync` FAILS on FA87 (`rowsWait=toggle` — the fixture's synced profile has Show Hero ON and flips the `-debug.homeHeroOff` launch state after the pull) and PASSES on F38F573A, whose profile matches: fixture mismatch, not this batch; `test55FolderHeroLateBackdropAdopts` PROVED the late path once (`present … backdrop=none waited=2` → `backdrop=late waited=138`, same folder, Genres row) and SKIPS when the walk lands on the Services row while i.postimg.cc stalls (0–32 KB in 25 s, twice today) — the skip rule says so in its message. Codex: r1 four P2 fixed, r2 app code CLEAN + one test-only P2 fixed. Side clone `~/Claude/Projects/NuvioMobile-b96` (BUG-96 agent's build lane) deleted. **Owed:** rc6 cut + DM (held for Christian's go; DM also carries the reworded Row Settle pane-tail ask and the Saga poster-variant question); device pass on the Living Room ATV — one Down = one motion on three titles incl. a series and a Saga title; bridge leave shows the logo scaling into place with the fade; title→folder→title hero swaps with a cold cache (force-quit the app, then focus a folder never visited) show the mosaic arriving late rather than a blank hero; plus the carried batch-9/toolchain items.
- 2026-09-08 ~18:20 ET: **rc6 CUT.** Build number 122 (`7ab5e25f`, tag `tvos-v0.3.0-beta.18-rc6`, on `tvos-shared-extraction`, pushed with the tag); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc6 NuvioCommitSHA=7ab5e25f`; IPA 26,268,565 bytes at https://litter.catbox.moe/jit6b6.ipa (litterbox 72 h ≈ 2026-09-11 18:20 ET, `content-length` verified; copy `~/Downloads/NuvioTV-beta18-rc6.ipa`). DM draft at 5/5: `docs/comms-dm-drafts-2026-09-08-rc6.md` — NOT sent, waiting for Christian's go.
- 2026-09-08 ~18:30 ET: **rc6 DM SENT** to Steven (Reddit chat via ego task space `reddit chat steven`, id 11: `fillInput` on the textbox ref + page-side `btn.click()` on the Send button through the shadow DOM, composer 1693 → 0 chars, verified from a fresh process; no wedge). Asks: commit check `7ab5e25f`, cold-folder check (quit, reopen, land on an unvisited folder), Saga poster-variant question, Row Settle pane TAIL photo. Next: his verdict → beta.18 or another rc; device pass on the Living Room ATV still owed.
- 2026-09-08 ~18:45 ET: **Steven's rc6 VERDICT (DM within ~15 min of the send; video https://fromsmash.com/-_CL8PV-yq-ct + two photos).** FIXED per him: collection wallpapers ("everything works perfectly"); trailer animation ("works really well and looks clean" — he now says it is not the same animation as official Nuvio's, "which is a shame", but very good; he thought something changed between rc2 and rc5). NOT RIGHT: **the description page now scrolls ONCE per Down press but "way too far" — information is skipped and it feels fast** (video); **Saga row: official Nuvio shows the saga images in LANDSCAPE format** (photo) — so the ask was never the title-in-poster, it is the card shape. NEW BUG: on Home, a swipe UP on the Siri remote's touch surface brings up the tab menu (Search, Settings…) while a Up press on the button does not (video, sound on to hear click vs swipe). Row Settle Diagnostics: he photographed the pane again "paying attention to the navigation at the bottom" but cannot scroll further down in it — "the problem will be the same". Trailer sourcing: for War Machine the French trailer is gone and the app shows no trailer at all; official Nuvio falls back to the English trailer. Reply not drafted.
- 2026-09-08 ~23:20 ET: **rc6 VERDICT TRIAGED (video `~/Downloads/IMG_8491.mov`, 66 s portrait, sheets `docs/research/steven-beta18-video-evidence/rc6-video-*.jpg`; photos `docs/research/steven-beta18-photos/2026-09-08-*.jpg`).** (1) **Down scrolls "way too far" (BUG-99, the BUG-96 v3 shape):** t 0–14 s — one Down from the header pins the Casting row at the 108 pt rest, so the whole top block (title, synopsis, buttons, Statut/Released lines) leaves the screen in one move; the official app (t 38–52 s) reveals the next section at the BOTTOM with the description still visible (the engine's minimal reveal), then the Saga row. Decision owed: direction-aware anchoring (Down = engine reveal, anchor only on Up / header straddle) vs a lower rest. (2) **Swipe-up opens the sidebar, click does not (BUG-98):** t 14–34 s — with focus on Home's first row a touch-surface swipe up reveals the FEAT-30 sidebar (Rechercher/Bibliothèque/Addons/Paramètres/Profil); a clickpad Up press does nothing. Code: `SidebarOverlay.swift` — Home's `sidebarUpRevealHandler` and the tab-root modifier's `.onMoveCommand(.up)` both gate the reveal on `SidebarChrome.upIsDeliberate()` (≥0.45 s since `HiddenTabBarFocusBlocker.lastFocusUpdate`, stamped on every `UIFocusSystem.didUpdateNotification`), meant to ignore swipe overshoot; on his hardware the outcome is inverted (the click's own focus update likely lands inside the 0.45 s window while the swipe's move command arrives after it settles). Fix candidates: drop the settle gate (any Up with no target reveals, consistent for both inputs) or key it on `UIPress` type via the UIKit host. (3) **Saga row (FEAT-34):** official Nuvio renders the saga as LANDSCAPE cards (16:9 artwork with the title logo baked in) under "Avatar - Saga"; ours is a portrait `PosterCard` row. Design call owed. (4) **Row Settle Diagnostics pane unreadable past its head (BUG-100):** his photo shows six lines (`0ms regime/plan`, `1165ms regime/plan/lastRow`, `3726ms settle … nudge=-12 bound=44 n=1 UNEXPECTED-WITH-FIT`) and the List row clips the rest — the 28-line tail is unreachable on hardware; fix = a scrollable/tail-first rendering (the hero probe's blob trick is AX-only, not visible). What the head does confirm: `fits=1 vh=523 link=491 rest=32 topR=64 botR=24 botInset=190 lastRowH=393` and the first settle is again a 12 pt pull-up with a fitting frame. (5) **Trailer for War Machine (BUG-101):** the shared `fetchTmdbVideos` already merges the preferred language with an `en-US` fetch (BUG-63), so the list is not empty; the French video is still listed by TMDB, `selectHeroTrailer` ranks it first, its YouTube id is dead, and extraction failure ends the search instead of trying the next candidate — fix = iterate candidates on extraction failure (tvOS trailer path). (6) Bridge: he calls it "very good"; the official leave (t 52–66 s) dims to black with the logo shrinking into a bottom-left caption, then cuts to the trailer — the same shape as ours; no action. (7) Collection wallpapers: "everything works perfectly" — BUG-95 CLOSED on his device. Reply not drafted.
- 2026-09-09 (early, ET): **rc7 BATCH BUILT on `tvos-shared-extraction` (Christian's 09-08 decisions; plan `~/.claude/plans/rc7-batch-2026-09-08.md`; model-delegated: 5 Sonnet implementation agents in two waves by file ownership + 3 Sonnet review-fix agents, Codex 2 rounds, main session verified).** Commits: `0390fb41` BUG-98 (the 0.45 s "deliberate Up" gate removed at all three reveal sites — any Up with no focus target reveals the sidebar, both inputs); `c359400e` BUG-100 (Row Settle pane renders newest-first: reversed tail, marker, reversed head, caption "Newest first; the launch lines are at the end." in en/fr/de/es/it/vi; persistence and the AX blob stay chronological); `c1cdb8da` BUG-101 (`rankHeroTrailers` in shared, `selectHeroTrailer` = its head; both tvOS resolvers walk up to 3 candidates on a nil extraction and retry the next one once after an AVPlayer failure; `candidate=i/n` in the trailer probe; jvm 757 / K/N 777); `173d78f5` BUG-99 (direction-aware anchor: Down = the engine's reveal, then a 0.35 s settle check anchors only when the row's on-screen top `rowTop − contentOffset` is above the 108 pt rest; Up = blend/fallback/verify as before; probe `anchor=<row> top=<content> screen=<on-screen> free`; the UI walk now asserts no Down step above the rest, ≥2 free steps, Up steps at 108 ± 16, `moves ≤ 1` — fixture PASS); `2dee1f54` FEAT-34 (`SagaCard`: 16:9 backdrop, title logo bottom-leading via `ArtworkStore`, text fallback, name + year under the card, `SagaLogoStore` lazy `fetchPreviewEnrichment` per part). Codex r1: 7 P2 + 1 P3 — Up from the unanchored Comments row classified as Down (fixed: `DetailRowAnchor.direction` with `lastKnownRow` + content-offset test), relayout pass owning the previous row's anchor (fixed: per-row `lastAnchored`), trailer repack failure not advancing / `reset()` dropping candidates mid-flight / watchdog vs 3×15 s (fixed: advance on nil URL, keep the in-flight list, `touchExtraction` per attempt), SagaCard overlay padding outside the frame (fixed), `SagaLogoStore` unscoped cache (fixed: language + gates in the key, no caching when gated off, 200-entry cap), short settle buffers not reversed (fixed). One spec slip caught by the A1 agent and fixed by hand: the Down settle check must use `rowTop − contentOffset`, not `+ inset` (157 pt on the fixture would have made every Down read free). Gates and landing: next entry.
- 2026-09-09 ~01:20 ET: **rc7 BATCH LANDED — `tvos-shared-extraction` tip `f8861f41` (8 commits on rc6's `7ab5e25f`: `0390fb41` BUG-98, `c359400e` BUG-100, `c1cdb8da` BUG-101, `173d78f5` BUG-99, `2dee1f54` FEAT-34, then review fixes `a8670b37` anchor / `4038aea6` trailer / `f8861f41` saga+pane), pushed; outer pointer bumped.** Gates: `:shared:jvmTest` 757 / `:shared:tvosSimulatorArm64Test` 777 (+4 each, `HeroTrailerSelectorTest`), `NuvioTVTests` 216/0 (was 199 → +17: decision/direction, motion, displayOrder, SagaCardArt/shouldCommit), Debug + Release simulator builds; UI on FA87: `DetailRowAnchorTests` (Down free ≥2 steps / never above the rest, Up 108 ± 16, moves ≤ 1) PASS, `test52SidebarOverlay` PASS, `test51TrailerBridgeAutoPlay` PASS, `test33SeasonPosterRow` PASS, `test31HeroCommitsOnce` PASS, `RowLeadingEdgeTests` ×2 PASS. Codex: r1 7 P2 + 1 P3 (all fixed), r2 3 P2 + 1 P3 (Comments-origin heuristic → Comments is now a tracked, never-anchored row; retry ownership; stale-scope pending entry; cap on completion — all fixed), r3 2 P2 (rejected hand-off retaining `resolvingKey`; completion-path eviction wiping mounted cards — fixed), r4 CLEAN. Three compile slips caught by the bundle (missing `import Combine`, two `nonisolated` statics) and one probe-format slip (the free-row note printed the screen top where the UI test expects the content-space top) fixed by hand. NOT captured: a simulator screenshot of the new saga cards — three scratch search walks landed on the wrong "Avatar" (1916 film, the 2026 Aang film, then a series episode); FEAT-34 rests on its unit tests + Codex and Steven's eyes. **Owed:** rc7 cut (build 123) + DM (held for Christian's go); device pass — Down on a movie keeps the description visible, Up rests the header at 108, clickpad Up and swipe both open the sidebar, saga cards landscape with logos and year, War Machine shows the English trailer, Row Settle pane photo carries the walk-back settles first.
- 2026-09-09 ~01:30 ET: **rc7 CUT.** Build number 123 (`e3a51787`, tag `tvos-v0.3.0-beta.18-rc7`, on `tvos-shared-extraction`, pushed with the tag); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc7 NuvioCommitSHA=e3a51787`; IPA 26,307,593 bytes at https://litter.catbox.moe/mbuxf8.ipa (litterbox 72 h ≈ 2026-09-12 01:30 ET, `content-length` verified; copy `~/Downloads/NuvioTV-beta18-rc7.ipa`). DM draft at 5/5: `docs/comms-dm-drafts-2026-09-09-rc7.md` — NOT sent, waiting for Christian's go.
- 2026-09-09 ~01:40 ET: **rc7 DM SENT** to Steven (Reddit chat via ego task space `reddit chat steven`: `fillInput` on the textbox ref + page-side Send click through the shadow DOM, verified from a fresh process; no wedge). Asks: commit check `e3a51787`, whether a swipe now opens the menu unintentionally, the wide saga cards (first eyes), War Machine's English trailer, the Row Settle pane photo (top of the pane, newest lines first). Next: his verdict → beta.18 or another rc; device pass on the Living Room ATV still owed.
- 2026-09-09 ~01:50 ET: **Steven's rc7 VERDICT (DM within ~10 min; 4 photos; earlier a video https://fromsmash.com/aScMP6iNcJ-ct showing the Apple TV app's home-menu collection browsing as a suggestion).** FIXED per him: the description scrolling (BUG-99 — "definitely much smoother", though not as smooth as Fusion/Omni, "maybe missing a little smooth transition effect"); the saga row (FEAT-34 — "better this way"; asks for LARGER cards like Nuvio's, ours are the same size as the trailer cards); War Machine's trailer (BUG-101). **REGRESSION (BUG-98 v2): the Up-reveal "is a disaster 😅" — the menu opens no matter where he is in the interface, navigation impossible.** His clarification: the Up BUTTON was fine before and should not have changed (on his device the gated click never revealed and he was happy with that); the real bug is the TOUCHPAD swipe up, which OPENS the menu AND CLOSES IT AGAIN (a flicker, visible in the rc6 video t 14–34 s). Design suggestions (not bugs): a Netflix/Apple TV-style description page (tabs for trailers/similar, a large poster with a shadow like Nuvio's hero — three example photos), the Apple TV app's collection browsing from the home menu (video), and a Reddit post by someone building an Apple-TV-styled Nuvio fork who offers to help (https://www.reddit.com/r/Nuvio/s/lYGHxYReHj). Fourth photo = the Row Settle Diagnostics pane, newest-first. Reply not drafted.
- 2026-09-09 ~02:00 ET: **rc7 photos read** (`docs/research/steven-beta18-photos/2026-09-09-*.jpg`). Row Settle pane, newest-first: only THREE lines fit above the fold once the caption took a line — `42771ms lastRow collection_collection-erfs5gwk…community-2 lastRowH=393 botInset=190 rest=32`, `42771ms plan L403c0p0r0 vh=523 link=491 rest=… topR=64 botR=24 fits=1 botInset=190 lastRowH=393`, `42770ms regime L403c0p0r0 fits=1 from=L403c0p1r0` — a regime/plan re-plan at 42.8 s (hero p1→p0 flip) logged AFTER the walk, so the settle lines are still just below the fold. **BUG-100 v2:** the pane needs paging (several List rows of ~4 lines each, or a focusable scrollable block), not just reordering. Design refs: the Apple TV app's film page (Mayday: hero still, title logo, meta line, synopsis, Regarder/+/note buttons, cast credits right-aligned) and a second example. Decisions owed: BUG-98 v2 (Menu-only reveal vs restore the gate + kill the swipe flicker), saga card size, pane paging, whether to take up the detail-page redesign / Apple TV home-menu collections (FEAT-35/36).
- 2026-09-09 (morning ET): **rc8 BATCH BUILT + LANDED on `tvos-shared-extraction` (Christian's decisions: Menu only, bigger saga cards, paged pane; 3 Sonnet agents in one wave, Codex 1 round CLEAN).** `0c0f184b` BUG-98 v2 — the Up-reveal path is GONE at all three sites (tab-root modifier `onMoveCommand`, Home's hero-carousel Up branch, `sidebarUpRevealHandler`/`SidebarUpRevealModifier`); the sidebar reveals on the Menu press only, Home's hero Up is the pre-FEAT-30 no-op again; test52 reveals via Menu and asserts a lone Up does not. `a673d60d` FEAT-34 follow-up — `Theme.Size.sagaCardWidth/Height` 440×248 (from the official-app photo: three cards across ~two thirds of the width). `a8ac0d66` BUG-100 v2 — `PinnedRowSettleProbe.displayPages` (5 per page, newest first) rendered as one focusable List row per page with a localized "Row Settle Diagnostics · page N/M" header (en/fr/de/es/it/vi); `settle_probe_lines`/`settle_probe_blob` on page 1. Gates: `NuvioTVTests` 217/0, Debug + Release sim builds, `DetailRowAnchorTests` PASS, `test52SidebarOverlay` PASS on rerun (first run died at launch on an AX snapshot timeout before any sidebar code ran), Codex CLEAN. Environmental, not this batch: `test33SeasonPosterRow` SKIPPED ("no multi-season series found in the first five rows" — the fixture's Home order today), `test31HeroCommitsOnce` Leg C failed on the Services folder `erfs5gwk` with `backdrop=none` while i.postimg.cc answered 20 KB in 12 s (the same host stall as 09-08; a rerun is chained after the rc8 build).
- 2026-09-09: **rc8 CUT.** Build number 124 (`c41fb67c`, tag `tvos-v0.3.0-beta.18-rc8`, on `tvos-shared-extraction`, pushed with the tag); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc8 NuvioCommitSHA=c41fb67c`; IPA 26,311,482 bytes at https://litter.catbox.moe/ouonba.ipa (litterbox 72 h, `content-length` verified; copy `~/Downloads/NuvioTV-beta18-rc8.ipa`). DM draft at 5/5: `docs/comms-dm-drafts-2026-09-09-rc8.md` — NOT sent, waiting for Christian's go. test31 rerun result: see the log line appended below by the chain.
  test31 rerun: NuvioTVUITests test31HeroCommitsOnce]' passed (315.237 seconds).
- 2026-09-09: **rc8 DM SENT** to Steven (Reddit chat via ego task space `reddit chat steven`, same fillInput + page-side Send recipe, verified from a fresh process; no wedge). Asks: commit check `c41fb67c`, the Menu-only sidebar, the bigger saga cards, a photo of the first two or three pages of the paged Row Settle pane after a Large walk. Told him the design suggestions get a separate reply later. Next: his verdict → beta.18 or another rc; device pass on the Living Room ATV still owed.
- 2026-09-10 ~11:15 ET: **Steven's rc8 VERDICT read** (DM 09-09 7:46 PM ET + two photos; two new reports 09-10 8:44 / 8:47 AM ET). **BUG-98 v2 (Menu-only) CONFIRMED** — "the menu behavior has been fixed; everything is working well!". **FEAT-34 size still short** — "very slightly larger than before, but still not at Nuvio's size, still too small": 440×248 is not enough; measure the official card in `docs/research/steven-beta18-photos/2026-09-08-official-nuvio-saga-row-landscape.jpg` before the next bump. **BUG-100 v2 paging WORKS** — the two photos (`docs/research/steven-beta18-photos/2026-09-09-row-settle-diagnostics-rc8-pages-1-4.jpg`, `…-pages-3-6.jpg`) show pages 1/6 through 6/6; the walk-back settle lines are photographable for the first time. **Pane decoded** (Large 403 / no captions / hero ON: `0ms plan L403c0p1r0 vh=523 link=491 rest=32 …topR=64 botR=24 fits=1 botInset=60 lastRowH=0`, `3267ms regime L403c0p0r0 fits=1 from=L403c0p1r0`, `3268ms plan … fits=1 botInset=190 lastRowH=393`, `3268ms lastRow collection_collection-erfs5gwk-community-2 lastRowH=393 botInset=190 rest=32`). Settles during the walk, oldest first: `9491ms settle row=tmdb-addon:movie:tmdb.lates… nudge=-12 bound=44 n=1 UNEXPECTED-WITH-FIT` (same row, same −12, as photo-a on 09-07); `14039ms settle row=collection-erfs5gwk-commun…` (tail cut); `14556ms settle row=collection-erfs5gwk-commun… deficit=0 nudge=-94 bound=76 n=1 UNEXPECTED-WITH-FIT`; `16484ms settle row=collection-hftcv0ta-co… deficit=0 bandLo=0 bandHi=48 inBand=1 nudge=0` (in band, no correction); `17226ms settle row=collection-hftcv0ta-commun… nudge=27 bound=103 n=1 UNEXPECTED-WITH-FIT`. Reading: the 09-07 finding holds across a full walk — the frame fits (`fits=1`, `link=491` in `vh=523`) and the engine still rests rows out of band on his AppleTV11,1, `n=1` every time (lands, no oscillation). New this time: the collection rows are the worst offenders (−94 pt on the erfs5gwk folder row, twice the 09-07 −44), and the hftcv0ta row produced the first POSITIVE nudge seen (+27, rested too HIGH) two seconds after an in-band rest on the same row. Belt lines (`belt arm`/`belt grace` alternating on every landing, 10126–19916 ms): margins −4…−16 with slide=intr=4…16 early in the walk, then margins −63…−111 with `slide=72 intr=72` (the title's slide clamp exhausted) on the tmdb / fr.apertaa.top10 / collection rows later — and **`clearanceLift=0` on every one of the ~15 belt lines**. The `BrowseComponents.swift:2738` comment names `lift=0 clearanceLift=26` as the expected shape (Codex r7 P1), so a flat 0 on hardware wants decoding next session: either the focused clearance never publishes on the device or the belt is armed while the corrector is idle (both are the ceiling-backstop cases the belt doc lists). **NEW reports:** BUG-102 (P2) no focus ring on collection tiles with No Zoom on Focus OFF — `FolderTile` never reads `accent_focus_ring`, only the no-zoom still ring exists there; BUG-103 (P3) collection carousel does not reach the screen edges, unlike the Library — surface unclear, photo owed. **His question:** "You don't have an Apple TV at home for testing? Do you only have a simulator?" — the reply should say the Living Room Apple TV 4K exists and that the device passes run there (the rc-by-rc cadence is what makes it look sim-only). Owed: reply DM (SlopMonster), next saga size, BUG-102 fix, BUG-103 photo ask, device pass on rc8.
- 2026-09-10 11:28 AM ET: **REPLY DM SENT** to Steven (`docs/comms-dm-drafts-2026-09-10-reply.md`, SlopMonster 5/5, 157 words; ego task space `reddit chat steven`, fillInput on the composer ref + page-side Send click, verified from a fresh process: one message, no duplicate). Says: latest Apple TV 4K on hand, simulator for quick repro + every build checked on the device; saga size will be measured from his official-app photo (next build); BUG-102 cause found, fix next build; asks which screen BUG-103 is about + a photo; design examples after the bugs. Owed now: rc9 batch (saga size measured, BUG-102), BUG-103 photo, device pass.
- 2026-09-10 ~12:30 ET: **rc9 BATCH BUILT on submodule branch `claude/rc9-batch` (off rc8 `c41fb67c`, main checkout, not pushed): `1841827a` FEAT-34 saga cards 500×281, `7b1c42e0` BUG-102 accent ring on folder tiles + cast avatars, `c407742c` test56 defer.** Saga size MEASURED from `2026-09-08-official-nuvio-saga-row-landscape.jpg` (screen ≈1010 px for 1920 pt; unfocused cards ≈271/≈261 px ≈ 515/496 pt at 16:9, spread = perspective; three cards + gaps ≈84% of the width — the rc7 "two thirds" note under-read the photo); 500×281 gives 3×500 + 2×24 + 60 = 1608 of 1920. BUG-102: `FolderTile`/`CastCard` only knew the no-zoom still ring; new `PlainLabelRing` in PosterCard.swift (accent whenever the setting is on, still ring as the no-zoom fallback, band reserved whenever either is on), native `.borderless` lift kept (`RowCardTreatment.plainBorderless` clearance math). Deliberately NOT applied to the four `TileFocusLift` tiles (hover-effect lift hides overlays on hardware, FEAT-14 finding) → BUG-104 follow-up. Gates: Debug sim build green, NuvioTVTests 223/223 (+6 `PlainLabelRingTests`), test56 PASSED with the ring visible on two focused folder tiles (`docs/research/rc9-sim-evidence/2026-09-10-test56a-folder-ring-zoom-on.png`); Codex r1: 1 P2 (test56 skip before restore → `defer`, `c407742c`), r2 CLEAN (defer verified on both exit paths). Release sim build green. MERGED + CUT (next entry). Owed: DM, device pass (ring under the native lift on hardware; saga size against his eye).
- 2026-09-10 12:28 ET: **rc9 CUT.** `claude/rc9-batch` fast-forwarded into `tvos-shared-extraction` (`c407742c`), build number 125 (`fde6136e`, tag `tvos-v0.3.0-beta.18-rc9`, branch + tag pushed, feature branch deleted); unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc9 NuvioCommitSHA=fde6136e`; IPA 26,308,584 bytes at https://litter.catbox.moe/5cth6p.ipa (litterbox 72 h ≈ 09-13 12:30 ET, `content-length` verified; copy `~/Downloads/NuvioTV-beta18-rc9.ipa`). Cut recipe as a script this time (`cut-rc9.sh` in the session scratchpad: stamped `generic/platform=tvOS` build → PlistBuddy stamp check against `git rev-parse --short=8` → Payload zip without PlugIns → Downloads copy → litterbox → HEAD size check), detached with `nohup … & disown` and watched by a Monitor; 6 min end to end. DM draft: `docs/comms-dm-drafts-2026-09-10-rc9.md` — NOT sent, waiting for Christian's go.
- 2026-09-10 ~14:30 ET: **Steven's replies to the 11:28 DM read (5 messages 11:37 AM–1:46 PM ET, all BEFORE the rc9 DM, which is still unsent).** (1) 11:37 two photos (`docs/research/steven-beta18-photos/2026-09-10-home-*.jpg`) → BUG-103 ROOT-CAUSED: the HOME collection row is clipped at its padded bounds (~60 pt inside the screen edges) while catalog rows bleed — `CollectionRowView`'s `ScrollView` has no `.scrollClipDisabled()`; plus "let me know when you think you've fixed all the bugs in my complete lists" (a full-list reconciliation is owed before beta.18). (2) 12:33 video https://fromsmash.com/DgAMuH~srG-ct (`~/Downloads/IMG_8560.mov`, 70 s, 4K; sheet + frames in `docs/research/steven-beta18-video-evidence/rc8-video-2026-09-10-*.jpg`): 0–10 s **NEW BUG-106** saga focus-lift overlaps the next card (ring mode manual scale grows a 16:9 card ≈71 pt at any size vs the row's 24 pt gap; rc9's 500-wide card does not change it; options in the tracker row — Christian's call); 12–20 s our Search vs 50–70 s official Nuvio's search-as-you-type → **FEAT-37**; 24–46 s the collection rows (BUG-103, also 38 s Services row clipped). (3) 12:38 **FEAT-38** OLED true-black option. (4) 1:45 **BUG-105** depth effect missing on collection tiles — `FolderTile` never applies `.nuvioCardDepth`. (5) 1:46 "Sorry, I just say things as I see them 😅". Smash recipe note: the leaf-text click landed on a `<style>` node the first time; the second `js()` dispatch on the same page did start the download (page text flips to "Your download has started"), the .mov landed ~90 s later. rc9 DM redrafted to acknowledge all of this (`docs/comms-dm-drafts-2026-09-10-rc9.md`, still NOT sent).
- 2026-09-10 ~15:30 ET: **rc9 FIXES BUILT (Christian: fix his three reported bugs and fold them into rc9 before it goes out; the rc9 already cut as build 125 is superseded — re-cut as build 126 with the rc9 tag moved).** Submodule branch `claude/rc9-fixes` off `fde6136e`: `7ed5d7f4` BUG-106 saga row gap `landscapeLiftRowGap` 40 (derived from the lift: 2 × 20 × 16/9 ≈ 71 pt of width growth on any 16:9 card; 24 → overlap, 40 → ≈4.4 pt clear); `cac97118` BUG-103 `.scrollClipDisabled()` on `CollectionRowView`'s ScrollView (no `RowLeadingEdgeClip`, trailer-only concern) + BUG-105 `.nuvioCardDepth` on `FolderTile` before the ring-band scaleEffect. Video fully watched (five 1-fps sheets, `watch-A..E`): Home rows 24–48 s confirm the clip on the Genres first tile and the Services fourth tile with the Top 10 row bleeding beside them; the 30 s "Genres/Drame" ghosting is camera blur during a hero crossfade; the official app's Genres row (48 s) bleeds both edges; its search (52–70 s) refreshes a Popular row per keystroke (FEAT-37 reference). Side finding BUG-107: his genre tiles are black boxes, the BUG-38 cover fallback never resolves on his device. Gates: Debug sim build green, NuvioTVTests 223/223, Codex r1 CLEAN (math confirmed 35.59 / 4.41 pt). test56 rerun PASSED (ring + depth rail visible on every folder tile, `docs/research/rc9-sim-evidence/2026-09-10-test56b-folder-ring-and-depth-rail.png`; the edge bleed cannot show on the fixture — its seven Genres tiles fit inside the safe area at Medium — so BUG-103 rests on the modifier parity + device pass); Release sim build green.
- 2026-09-10 14:51 ET: **rc9 RE-CUT as build 126.** `claude/rc9-fixes` fast-forwarded into `tvos-shared-extraction`, bump `665fc031`, tag `tvos-v0.3.0-beta.18-rc9` MOVED `fde6136e` → `665fc031` (force-pushed; build 125 never reached Steven, its DM was unsent), branch pushed, fixes branch deleted. Stamp `NuvioBetaTag=tvos-v0.3.0-beta.18-rc9 NuvioCommitSHA=665fc031`, IPA 26,311,195 bytes (`~/Downloads/NuvioTV-beta18-rc9.ipa` overwritten with build 126). **Hosting hiccup:** litterbox answered every upload with HTTP 500 (four attempts over ~8 min); catbox proper accepted and returned `https://files.catbox.moe/66o5ny.ipa` but stored a ZERO-byte object (the known catbox failure — it dedups by hash, so the same file cannot be re-uploaded there). Litterbox recovered on the 8th attempt (~15:05 ET): **https://litter.catbox.moe/sxpipj.ipa** (72 h ≈ 09-13 15:00 ET, `content-length` 26,311,195 verified). **rc9 DM SENT 3:07 PM ET** (`docs/comms-dm-drafts-2026-09-10-rc9.md`, 5/5; fillInput + page-side Send, verified one copy). Owed: his verdict + the device pass (ring under the native lift, saga gap, row bleed, depth rail).
- 2026-09-10 ~22:30 ET: **Steven's rc9 (build 126) VERDICT read** (DM 5:17 PM ET + 5:34 / 5:52 (two photos) / 5:54 follow-ups; photos `docs/research/steven-beta18-photos/2026-09-10-rc9-genres-*.jpg`). **OK:** saga cards (FEAT-34), collection scrolling to the edges (BUG-103), depth effect (BUG-105); saga overlap (BUG-106) not mentioned → treated as fixed. **Not OK → BUG-108 (P2):** ring mode on the collection tiles (and cast avatars) lifts the artwork while the ring stays — the FEAT-14 overlay-vs-system-lift finding, now confirmed for the `.borderless` lift BUG-102 kept; the "white outline" is his cover's own white border sliding under the static ring. Fix = BUG-93 architecture (`RingCardButtonStyle` + `CardArtworkFocusLift(.manualScale)` on the tile's artwork+ring group); check `RowCardTreatment` for the pinned collection row first. **BUG-107 downgraded:** the black tiles are his own cover art (white border + outlined lettering, present in the rc8 photos too). **NEW:** BUG-109 leaving a folder page returns focus to the row above the collection row; FEAT-39 intermediate poster size; FEAT-40 folder page title centred, larger, persistent. **His priorities for what's left:** (1) Home description on 1–2 lines (the carousel's `HeroSlotGive` tier-3 ceiling by design; the hero-OFF panel got 3 lines — Christian's call on the carousel), (2) titles bounce at Medium / disappear at Large (BUG-87/89: the `UNEXPECTED-WITH-FIT` hardware rests, `clearanceLift=0` on every belt line still undecoded), (3) the tab bar (old format) stays visible on scroll (BUG-66, hardware-only). "Overall, it's coming along really well 👍". Reply not drafted.
- 2026-09-11 (early ET): **rc10 BATCH BUILT, model-delegated** (plan `~/.claude/plans/glimmering-brewing-umbrella.md`; 3 Explore + 1 Opus Plan agent for design, then 3 Sonnet execs in Wave 1, 1 Opus in Wave 2, 1 Sonnet in Wave 3; main session built/tested/committed per wave). Submodule branch `claude/rc10-batch` off `665fc031`: `d1c3c7ee` **BUG-108** FolderTile + CastCard on the BUG-93 architecture in ring mode (`CardButtonStyleKind`, `PlainLabelRing.lift`, `CardArtworkFocusLift(.manualScale)` + caption drop + zIndex on the LazyHStack child, landscape-aware collection gap 40, `RowCardTreatment.plainBorderless` deleted as inert, `.id(folder.id)`); `fb8ff545` **BUG-109** `PinnedRowSettle.hostCovered` from Home's NavigationPath — no settle applies while Home is covered, re-arm on uncover; `5a388ce4` test56 geometric oracle (folder_artwork/folder_card rects: rise 20 ± 2.5, layout box still); `8cf22828` **BUG-66 probe** `TabBarStateProbe` + About pane "Tab Bar Geometry Diagnostics" (inert unless `debug.tabBarStateProbe`; `.tabBarMinimizeBehavior` is unavailable on tvOS so no one-liner exists); `6c96b7c7` **BUG-87/89** `topReachFloor(lift:)` = 62 + lift + 4 → 86 zoom-on / 66 No Zoom at Large, plan/regimeKey take `FocusModeFlags` (`z` suffix), `Clearances.focusedRaw` + `clearanceLiftRaw=` + `LIFT-DEFICIT` stand-down (Christian's call: take the clearance); Wave 3 synopsis: `Theme.Font.bodyLineHeight` measured (system ≈35, Open Sans ≈39.5) drives `synopsisLineLimit` with a 1 pt tolerance + `debug_hero_synopsis` probe. Gates per wave: Debug sim builds green; NuvioTVTests 228 → 231 → 233 all green; test56 + test57 PASSED on FA87 (evidence `docs/research/rc9-sim-evidence/2026-09-10-rc10-test56a-*.png`, `…test57a/57c…png`). Findings recorded by the executors: at Large the panel synopsis drops to 2 lines in BOTH modes under Open Sans (Wave 3 keeps 3 for No-Zoom under the system font; a 3-line Open Sans slot needs ≈118.5 pt — decision owed); a measured title taller than 42 pt would trip the LIFT-DEFICIT stand-down (intended handoff, comment says 38 pt); the carousel at Large now sits exactly on its 70 pt compression cap. **Codex r1: 7 findings** (5 P2 / 2 P3), all fixed by two fix agents (Opus: scheduler + floor; Sonnet: probe + classifier + test), commit `ba16aab9`: (1) uncover armed a settle nobody scheduled → the rearm token now goes to the registered scheduler (`armed=false` when none); (2) covered work consumed correction state → rejected BEFORE `settlePlan()` (`rejectCoveredSettle`, one probe line per push), cover begins with `invalidateEpoch`; (3) **LIFT-DEFICIT tripped by Open Sans' 42.2 pt section title** → the floor reads `Theme.Font.sectionTitleLineHeight` (nonisolated, UIFont metric; Open Sans + zoom on caps at 88 with `focusedRaw` +1.8, `fits` true; the 38 pt constant is now the test reference only); (4) the synopsis probe added hero height → overlay; (5) tab-bar classifier checked the bottom edge (tvOS bar is at the top) → `state=expanded|minimized|partial|unknown`; (6) test56 skipped on a missing lift → poster-row control decides fail vs skip, exactly one lifted tile asserted; (7) synopsis tests depended on the host font → explicit `titleHeight`/`mode`. Fix gate: Debug build green, NuvioTVTests 235/235; Release build green; test56 + test57 rerun PASSED. **Codex r2: 4 findings** (2 P2 / 2 P3), fixed by two Sonnet agents: (1) a System ↔ Open Sans switch changed the Large plan under the SAME regime key, inheriting the previous font's disarms → `regimeKey` appends `t<titleHeight>` (`L403c0p1r0z1t38`); (2) test56's transform control could sample the Continue Watching row and blank → sampled on every Down press; (3) the tallest-rect heuristic picked a resting poster over a lifted square in mixed-shape rows → per-aspect-ratio grouping; (4) synopsis tests read the host's live font → explicit line heights. Gate: Debug build green, 235/235, test56 PASSED; fixes committed `4aa52141`; Release build green. **Codex r3 hit the account's usage limit** ("try again at 4:52 AM", 16k tokens in) → **internal round 3 by an Opus reviewer with the same brief** (the 08-26 quota-gap precedent): all three r2 fixes VERIFIED (no consumer parses the regime key; every synopsis helper caller passes a metric; test56's grouping cannot pick a resting tile and the control cannot latch on a non-lifted layout), plus 4 findings (1 P2: the LIFT-DEFICIT stand-down fired on ANY negative `focusedRaw`, but the floor reserves the font METRIC while the clearance uses the LIVE rendered title, so a CJK/Thai/Arabic fallback face a few points taller would disarm the corrector for the session → tolerance `< -fadeIntrusionArm`; 3 P3: TabBarStateProbe could not re-arm/never invalidated its timer/MainActor closure/armer mounted in Release → disarm + `#if DEBUG`; `folder_artwork` comment misstated its rect; an unreachable test56 branch reused a misleading skip). Fixed by one Sonnet agent (`62dc4d83`); final gate Debug + 235/235 + Release green. **MERGED** fast-forward into `tvos-shared-extraction`, build 127 `1f5121d0`, tag `tvos-v0.3.0-beta.18-rc10` (local until the IPA is verified). Cut launched 02:55 ET (first launch died: the cut script takes the log path as `$1`). **rc10 CUT 03:00 ET:** stamp `NuvioBetaTag=tvos-v0.3.0-beta.18-rc10 NuvioCommitSHA=1f5121d0`, IPA 26,332,528 B at **https://litter.catbox.moe/2sjrq2.ipa** (72 h ≈ 09-14 03:10 ET; litterbox 500 on the first try, second attempt verified; copy `~/Downloads/NuvioTV-beta18-rc10.ipa`); branch + tag pushed, `claude/rc10-batch` deleted. **rc10 DM SENT** (`docs/comms-dm-drafts-2026-09-11-rc10.md`, 5/5; one copy verified). Owed: Steven's verdict + the two pane photos (Row Settle after a Large walk, Tab Bar Geometry), the device pass, and the full-list reconciliation before beta.18.
- 2026-09-11 ~15:00 ET: **Steven's rc10 (build 127) VERDICT read** (DM 1:21 PM ET + 3 photos + 1:57 PM note). **OK:** BUG-108 zoom on collections, BUG-109 folder-exit focus. **Not OK per him:** description lines "still the same" (regime `t37` = system font, Large, hero ON → the carousel's 1 line by design; the hero-OFF panel at Large + zoom on is ~1 line after the accepted clearance trade); titles bouncing at Medium / disappearing at Large "same thing". **Pane decoded (`L403c0p0r0z0t37`, `topR=85 botR=24 fits=1`, carousel `vh=525 link=512 rest=13`, panel `vh=544 rest=32`):** the clearance fix holds — every belt line `clearanceLift=4`, band `[-4, 48]`, middle rows `inBand=1 nudge=0`; the ONLY offender is the LAST collection row (`lastRowH=414 botInset=171`): parked ≈84 pt out of band → `nudge=-84 bound=67 UNEXPECTED-WITH-FIT` → in band → `pullback=1` → again (−85, −83) → `disarmed=1`. So the remaining bounce/hide is the last-row deep park (BUG-89's original symptom), not the clearance. **Tab Bar Geometry pane: DEAD** — `0ms NOT-FOUND state=unknown mode=classic reason=up/down`: the armer is `#if DEBUG` since the round-3 fix and rc builds are Release, so the sampler never armed (rc11 fix). **FEAT-39 re-asked** with a new data point: official Nuvio shows 4 description lines with a poster slightly larger than Medium — the intermediate size is the one change that resolves clearance + lines + size together (Christian's call). Also: his collection images stopped loading today in BOTH apps ("not because of your app") — an artwork-host outage, not ours; ignore in the next diagnosis. Reply not drafted; decisions owed: FEAT-39 vs a Large-only re-balance, and the last-row rest design.
- 2026-09-11 (evening ET): **rc11 BATCH BUILT, model-delegated** (plan `~/.claude/plans/glimmering-brewing-umbrella.md`, rewritten for rc11; 2 Explore + 1 Opus Plan for design; Wave 1 = 2 Sonnet + 1 Haiku in parallel). Submodule branch `claude/rc11-batch` off `1f5121d0`. **Root cause of the last-row deep park (BUG-87/89 remainder):** tvOS reveals the focused LABEL frame only; the label sits `Spacing.lg` below the row top, so the engine tolerates `margin ∈ [24, 24 + viewport − labelFrame]`. Middle rows (label = `plan.linkFrame` 512.33, vh 525) get `[24, 36.67]` — inside the band, hence `inBand=1`. The last row's hidden-title SQUARE folder tiles (artwork = `style.width` 268.89) have a 377.89 label → tolerated `[24, 171]`, so the observed rest at margin 128 was LEGAL and every corrector nudge was undone (the rc5 trailing inset only gave the corrector range; `bound=67` on the firing line was the DOWNWARD bound, a reporting artifact). **Fix (1A):** `EnvironmentValues.rowCardLinkFrameFloor` published by HomeView on `model.rows.last` in pinned mode, applied as `.frame(minHeight: plan.linkFrame, alignment: .top)` inside every card label (catalog ×3, folder, Upcoming, CW); `PinnedRowGeometry.lastRowLinkFrameFloor(plan:)`; `pinnedLastRowHeight` = shelf pad + max(natural, floor) + pad (last row 548 tall, inset 168.67); settlePlan last-row exemption when shaped + `roomUp=` on firing lines; tests (extra 134.44 for the square tile, deepest shaped margin inside the band; test48 leg B asserts `nudge=0 pull=0`). **1B:** probe armer mounted in every build (`why=no-window|no-tabbar`). **1C FEAT-39:** Medium+ at 134 dp (351 pt; Christian's call — the mobile 'comfort' preset; takes the Large dial: reach 86/24, compression 37.95, fits, `P351…` regime; 3 system / 2 Open Sans synopsis lines — one fewer than Medium, to be told honestly; 4 lines + a bigger poster would need `heroPinnedRowsViewportBudget` to move). Gate: Debug build green, NuvioTVTests 239/239. Commits: last row, Medium+, probe. UI legs: Large fixture + test48 PASSED, Medium+ fixture test PASSED (`w≈234`), Medium restore PASSED — BUT the fixture's regime is `L403c1p0r0z1t37 fits=0` (Large + captions + carousel, the documented non-fitting one), so the last-row line read `last=1 lastRowShaped=0 corrN=1`: the shaping is inert there by design, and the harness had no Hide Titles switch → new `FixtureSetupTests.testSetHideLabelsOn/Off` (`52ef0e60`, drives the Appearance toggle, verifies `c0`/`c1` in the regime key). **Codex r1: 3 findings** (`9cd04d34`): the floor `= plan.linkFrame` still permitted margin up to `24 + restRange` = 56 on the hero-off panel (band ceiling 48) → floor is now `max(linkFrame, viewport − Spacing.lg)`, confining the engine to `[24, 48]` by construction, with a 64-regime containment sweep (one assertion corrected after 12 false failures: the floored label must fit the viewport, not the viewport minus the shelf pad — Small + captions already exceeds that today); test48 leg B asserts `corrN=0` and a `pull=` baseline; the regime test's Medium gate is 212–228 with an `M`-tag check. **Codex r2: 1 finding** (fixed `2439d5fe`; **Codex r3 CLEAN**, all six commits / 14 files, 35 geometry tests passed on its side; gate after the fix: Debug green, NuvioTVTests 241/241, Release green): the exemption keyed on a positive floor alone — in Landscape Rows mode a last collection row can hold a PORTRAIT folder whose natural label (535) exceeds the landscape plan's floor (431), so the exemption would swallow a legitimate ~47 pt correction → gate on `lockupExtent ≤ floor (+ shelf pad)` via a pure `lastRowExemptionApplies` predicate + regression test. Gates so far: NuvioTVTests 240/240. Owed: fix gate, Codex r3, Release, UI sequence in a fitting regime (Hide Titles ON + Large → test48 → restore OFF + Medium), merge, build 128, rc11 cut, DM.
- 2026-09-11 ~17:15 ET: **rc11 UI GATE, fitting regime — the simulator cannot prove the in-band half; it proves parity.** (1) Harness: the first fitting-regime attempt (`rc11-ui-c`) lost `testSetPosterSizeLarge` to "Lost connection to the application (pid 6799)" — no crash report, the sim log shows a normal spawn and an exit 3.5 s later, and the same test passed on every later run (harness hiccup, not a crash). (2) Premise conflict: test48's Leg A (the device deep-park gate) needs captions ON (`L403c1p0r0z1t37 fits=0`, the row must clip on the artwork), while the rc11 last-row contract only exists in a FITTING regime (`fits=1`, `lastRowShaped=1` — Large + Hide Titles ON on FA87); with Hide Titles on, Leg A reads PREMISE UNREACHABLE (`margin=-56 net=0`, fully clipped). So the last-row walk moved out of test48 into `test58LastRowFrameFloor` (shared `walkToLastRow`/`readSettleLine` helpers; test48 keeps BUG-89's `prevHidden`/`inBand`/`beltFaded` checks and asserts the rc11 trio only opportunistically when `lastRowShaped=1`). (3) **Finding (both regimes):** the FA87 simulator's focus engine parks EVERY row out of band, not just the last one — zoom on, Steven's own regime `L403c0p0r0z0t37`: middle rows rest `margin=-12 intrLifted=8 inBand=0`, `nudge=12 UNEXPECTED-WITH-FIT` on each, two `pullback=1` → `pbDisarm=1`, every later title fades; No Zoom `L403c0p0r0z1t37`: the same at `margin=-22`. His rc10 pane shows the Apple TV parking the middle rows `inBand=1` in that exact regime, so the sim is ≈12 pt deeper than hardware and cannot stand in for it here. The rc11-shaped last row rests at `margin=-13` (zoom on) / `-18` (No Zoom) — one to four points from the middle rows, `lastRowShaped=1 prevHidden=1 rowH=548` — which is the design's claim (the floor hands the last row the middle rows' rest interval). test58 therefore asserts PARITY (|last − median(middle)| ≤ 8), shaping and `prevHidden` unconditionally, the corrector trio only while the corrector is still armed, and the band only when the walk's middle rows sit in band (attached notes otherwise); the in-band/no-nudge half of the contract is Steven's rc11 Row Settle pane. The No Zoom variant is logged on BUG-87 as an open follow-up (reach 66 vs 86: keep the lift allowance in No Zoom, or give the pullback brake a per-row budget). (4) test48 at Large + captions: PASSED on the rc11 tip (`rc11-ui-l`: Leg A deep rest `margin=-100 net=-28 intr=45`, belt fired; Leg B healthy `margin=1`; last row `margin=44 lastRowShaped=0`, unshaped path noted) — one earlier run (`rc11-ui-h`) hit PREMISE UNREACHABLE because the down→up re-entry parked top-visible (`margin=24`) that time; the park varies run to run on this fixture (2 of 3 today reproduced it). Fixture restored to captions on + Medium after every sequence. (5) **test58 PASSED on the final tip (`rc11-ui-n`, 17:10 ET): last row `margin=-13 lastRowShaped=1 prevHidden=1 rowH=548`, middle-row median −12, parity within 1 pt; the corrector-disarmed and simulator-out-of-band notes attached as designed. Test commit `721e9f02`.
- 2026-09-11 ~17:45 ET: **rc11 CUT.** Codex r4 on the test commit (`721e9f02`) found 3 (even-count median, the entry rest could be the last row, a missing `pbDisarm=` silently skipped the corrector checks) → fixed `4f77a5aa`, **Codex r5 CLEAN**. Fast-forwarded into `tvos-shared-extraction`, build 128 `40e8060f`, tag `tvos-v0.3.0-beta.18-rc11`, branch + tag pushed, `claude/rc11-batch` deleted. Unsigned Release build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc11 NuvioCommitSHA=40e8060f`, IPA 26,341,818 B at https://litter.catbox.moe/b8syph.ipa (litterbox 72 h ≈ 09-14 17:40 ET, accepted first try, `content-length` verified; copy `~/Downloads/NuvioTV-beta18-rc11.ipa`). Final test58 run on the tagged tip PASSED (`rc11-ui-r`) — note for the record: the first post-fix rebuild (`rc11-ui-build5`) raced the `git checkout tvos-shared-extraction` that preceded the fast-forward (the checkout put the rc10 tree on disk for a second and the compiler read it), producing a bundle with zero test58 symbols and an `Executed 0 tests` run; never rebuild across a branch switch. DM draft `docs/comms-dm-drafts-2026-09-11-rc11.md` (5/5, 256 words) NOT SENT — Christian's go owed. Owed after his verdict: Row Settle pane at Large (the last row's in-band half), Tab Bar pane photo, Medium+ look; device pass (last row at Large/Medium, Medium+, folder ring lift); the No Zoom follow-up on BUG-87.
- 2026-09-11 ~23:00 ET: **rc11 DM SENT** (10:59 PM ET, Christian's go with the simulator sentence removed; one copy verified from a fresh process, composer 0 → 1320 → 0). Three unread Steven messages sat above it, read after the send: (1) 6:17 PM — "Other notes": add the movie logo to the feed selection (FEAT-42); the STUDIO tiles zoom the artwork instead of the card ("you fixed it for the cast — it works fine now", BUG-111 = BUG-108's shape on the studio row); Card Depth vivid/subtle/off show no difference (BUG-110); his logo collection works now. (2) 6:28 PM — "For the depth effect, Nuvio implemented this — maybe you can do the same" + photo of official Nuvio's Fine-tune Depth panel (Edge glow 30 %, Sheen 10 %, Edge coverage 20 %, preview card, reset) → FEAT-41, saved as `docs/research/steven-beta18-photos/2026-09-11-depth-effect-official-nuvio.jpg`. (3) 8:13 PM — "I don't experience the bouncing effect on average when I move slowly through the lobby" → BUG-87 note (press-cadence dependence; ask for a slow-walk and a fast-walk pane pair). Reply not drafted — Christian's call whether to fold an acknowledgement into the next DM.
- 2026-09-12 ~10:00 ET: **Steven's rc11 (build 128) VERDICT read** (DMs 09-11 11:37 PM → 09-12 8:41 AM ET, 5 photos, 1 Smash video NOT yet downloaded: https://fromsmash.com/2WVg-7X8.l-ct, 7-day expiry ≈ 09-18). **Medium+ (FEAT-39): "absolutely perfect", it is the size he uses on official Nuvio** — the video and his daily use are at Medium+; he switched to Large only for the pane. **Row Settle pane (2 photos, pages 1–5 and 5–9, newest-first) — regime `P351c0p0r0z0t37` (Medium+, Hide Titles ON, carousel, zoom on, system font), NOT Large despite his note; `vh=492 link=460 topR=85 botR=24 fits=1`, last row `lastRowH=504 botInset=188 rest=32` (shaped).** Decode: middle rows mostly `margin=-8 slide=8 intr=4 clearanceLift=4` → `bandLo=-4 bandHi=48 inBand=1 nudge=0` (the clearance trade holds at Medium+); BUT the walk carries seven `UNEXPECTED-WITH-FIT` settles (`bound=93 roomUp=2221`, `bound=207 roomUp=133`, `bound=72 roomUp=681`, `bound=93 roomUp=2659 n=2`, `bound=139 roomUp=2982`, `bound=122 roomUp=1531`, `bound=141 roomUp=2619`) on catalog AND collection rows in the middle of the walk (margins −35/−40/−41/−42/−60), one `belt fire … margin=-13 reason=rest armedFor=833ms` + `belt recover`, and the NEWEST end (135618 → 132464 ms, the return to the top) shows FOUR consecutive rows parked at −102…−108 (`fr.apertaa.top10.custom`, `tmdb series`, `tmdb movie`, collection `3612bd0d…`) — `slide=72 intr=68` each, i.e. the row above lands ≈100 pt too high on the way back up. That matches his new report: **NEW BUG-112 (P2)** "my genre collection (the one right at the top) no longer appears when I scroll down and back up through my catalogs; I have to press Back to make it appear" — the top rows park under the hero on the walk back (rc11 regression candidate: the `rowCardLinkFrameFloor`/last-row shaping or the settle exemption; verify on the fixture at Medium+ with Hide Titles ON). **Tab Bar Geometry pane (1 photo, pages 1–4, 35 s):** the probe is ALIVE in rc11 — `0ms minY=46 h=68 alpha=1 hidden=0 state=expanded mode=unknown reason=arm`, ticks at `minY=45`; `9509ms reason=down minY=-267 state=minimized scrolledDown=1 mode=classic`, then minY tracks the content (−404 → −1426 → −2677 → −2917 → −2421 → −1936 → −1427 → −707 → −411), `27245ms reason=up minY=-111 state=minimized scrolledDown=0`, `28006ms+ minY=46 state=expanded`. `alpha=1.00 hidden=0` on every line: the bar is never hidden or faded, its frame scrolls with the content. **His BUG-66 read: "the top menu bar no longer appears split — looks fixed"; new note: it disappears when scrolling down right after launch, but after switching to another tab it stays visible all the time** (BUG-66 note: the classic bar scrolls off in the launch-time Home state and pins after a tab switch — two different scroll-view containments; the pane above is the launch-time one). **Still not OK per him:** descriptions on a single line (carousel by design), catalog names still disappear, Medium titles still bounce on a fast scroll (BUG-87/89 remainder; his 8:13 PM cadence note applies). **rc10 note (11:52 PM):** after a long session rc10 reproduced the older "collection does not return to where I left off, scrolls back to the top of the catalog, even after closing and reopening the app" bug; NOT seen in rc11 so far → logged as BUG-113 (P3, watch — no repro steps, not reproduced on rc11). **Personal (11:58 PM):** he asks what country Christian lives in and how old he is ("I'd like to know a little about who I'm talking to every day") — Christian's call, not for a drafted reply. **NEW FEAT-43 (8:35–8:41 AM, 2 photos):** "Does your app support landscape mode within collections?" — in official Nuvio, with landscape rows on, opening a collection shows the same interface as Home (hero: title logo + metadata + synopsis + backdrop; landscape rows per sub-catalog with row headers; first card a landscape card with the title logo — official's **"Follow layout"** disposition), while ours opens the folder as the chip-filter poster GRID ("tab mode": Tout / Pixar Animation Classics / Walt Disney Animation Gems / DreamWorks Heavyweights / Illumination Blockbusters, portrait posters). Photos saved: `docs/research/steven-beta18-photos/2026-09-11-row-settle-diagnostics-rc11-mediumplus-pages-1-5.jpg`, `…-pages-5-9.jpg`, `2026-09-11-tab-bar-geometry-rc11-pages-1-4.jpg`, `2026-09-12-collection-landscape-official-nuvio-follow-layout.jpg`, `2026-09-12-collection-landscape-ours-grid-mode.jpg`. Owed: the Smash video download + frame triage (Medium+ row behaviour, the top-bar disappear-then-pin sequence), BUG-112 repro on the fixture, tracker rows BUG-112/113 + FEAT-43, reply DM (Christian's go).
- 2026-09-12 (early ET): **rc12 BATCH BUILT, model-delegated** (plan `~/.claude/plans/glimmering-brewing-umbrella.md`, rewritten for rc12; 4 Explore + 3 Opus Plan for design; Wave 1 = 3 Sonnet in parallel + 1 Sonnet after). Submodule branch `claude/rc12-batch` off rc11 `40e8060f`. Scope (Christian, 09-11): BUG-110, BUG-111, the BUG-87 No Zoom follow-up, FEAT-42; FEAT-41 excluded. **BUG-111** (`baeeff03`): `CompanyChip` was the last card-like tile on a bare `.borderless` button with no treatment of its own — it now takes the plain-label ring architecture (ring on the capsule's true bounds, `CardArtworkFocusLift(.manualScale)` outermost, the row's `@FocusState` as the focus truth, `.cardFocusButtonStyle()` + zIndex); `PlainLabelRing.smallLabelRise` caps the lift's scale at ×1.12 for small labels (a 52 pt chip would otherwise grow ×1.77) via an optional `rise` on `CardArtworkFocusLift`; no ring band inset (the chip's outer band is white padding wider than the ring), no card depth (no `companies` surface in the shared Kotlin enum). **BUG-110** (`fd5aee31`): the three Edge presets differed only by the alpha of a 1 px line at Full coverage and 1/2/2 pt at Top/Half (BUG-83's two-band rule) — the renderer now separates them on three channels: width 1/2/3 pt at every coverage, top alpha 0.35/0.60/0.90, and a halo stroke under the crisp rail (0/3/5 pt, alpha ×0.18, suppressed on a focused card when the 4 pt ring band is reserved); Edge gains an "Off" preset (0 was already a legal synced value); storage contract and preset values untouched; DEBUG knobs `-debug.cardDepthOn/Edge/Coverage/Sheen`, `debug_env railW=/railA=`. **FEAT-42** (`21e916a9`): the hero already rendered a logo bitmap and fell back to text only when no URL reached the focused item before the commit; `SagaLogoStore` → `TitleLogoStore` (own file), `HeroArtResolver.logoPlan` (own logo → folder → store URL → metahub for IMDb ids → `.pending` → `.none`), a `.pending` plan waits for the URL inside the existing 400 ms art deadline and never adopts late (BUG-90), the URL never enters the payload, the lookup fires from the dwell (a fast scrub spends nothing) plus a once-per-row prewarm; probe `logoSrc=` / `plgs=`. **No Zoom follow-up** (`1ca77c31`): Option C — the geometry ships INERT behind About → "No Zoom Row Reach (A/B)" (default OFF, 5 locales); the flag moves the REACH FLOOR only (`plan`: `floorLift = max(lift, 20)` when effective in No Zoom; `focusLiftAllowance` stays 0 — routing it through the lift would leave `bandLo` at −4, the Wave 7/BUG-93 error), `regimeKey` appends `h1` only when effective; cost stated (carousel restRange 31.67 → 11.67; hero-off panel at Large 3 → 2 system synopsis lines; Medium+ unchanged); rc13 promotes or deletes on Christian's device A/B. Gates: Debug + Release green, NuvioTVTests 286 → 295 (one nonisolated fix on `key(for:)`, one URL-premise fix: Foundation's parser now percent-encodes spaces and even NUL, so the "unparseable" case uses `http://[::1`), test50/test56/test31/HeroFolderSwap unchanged and green, **test60 PASS** (railW 1/2/3 triptych), **test61 PASS** (with the hold, No Zoom rows rest `margin=-12` with clearance 24, in band, no corrections; the control leg reproduces `bandLo=-4`). **Codex r1: 2 P2 + 2 P3** → `997c2da2` (No Zoom's neutral still ring was 85 %-white inside a 92 %-white capsule → `PlainLabelRing.stillColor(onLightSurface:)` gives this one label a dark neutral; test59 walks to the `logos` anchor only — its first run measured the header because the probes sit in the AX tree regardless of focus — and fails on a missing lift) + `873ce97e` (a `.pending` plan restarted the 400 ms budget on a same-title payload update → `HeroArtResolver.resolveDeadline` inherits the remainder; test62 requires `plgs=tmdb plg=1` somewhere on a full forward+back row walk; the first run's `plgs=none` was a real no-logo title, `tt37521549`: metahub 404, TMDB `logos: []`). **Codex r2: no P1/P2, 4 P3 test hygiene** → `bdf18276` (focusable-chip count, unconditional boundary traversal, an online metahub check before calling a logo-less fixture a regression, the `plgs`/`plg` invariant on every read). Sim proof after the fixes: **test59 PASS** (screenshots show the accent ring riding the lifted "20th Television" chip, then "Hulu" after Right), **test62 PASS** (`tt39019156 plgs=tmdb plg=1` at card 3 of the first row — the metahub guess missed and the store's TMDB lookup landed in the same commit). **Codex r3** on the test commit: 1 P2 (the unconditional walk left focus on row 2, so the warm leave-and-return compared a different card) + 2 P3 → `ec87530a` (warm read from the walk table; the metahub-404 skip removed — this fixture has store-resolvable titles, so a logo-less walk is a regression signal; test59 re-snapshots after the confirmation press). **Codex r4**: 2 P2 on the warm/fallback logic → `feab397a`, which also made test62 DETERMINISTIC: the `plgs=tmdb` proof had been racing the Kotlin row overlay (a third run read `addon` on all 16 cards, the store had no miss to repair), so a DEBUG knob `-debug.heroLogoStoreOnly` makes `logoPlan(... allowMetahub: false)` the only path and the test requires ≥ 1 resolved card with unresolved ≤ 50 %; test59 tries up to four titles. **Codex quota out at 5:32 → internal Opus rounds.** Under the knob test62's first run ended in **SIGABRT** (`Kotlin_ObjCExport_runCompletionFailure`): the Swift side called the unchecked `suspend fun fetchPreviewEnrichment`, which Kotlin/Native treats as unhandled on any non-cancellation throw → `144af9f8` (`@Throws(Throwable::class)` twin `fetchPreviewEnrichmentChecked`; `TitleLogoStore` no-latch on failure with a pure `completionOutcome` seam; `enrichIfNeeded` switched too). The internal review of that commit then showed the named causes are already swallowed inside Kotlin by `runCatching`, and named the real suspect: `TmdbMetadataService`'s ten plain `mutableMapOf` caches written on the caller's thread AND inside `withContext(Dispatchers.Default)` while the row prewarm fires a row's lookups concurrently → `d58a14eb` (`cacheMutex.withLock` on all 22 accesses, none across a network call — Codex r6 CLEAN on the locking; three more checked twins for the person page and entity browse; a 30 s per-key retry cooldown; `d714e991` prunes expired timestamps). Two more test-only rounds (`7794af86`, `706959eb`: the reviewer's own "wait for a non-dash anchor" fix was unsatisfiable — the anchor names a row only after the first Down — reverted to "page mounted"). **Final gates on `706959eb`/`d714e991`:** Debug + Release green, NuvioTVTests 305, test50/56/60/61/31/HeroFolderSwap green, **test59 PASS ×3, test62 PASS ×3 (11 of 17 cards resolved through the store under the knob, no crash)**. One interrupted fixer's partial test edits (the last internal round's P2s on a cold-row warm path and the trailer-cover retry) are STASHED, not committed — follow-up. 09-12 09:54 ET: the release chain fast-forwarded `tvos-shared-extraction`, committed the build-129 bump `d903c63a` and tagged `tvos-v0.3.0-beta.18-rc12` LOCALLY, then was stopped 30 s into the Release build on Christian's instruction — **no IPA, no upload, nothing pushed; the cut waits for his ok.**
- 2026-09-12 ~10:45 ET: **rc11 video DOWNLOADED + TRIAGED** (`~/Downloads/IMG_8685.mov`, 189.6 MB, 60 s, 4K30; triage `docs/research/steven-rc11-video-triage-2026-09-12.md`, frames in `docs/research/steven-rc11-video-frames-2026-09-12/`). Recorded at Medium+, Hide Titles ON, carousel, zoom on. **BUG-112 reproduced on camera (36.5–41.6 s):** focus on row 2 (Nouveaux films), four Up presses do nothing (hero and focused card unchanged), the Genres row is fully under the pinned hero; Back rebuilds the top-of-Home state with the tab bar. Hypothesis: the rc11 up-walk deep park (−102…−108 on the pane's newest four rows) puts the row above entirely behind the hero overlay, and tvOS will not focus an occluded item — repro on FA87 at Medium+ / Hide Titles ON, check the `rowCardLinkFrameFloor` environment scope and the settlePlan exemption. **"Catalog names disappear" = the same up-walk park + the belt fade** (titles readable on every row on the way down; hidden or faded on the way up; the last row's title fades at rest at 31.5 s via `belt fire reason=rest`). **Tab bar:** launch-time Home scrolls the bar off with the content (the pane's minY walk); after a Settings visit the bar stays pinned over the hero at every depth (50–59 s). Medium+ looks right at every depth. Not in the video: Medium bounce, BUG-113, FEAT-43.
- 2026-09-12 ~11:00 ET: **BUG-112 SIM REPRO + ROOT-CAUSE PASS (model-delegated: 2 Explore maps, 1 Sonnet test writer; builds/runs in the main session).** New `NuvioTVUITests.test63Bug112FirstRowReachableAfterUpWalk` + `test64Bug112UpWalkWithCompressionOff` (shared `runBug112UpWalk`, UNCOMMITTED on `tvos-shared-extraction` @ `d903c63a`, 249 lines after test61): Medium+ + Hide Titles ON + zoom on (fixture regime `P351c0p0r0z0t37 fits=1 vh=492 link=460`), one Down, `walkToLastRow` (37 rows on FA87: row 1 = Upcoming, 2–3 catalog, 4 = folder collection `erfs5gwk`, 6 = folder collection `hftcv0ta`, last = a catalog row), Up back to row 2, then Up ×3 with a per-row down-vs-up table. Evidence `docs/research/bug112-sim-evidence-2026-09-12/`. **Findings:** (1) **The focus failure does NOT reproduce on the simulator** — both legs PASSED: Up from row 2 reached row 1 (`row=upcoming margin=143`, seq advanced) even though row 1 was entirely above the rows viewport (rowTop −70, row 360 tall → [−430, −70]); the up-walk also entered the folder row `erfs5gwk` from below while it was fully off-screen. So "clipped/off-screen ⇒ unfocusable" is wrong on this engine; the device no-op needs a device-only cause (see below). (2) **The up-walk deep park exists on the sim in the fitting regime and is regime-bound, not floor-bound:** test63 (rc11 dials) — down rests `margin=0` (row 2 −22), up rests `margin=-22` on EVERY catalog row, uniform from the first Up press; the folder row `erfs5gwk` rests +83 SHALLOWER on the way up (44 → 127) and the row above it is back at −23, so the engine re-rests each row on its own reveal (no surplus offset carried up from the last row — H1's mechanism refuted; the fixture's last row is a catalog row, floor excess 8 pt, `lastRowShaped=0` on every non-last row — H4 refuted). test64 (`-debug.pinnedHeroCompressionOff YES` → `fits=0 vh=455 link=483`, floor OFF, pre-BUG-87 reaches): down 8 / up 7 on every row, `inBand=1 nudge=0 pull=0`, delta ≤ 1 — **no asymmetry at all**. So the engine's Up-reveal rests deeper than its Down-reveal only under the rc10/rc11 fitting dials (reach 86/24 + compression 38, label 461 in a 492 viewport), and the corrector cannot repair it: on the sim it is already `pull=2 pbDisarm=1` by row 3 of the DOWN walk (row 2's −22 nudge pulled back twice). (3) **On the device the up-walk deep park predates rc11:** re-read of the rc10 pane (`…rc10-pages-1-4.jpg`, Large) shows the DOWN walk at −30/−52/−62/−36/−52 and the UP walk at `hftcv0ta −100, top10 −75, series −96, top10 −48, tmdb.latest −101` (22264–24382 ms) — the 09-11 decode's "middle rows in band" was the last row's post-correction lines, not the walk. rc11's pane repeats it at Medium+ (−102…−108 on the walk up from the last row, 132464–135618 ms, ending on row 2 = `tmdb.latest`, the BUG-112 moment; `erfs5gwk` = his LAST row "Services de Streaming", `hftcv0ta` = his row 1 "Genres"). **Root cause of the deep park + hidden titles on the way up:** tvOS's reveal on an Up move rests the row deeper than on Down (22 pt sim / ≈80–100 pt device) in the fitting regime; the settle corrector is disarmed by early pullbacks (`bound` caps it, two pullbacks → `pbDisarm`) so nothing restores the band; the belt then fades/hides the titles. Not an rc11 regression; rc11's floor is confined to the last row. **Root cause of the Up no-op at row 2: device-only, not proven.** Candidates, ranked: (a) LazyVStack culling of row 1 at the device's deeper offset (row 1 at [−532, −153] vs the sim's [−430, −70]; the sim keeps rows realized) — one `NSLog` on `FolderTile`/`CatalogRowView` `onDisappear` would settle it; (b) the classic tab bar in its scrolled-off state (hardware-only per the BUG-30/66 history) being picked as the Up candidate and refusing focus — the sim never minimizes the bar, so untestable here; (c) the hero CTA — excluded on the sim (Up went row→row). **Fix direction regardless of (a)/(b):** an Up fallback on the first content rows mirroring the Menu path (`onExitCommand` at `HomeView.swift:823`): `onMoveCommand(.up)` fires only when the engine moved nothing → `scrollTo("home_top")` + focus row 1; plus a corrector re-arm on direction change (pullback disarm is session-wide today) so the up-walk rests are corrected on the device. Fixture restored (Hide Titles OFF → Medium). Tracker rows BUG-112/113, FEAT-43 still owed.
- 2026-09-12 ~13:00 ET: **BUG-112 FIX BUILT, model-delegated** (1 Opus Plan for the specs, 2 Sonnet executors in parallel by file ownership, 3 Sonnet fix agents for Codex rounds; builds, sim legs and commits in the main session). Submodule branch `claude/bug112-fix` off the rc12 bump `d903c63a`: `dcc0352b` test63/test64 (the repro tables), `7e5c5bcb` **Item B** — `PinnedRowSettle.PullBackLedger`, the pull-back brake per WALK DIRECTION (sign of the offset delta across a row hop; re-arm on a reversal; the verify-miss latch released on a reversal only with pull-back evidence; hard stop at 2× the per-direction budget), `plannedCorrection` extracted pure, `dir=`/`rearm=` on every settle line right after `fits=`, `noteExternalScroll(reason:)`; 11 unit tests (`PinnedRowSettleDirectionTests`). `fb2cd2cb` **Item A** — Home Up fallback: one `.onMoveCommand` on the rows ScrollView (delivered only for moves the engine did not consume) → `PinnedRowFocusRequest` environment value each row applies to its OWN `@FocusState` via `.pinnedRowUpFallbackTarget` (new `PinnedRowUpFallback.swift`; catalog, collection, Upcoming, CW rows), 4-rung ladder (focus request → scroll + retry → scroll to `home_top` + retry → hero hand-off for the topmost target only, `giveup` deeper), row ownership reported from the rows' focus bindings (See All included) through `PinnedRowFocusOwnership`, one `endUpFallback` lifecycle (landed / superseded / restarted / cancelled / giveup / hero), `noteExternalScroll` before every programmatic scroll; probe `upFallback row= prev= action=` on the console + the About Row Settle pane + DEBUG `debug_upfallback`; DEBUG-only Play/Pause proxy trigger behind `-debug.homeUpFallbackForce` (test65). **Codex:** r1 7 findings (verify-miss latch stranded on split pull-backs; retries stealing focus after success; See All leaving the row key stale; hero rung without the scroll notice; late-mounted targets missing the request; the proxy Release-reachable; field placement) → fixed; r2 2 (obsolete requests surviving a diversion/hero escalation; cancellation not retiring the ladder) → fixed (`endUpFallback` + captured origin); **r3 CLEAN**. **Gates:** Debug + Release builds green, NuvioTVTests 316/316, UI: test65 PASS ×3 (`action=scroll anchor=home_top` then `landed`), test63 PASS ×3 at Medium+ / Hide Titles ON — **with Item B the up-walk rests are 0 on every row (were −22), `inBand=1`, `dir=-1 rearm=1`, `pbDisarm=0`**; test58 + test48 at Large PASS; fixture restored (captions ON + Medium). **Sim finding on the ladder:** the direct `@FocusState` write into the off-screen row 1 never landed within 0.3 s; rung 2 (scroll then re-ask) did — expect the same on hardware, i.e. ≈300 ms before the reveal starts. Not merged, no cut; Christian's call whether it rides rc12 (the peer session holding rc12 was told). Device pass owed: walk Home down and back up at Medium+ with Hide Titles ON, then Up from row 2 → the Genres row must appear (pane: `upFallback … action=`), and the Row Settle pane must show `dir=-1 rearm=1` with up-walk rests in band (`nudge` ≤ `bound`, so ≈ −12 residual is possible on the deepest rests).
- 2026-09-12 ~19:30 ET: **rc12 CUT with BUG-112 folded in (Christian's go).** The three `claude/bug112-fix` commits rebased onto the rc12 batch tip `d714e991` (now `9a54ae2d` tests, `ab7c3ef0` Item B, `6fa1e1a0` Item A), the build-129 bump re-applied on top as `243da21b`, `tvos-shared-extraction` reset there, tag `tvos-v0.3.0-beta.18-rc12` moved, branch + tag PUSHED (origin was `40e8060f`). Unsigned Release device build stamped `NuvioBetaTag=tvos-v0.3.0-beta.18-rc12 NuvioCommitSHA=243da21b`, IPA 26,407,169 B (`~/Downloads/NuvioTV-beta18-rc12.ipa`). Litterbox answered HTTP 500 on every attempt for the first ~20 minutes (the 09-10 pattern) — retry loop running, link recorded below once accepted; catbox proper deliberately not used. DM draft `docs/comms-dm-drafts-2026-09-12-rc12.md` (template from the rc12 session + the BUG-112 paragraph + the pane ask; the country/age question left for Christian): lint 5/5 → Codex cleanse (3 hunks, every fact kept, "in rc12" added to the A/B-toggle line) → 5/5. NOT SENT. The rc12 session (`nuvio-tvos-f6`) stood down for the fold/cut and will resolve `stash@{0}` afterwards.
- 2026-09-12 ~20:30 ET: **rc12 IPA HOSTED on filebin** — litterbox stayed down for over an hour (its home page and API answered HTTP 500 even for a 3-byte probe; 41 attempts), 0x0.st has closed uploads ("AI botnet spam"), pixeldrain now requires an API key, file.io and temp.sh rejected the API calls; filebin and gofile both accepted. **Link: https://filebin.net/nuviotv-rc12-c3597933e81b/NuvioTV-beta18-rc12.ipa** (bin `nuviotv-rc12-c3597933e81b`, expires 2026-09-20 00:25 UTC; verified by a full re-download: 26,407,169 B, md5 `a03879073c7a3867d405b85d5f434ada` = local). Backup page: https://gofile.io/d/6IvQQKaJ (server-side md5 identical). DM draft updated with the link, still 5/5, NOT SENT — Christian's go owed.
