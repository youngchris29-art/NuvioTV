# Nuvio tvOS — Claude Code project notes

## Upstream sync status (auto-checked daily against NuvioMedia/NuvioMobile)

A scheduled task diffs this fork's `NuvioMobile` submodule (`upstream` remote = `github.com/NuvioMedia/NuvioMobile`, branch `cmp-rewrite`) against upstream daily and logs actionable ports to `docs/upstream-port-plan-YYYY-MM-DD.md`. Latest run: **2026-09-06**, see `docs/upstream-port-plan-2026-09-06.md`. Upstream **moved**: `9ee9da09` → `68337ffa`, 14 real commits + 3 merges, no version bump this window, all read in full via `git show`. **One LOW/mechanical action item this run:** `8b43fd89` "perf(startup): avoid redundant collection and catalog processing" fixes two composeApp files that have confirmed-unported `shared/` extractions — `HomeCatalogParser.kt`'s metas-loop used `forEach { ... return@forEach ... }` to enforce `maxItems`, which only skips the current iteration and never actually breaks the loop, so it kept scanning the whole catalog after the cap was hit; fixed to a `for`/`break` loop. `CollectionRepository.kt`'s `import()` parsed the same JSON payload twice (`parseToJsonElement` then a separate `decodeFromString` on the raw text); fixed to `decodeFromJsonElement` off the already-parsed tree. `find shared -iname` confirms both files are the tvOS extractions of the exact upstream files, and `grep` on the shared copies shows both pre-fix patterns still present verbatim; `grep -rln HomeCatalogParser`/`CollectionRepository` against `iosApp/NuvioTV/` confirms `HomeView.swift`/`HomeViewModel.swift`/`CollectionsUI.swift` all call into them, so this is live on tvOS's Home hot path, not dead code — pure perf (extra CPU/allocation per catalog load), no correctness bug, no user report. Mechanical two-file port, safe to batch with other small work. Everything else in the batch confirmed not applicable: five Android-only player/downloads/foreground-service fixes (ExoPlayer, WorkManager, ATV-irrelevant); a Compose-recomposition resume-position staleness fix in mobile's `StreamsScreen.kt` (tvOS's `StreamPickerView.swift` computes resume position fresh at the call site with no memoization layer to go stale); an iOS Compose-Multiplatform-mobile-only GIF-decoding fix (`CollectionCardRemoteImage.ios.kt`, a different codebase from tvOS's native SwiftUI); four Compose-UI-only perf/skeleton-loading commits (no Compose layer exists on tvOS); a `composeApp/build.gradle.kts`-scoped Compose-1.12 dependency alignment (the shared `kotlinx-serialization`/`coroutines` catalog bumps apply project-wide automatically, no port action needed); and a continuation of the parked Supporter-perks-v1 member-gradient theming into player/profile screens (stays parked). Submodule pointer re-verified with no drift (outer pointer `25e07e08` matches `origin/tvos-shared-extraction`'s fetched tip exactly; this sandbox's own local submodule checkout was briefly stale at the pre-rc2 tip before `git fetch origin`, unrelated to fork drift).

Prior run: **2026-09-05**, see `docs/upstream-port-plan-2026-09-05.md`. Upstream **moved**: `d4891ffa` → `9ee9da09`, 1 real commit (+ version bump + store publish), read in full via `git show`. **No urgent action items this run** — `4f79bfe0` "fix(player): original audio language preference" is the composeApp-looking-but-shared-has-a-copy trap again: `shared/.../player/PlayerTrackSelection.kt`'s `findPersistedAudioTrackIndex()` is confirmed byte-identical to upstream's pre-fix (stale dub/original-variant matching), but `grep -rn "findPersistedAudioTrackIndex" iosApp/NuvioTV/` returns nothing — tvOS has no "restore explicit per-session audio track pick" feature, so the function is dead code on tvOS today; **LOW, opportunistic** port next time anyone touches that file for something else, not a standalone task. Second half of the same commit (mobile's `MPVPlayerBridge.swift` moving audio-language preference from reactive post-track-list `selectAudioTrack()` to proactive pre-init mpv `alang` option) lives in the *mobile* app's own bridge file, not `shared/`; tvOS's `MPVPlayerView.swift` `autoSelectPreferredTracks()` still uses the older reactive pattern mobile just moved away from — **LOW, investigate-before-building**: watch for an audible/visible audio-track switch shortly after playback start on a multi-audio-track title on tvOS; no user report of this to date, so it's a "look for it next time you're in this file" item, not scheduled work. `PlayerLanguagePreferencesKt.resolvePreferredAudioLanguageTargets` (the actual original-audio-language target resolution) is untouched and already correct on tvOS. Submodule pointer re-verified with no drift (`tvos-shared-extraction` @ `cf2f674e`, matches outer pointer exactly; the submodule's working-copy checkout being on WIP branch `claude/steven-beta17` @ `05d6749e` for the in-progress Codex round 8 work is unrelated active feature work, not sync drift).

Prior run: **2026-09-04**, see `docs/upstream-port-plan-2026-09-04.md`. Upstream **moved**: `9b09045f` → `d4891ffa`, 5 real commits via 4 merge PRs, all read in full via `git show`. **No action items this run** — all 5 confirmed not applicable: Android User-Agent/DASH-403 fix (`a48cc79e`, `composeApp/src/androidMain/` only, no `shared/` counterpart, no tvOS symptom evidence); two Compose subtitle-picker bug fixes (`1f2b8e1d` fling-tap, `136d467d` duplicate-track selection) both confirmed Compose-UI-only via `grep -rl` against `shared/` returning nothing — tvOS's native SwiftUI picker has no fling gesture to break, and `MPVPlayerView.swift` already dedups addon subtitles by URL at ingestion, so upstream's fix state is already tvOS's behavior by a different mechanism; el/es locale string files (`f6d4ddfa`, `bf37caa4`), no logic change. Fork submodule pointer re-verified with no drift (`tvos-shared-extraction` @ `cf2f674e`, one commit past the beta.17 tag, matches outer pointer). Carried, unchanged: upstream-batch8 device pass still owed (non-default auto-play-source-scope case only), 4 upstream-report candidates still unfiled (Simkl precedence, FNV size-prefixing, CatalogRepository harder guard, TMDB putIfAbsent→getOrPut), Supporter perks v1 + subtitle min font size still parked/deferred.

Prior run: **2026-09-03**, see `docs/upstream-port-plan-2026-09-03.md`. Upstream **had not moved**: still pinned at `9b09045f` (zero new commits since 2026-09-02). No action items that run. Fork submodule pointer re-verified with no drift (`tvos-shared-extraction` @ `3b140cc7`, matches outer pointer).

Prior run: **2026-09-02**, see `docs/upstream-port-plan-2026-09-02.md`. Upstream **moved** again: `312d499e` → `9b09045f`, 3 real commits, no merges, all read in full via `git show`. **One real action item this run.** **[MEDIUM, live autoplay/binge bug] Autoplay source-loading-scope fix** (`58864ec1`, upstream #1825): upstream refactored composeApp's `StreamsRepository.kt` auto-play flow — five duplicated ad-hoc "early binge-group match" blocks (cache-check, debrid-availability, debrid-prepared-stream, 60s timeout, final fallback) replaced with `evaluateAutoPlay()`/`settleAutoPlay()`/`updateAutoPlayAfterStreamsChanged()` plus a new `areAutoPlaySourcesLoaded()` policy (`StreamAutoPlayLoadingPolicy.kt`) that actually checks the user's configured `StreamAutoPlaySource` scope (all/installed-addons-only/enabled-plugins-only) before deciding sources have finished loading. **Confirmed unported and live on tvOS** — this is the recurring "composeApp-looking path, but `shared/` has its own copy" trap: `shared/.../streams/StreamsRepository.kt` (903 lines) is the shared extraction of the same file and still has the pre-fix duplicated pattern verbatim (5x `selectAutoPlayStream` + 1x `evaluateAutoPlayStream` call sites); `StreamsViewModel.swift` + `NextEpisodeAutoPlay.swift` both consume it, so tvOS's stream-picker autoplay and up-next auto-continue can fire against a source outside the configured scope, or fail to fire promptly. Port is mechanical — `StreamAutoPlaySource`, `AddonStreamGroup`, `StreamAutoPlaySelector` already exist in `shared/`; add `StreamAutoPlayLoadingPolicy.kt`, refactor `StreamsRepository.kt`'s `load()` to the consolidated pattern (diff line-by-line, don't blind-copy — this file has fork-only divergence from past ports), port `StreamAutoPlayLoadingPolicyTest.kt` into `shared/commonTest`. Not applicable this run: `92466c4e` (mobile player-icon alignment — tvOS is the reference style being copied, nothing to port) and `9b09045f` (mobile swipe-to-brightness gesture floor 0.02f→0f — composeApp-only Android/iOS mobile gesture code, no tvOS display-brightness API to gesture-control). No new upstream-report candidates; the 4 from 2026-08-30 remain unfiled. **09-02 run (`docs/upstream-port-plan-2026-09-02.md`): upstream `312d499e` → `9b09045f`, one action item (`58864ec1` auto-play source loading scope) — BUILT same day on `claude/upstream-batch8` (tip `3b140cc7`, MERGED into `tvos-shared-extraction` + pointer bumped, Codex clean r3); the daily note's "live-relevant" claim was corrected in the addendum (the shared block is inert on tvOS; the real tvOS fix is in `NextEpisodeAutoPlay.swift`).**

Prior run: **2026-09-01**, see `docs/upstream-port-plan-2026-09-01.md`. Upstream **moved** again: `6ceffbbe` → `312d499e`, 7 real commits + 3 merges, all read in full via `git show`. **One real action item this run.** **[MEDIUM, possible live degradation for anime skip-intro] ARM→Simkl ID-resolution swap** (`f212242a` + `6de79141`): upstream replaced the third-party ARM API (`arm.haglund.dev`) used to resolve anime IMDB/MAL/Kitsu/TVDB IDs for the skip-intro/outro feature with a new `SimklIdResolver` built on `api.simkl.com` — confirmed unported: `shared/.../player/skip/SkipIntroApi.kt` still has `ARM_BASE` + six ARM resolver functions, `SkipIntroRepository.kt` still uses `ArmEntry`/`resolveImdbEntries()`, no `SimklIdResolver.kt` exists in `shared/`. Live-relevant: tvOS's native player (`MPVPlayerView.swift:801`, `NativePlayerScreen.swift:187`) calls `getSkipIntervalsForContentId()`, which for `mal:`/`kitsu:`-prefixed anime content IDs routes through the exact ARM-dependent functions upstream is replacing — non-anime (plain IMDB) skip-intro is unaffected. Port is mechanical: `shared/` already has `SIMKL_API_BASE_URL`, `SimklConfig`, and `httpGetText()` that the new resolver depends on; skip the `ExternalPlayerLaunchCoordinator`/`PlayerScreenRuntimeEffects` half of upstream's diff (composeApp-only, no tvOS external-player-launch concept). Not applicable this run: `a4b5e29b` (large-episode-list virtualization, Compose `LazyColumn`-only, no tvOS equivalent — native SwiftUI `List`) and four Bulgarian-locale string commits (non-English localization file, no logic change; one incidentally strips now-orphaned `community_membership_*` keys for the parked Supporter perks v1 feature). No new upstream-report candidates; the 4 from 2026-08-30 remain unfiled. Carried unstarted from 2026-08-31: the addon season-poster/certification-fallback batch (`22096a1e` + `0504af98`) and the multiline TTML cue regex fix (`50ef6a84`), plus two manual UX checks (`085e8dc6`, `4026ec92`). **→ ALL of the above (this run's item, the carried 08-31 batch, both UX checks, and the LOW pause-description item) were BUILT the same evening on submodule branch `claude/upstream-batch7` and MERGED into `tvos-shared-extraction` on 2026-09-02 (tip `a87ab03f`, pushed, pointer bumped) — Codex settled r5, all gates green, **device pass PASSED 09-02**; see the OUTCOME ADDENDUM in `docs/upstream-port-plan-2026-09-01.md` and the 'Current open action items' bullet below. Device pass owed.**

Prior run: **2026-08-31**, see `docs/upstream-port-plan-2026-08-31.md`. Upstream **moved** again: `e68cbb08` → `6ceffbbe`, 10 real commits + 3 merges, all read in full via `git show`. **Two real action items this run — first actionable batch since 2026-08-27.** (1) **[MEDIUM] Addon metadata parsing gap**: port `22096a1e` end-state (season posters, specials-aware via `MetaVideo.season`, supersedes the simpler `f5726012` intermediate) + `0504af98` (ageRating → `app_extras.certificationLocal` → `certification` fallback chain) into `shared/.../details/MetaDetailsModels.kt` + `MetaDetailsParser.kt` (both confirmed unported), plus upstream's new `MetaDetailsParserTest.kt` cases; also check whether tvOS's season-poster UI actually consumes the new field once added. (2) **[MEDIUM, possible live subtitle bug] Multiline TTML cue regex** (`50ef6a84`): upstream swapped `Regex(pattern, setOf(RegexOption.IGNORE_CASE, RegexOption.DOT_MATCHES_ALL))` for inline-flag `Regex("""(?is)...""")` in `PlayerSubtitleCueParser.kt` — confirmed unported in `shared/`, and the commit message ("support multiline ttml cues") implies the named-RegexOption form may not reliably apply on Kotlin/Native, which `shared/` targets for tvOS; port the regex change and verify with a Kotlin/Native (not just JVM) test before assuming this was cosmetic. Two more upstream commits investigated and judged **not mechanical ports but worth a manual UX check**: `085e8dc6` (delayed addon content states, #1819 — check if tvOS Home/Search/Library flash empty-state while an addon manifest is still loading) and `4026ec92` (persist next-episode-card dismissal, #858 — tvOS's `NextEpisodeEngine` phase/`cancelled`-flag design looks structurally safe but its only `cancel()` call site found was backward-seek, not an explicit dismiss gesture; confirm tvOS even has a dismiss action for the up-next card before concluding parity). Remaining commits not applicable: `fbca95ce` (subtitle-reset-on-source-change, Android/Compose-runtime-only — tvOS's fresh-`PlaybackContext`-per-episode design looks immune, spot-checked not exhaustively), `802db48d` (theme gradient on updater banner — parked Supporter perks v1 follow-on), version bump + store publish. No new upstream-report candidates; the 4 from 2026-08-30 remain unfiled. Spot-checked (not full re-audit) that the two 2026-08-28 device-pass-owed batches (`upstream-batch6`, `subtitle-engine`) show no regression in current `shared/`.

Prior run: **2026-08-30**, see `docs/upstream-port-plan-2026-08-30.md`. Upstream **moved** again: `6318f0e4` → `e68cbb08`, 7 real commits + 1 merge, all read in full via `git show`. **None applicable to tvOS — zero action items.** Breakdown: i18n(el) strings for the already-not-applicable mobile device-link-code flow; a GitHub Actions + Python script batch automating Android sideload-source updates (no tvOS equivalent); an Android launcher-icon binary resize (70 `.webp` files); a version bump/store-publish pair; `feat(player): apply member gradients to player accents` (`b86932b9`) — a follow-on to the **parked Supporter perks v1** feature, `composeApp/`-only, stays parked but note upstream is now extending it into the player, not just profile/settings; and `fix(tmdb): use multiplatform map insertion` (`501c61d9`, swaps JVM-only `putIfAbsent` for `getOrPut`) — checked `shared/.../tmdb/TmdbMetadataService.kt` and found the fork's own CJK-fallback port (landed in upstream-batch6, 2026-08-28) **already avoids `putIfAbsent`** with an explicit "Fork deviation" comment, so the fork fixed this before upstream did — **new upstream-report candidate** (now 4 unfiled, alongside Simkl precedence, FNV size-prefixing, CatalogRepository harder guard from 2026-08-26/28). Spot-checked (not full re-audit) that the two 2026-08-28 device-pass-owed batches (`upstream-batch6`, `subtitle-engine`) show no regression in current `shared/`.

Prior run: **2026-08-29**, see `docs/upstream-port-plan-2026-08-29.md`. Upstream **moved** for the first time since 2026-08-27: `1b84ee47` → `6318f0e4`, one real commit (`388e613c` "feat(auth): add six-character login codes"). Verified by reading the full diff, not the commit message: it touches only `composeApp/` (new `DeviceLinkAuthRepository.kt` + `DeviceLinkAuthSection.kt`, plus `AuthScreen.kt`/strings.xml) and adds a "sign in with a code approved on another device" option to the mobile/desktop email-password screen (`start_device_login_session` RPC → same `poll_tv_login_session`/`tv-logins-exchange` polling/exchange tvOS's own QR flow already uses). **Not applicable to tvOS** — tvOS's `TvLoginRepository.kt` already implements the 10-foot-UI-appropriate equivalent (QR code, no typing), and the new RPC is additive on the backend with no compatibility impact on `TvLoginRepository`. No action item. This run also re-verified (by reading current `shared/` file contents) that both 2026-08-28 batches below are genuinely landed — `visionos` client swap, `aggregate_credits`, Simkl `JsonNull` guard, and `AddonSubtitleStartupMode` all confirmed present/absent as expected; both remain **device-pass owed only**, not a coding task.

Prior run: **2026-08-28**, see `docs/upstream-port-plan-2026-08-28.md`. Upstream had **not moved** since 2026-08-27 — still pinned at `1b84ee47`, zero new commits that day. This run re-verified (by reading current `shared/` file contents, not trusting prior notes) that all 6 items from the 2026-08-27 batch were **still unported** as of that check (since resolved same-day evening, see batch6 note below), and that the carried 2026-08-25 HIGH subtitle/player-engine batch (`StartupMode` still present in `shared/`) was also still unstarted at that point. The A2 crash-cluster fixes (reported ported 2026-08-26) were spot-checked and confirmed still present, no regression. Same 6-item batch as 08-27: (1) **[HIGH, possible live break]** `InAppYouTubeExtractor` preferred client swap `android_vr`→`visionos` (upstream `c72a3825`) — YouTube may have blocked/deprecated `android_vr`, meaning in-app trailer playback could be degraded right now; bundled with `isDefaultAudioTrack` dub-track skip logic; (2) **[MEDIUM]** trailer dedup + language-preference tiebreaker (`HeroTrailerSelector` `distinctBy`, `MetaTrailer.iso6391`, TMDB trailer-list sort) — same upstream commit as #1, port together; (3) **[MEDIUM]** TMDB CJK/Romaji cast-crew name fallback, 3-commit chain (`c66e80e6`→`e6314ba2`→`754605cb`) — person bio+name, filmography, network-browse pages, port as one batch; (4) **[MEDIUM]** `tv/{id}/aggregate_credits` instead of `tv/{id}/credits` for full-series cast/crew (`0aac4dbe`); (5) **[LOW]** Simkl `stringValue()` JsonNull guard (`e39084d6`), mechanical; (6) **[MEDIUM]** scrobble guard for short placeholder/error-clip durations (`31f5d0e6`) — shared `isProgressComplete()` half is a clean port and `WatchProgressRules.kt` picks it up for free, but tvOS's native player completion call site (near `MPVPlayerView.swift`) needs its own guard — upstream's fix lives in Compose-only `MainAppContent.kt` with no tvOS equivalent, so this needs investigation, not just a mechanical port. All confirmed unported by reading current `shared/` file contents on `claude/beta15`, not commit messages. Full detail, batching suggestion, and verification checklist in `docs/upstream-port-plan-2026-08-27.md`.

Prior run: **2026-08-26**, see `docs/upstream-port-plan-2026-08-26.md`. Upstream moved `f62de092` → `582ae863` (16 new commits + 2 merges). Same "path-only looks like `composeApp/`-only, but `shared/` is an extraction of it" pattern — three commits have confirmed-unported `shared/` targets. **ALL THREE RESOLVED SAME-DAY (2026-08-26 evening session, submodule branch `claude/bug75-ports` off `claude/beta15`):** (1) Simkl list-mutation anime-type PORTED (response-first repair precedence — deliberate divergence from upstream `5003d298`, matches the fork's own history path; upstream-report candidate); (2) Discover catalog persistence PORTED (storage + registry + both profile-switch fan-outs wired); (3) season-guard CLOSED NOT APPLICABLE (tvOS has no preferred-episode concept — `EpisodesSection.swift` verified). The same session ported the carried 08-25 MEDIUM crash-cluster four (atomicfu map incl. a fork-only 13th site; FNV signature HARDENED with size-prefixed collections — upstream-report candidate; trySend; regex-escape) and `MetaVideo.rating`; `96fb98c4` SKIPPED as already-superseded by the fork's harder guard. AND it fixed **BUG-75** (the afternoon sweep's Reddit CW/Simkl report, renumbered from "BUG-72" after colliding with the local 08-23 DM-batch IDs): cross-platform tracking-source sync (`tracking_source_shared` namespace + ordered sync step, dead outbox removed), shared provider-aware CW row builder for Home + Top Shelf (limit-300 parity), coordinator armed at launch + wipe cascade. Gates: 505 jvm + 516 tvOS-native tests green, sim build green, clean cold launch on FA87; Codex loop **CLEAN at 13 rounds** (10 on 08-26, +internal 8-angle review during the quota gap, rounds 11–13 on 08-27 — failed-pull restore, preferences mutation lock, paired edit-record reads); interactive sim smoke moved to the owed device-pass punch list. Outer `main` UNFROZEN (three sweep branches merged, 26f20d8 → pushed). Original findings for the record: **New this run, three items:** (1) **[MEDIUM]** Simkl list-mutation anime-type fix (`5003d298`) — `SimklResolvedListMutation` needs `resolvedMediaType`/`animeType` fields (the history/scrobble path already has this from an earlier port; only the list-add/move path is missing it), confirmed via direct read of `shared/.../simkl/SimklMutationReceipt.kt`; (2) **[MEDIUM]** Discover catalog-selection persistence (`ab57cf1b`) — `resolveDiscoverCatalog()` + `DiscoverSelectionStorage` expect/actual don't exist in `shared/.../search/SearchRepository.kt`, confirmed via grep, so tvOS's Discover tab forgets the last-picked catalog on every cold launch; also needs a `ProfilesViewModel.swift` wiring check for `SearchRepository.reset()` on profile switch; (3) **[LOW, needs investigation first]** season-switch episode-highlight guard (`aa67e069`, `preferredEpisodeNumberForSeason`) — `shared/.../details/SeriesSeasonSupport.kt` lacks the guard function, but no matching "preferred episode to highlight" concept was found anywhere in tvOS's Swift code by name, so it's unclear if tvOS has this bug or just lacks the feature — check `DetailView.swift`/`DetailViewModel.swift` before deciding to port. Also confirmed **not** needed: tvOS's `MPVPlayerView.swift` already defaults `controlsVisible = false` (upstream `582ae863`'s fix is already tvOS's behavior). Carried unstarted from 2026-08-25: the HIGH subtitle/player-engine batch and MEDIUM crash-cluster fixes — see that day's doc.

**beta.17: RELEASED 2026-09-02 (`tvos-v0.3.0-beta.17`, build 116, from `ff487392` on `tvos-shared-extraction`; both repos, marked Latest).** Carries Steven's beta.16-report batch (Waves 1–10, device-confirmed), upstream batch 7 (device-passed 09-02) and upstream batch 8. Highlights in `docs/comms-beta17-highlights.md`; the Reddit "Latest build" block (`docs/comms-reddit-beta17-changelog.md`) APPLIED and the beta.17 (+ beta.16 recap) announcement comment POSTED as `p7nbyk8` the same evening via the logged-in session's classic endpoints (no API creds in the shell; old.reddit has no edit control for this post) — see `docs/comms-reddit-beta17-comment.md`. No new README screenshot this cut (nothing in the batch is sim-capturable without touching the synced profile); README feature list updated. beta.16 announcement debt cleared by the recap in that comment.

**beta.15: RELEASED 2026-08-27 (`tvos-v0.3.0-beta.15`, build 113, from `c37d4784` on `tvos-shared-extraction`; both repos, marked Latest, thread block updated)** — device pass PASSED 08-27 (§J), BUG-76 + swatch-focus fixed and device-confirmed, three upstream issues filed (#1804-#1806). Originally: BUILT 2026-08-23 — plan + build log in `docs/beta15-implementation-plan-2026-08-23.md`. A1 addon-removal alert + A2 Simkl library cross-reference delta LANDED (`23079774`; the daily check should drop both from open items), subtitle sync offset in the player Subtitles tab for both engines with per-title persistence (`552900f1`), Settings revamped onto native `List`/`Toggle`/`Menu{Picker}` (`e2855c70`, `4a74ab9a`, `88582a6e`). Branch tip `c0632c41`, final Codex clean; device passes + release wave owed (punch list in the plan doc §H).

**Current open action items:**

- **[MERGED 2026-09-06 into `tvos-shared-extraction` `ebe14a0f` (Codex round 5 fixes on top); rc2 CUT build 118 `25e07e08` (DM SENT 09-06 ~01:45 ET); **rc3 CUT build 119 `9ddc4104` tag `tvos-v0.3.0-beta.18-rc3` at https://litter.catbox.moe/2cljii.ipa (72 h) = rc2 + BUG-96; follow-up DM SENT 09-06 ~16:00 ET (`docs/comms-dm-drafts-2026-09-06-rc3.md`); ACK DM for his rc2 feedback SENT ~16:55 ET (`docs/comms-dm-drafts-2026-09-06-ack.md`); rc4 (W4 reaches-first, build 120) IN PROGRESS in a separate session (task chip)** (`docs/comms-dm-drafts-2026-09-06.md`, sidebar early-stage ask included, synopsis paragraph omitted); device pass (Show Hero OFF) owed; BUG-96 (his ~23:45 edit: detail-page Saga row scrolled the header above off the top) FIXED 09-06 `072dfa63` (row rest anchor + top scrim + relayout re-anchor, 3 Codex rounds; trailer-crop row renumbered BUG-94→BUG-97 by the sweep), not in rc2, device pass owed] Steven beta.18 fix batch → separate clone `~/Claude/Projects/NuvioMobile-beta18`, submodule branch `claude/steven-beta18` off `1d4d8f9d`** (the main `NuvioMobile/` checkout is occupied by the parked-features session on `claude/steven-parked-feats`: FEAT-30 top bar, FEAT-31 Open Sans, FEAT-33 — that branch rebases after this batch merges). Steven's rc1 verdict (DM 09-05 5:48 PM ET, photos in `docs/research/steven-beta18-photos/`): five items fixed, six open. **Key finding: he runs Show Hero OFF** — both his photos have zero `publish`/`commit` lines, so the 09-04 plan's "Steven's configuration" (Nuvio hero ON, 2 hero sources) was wrong, the 09-05 device pass ran in the wrong mode, and the leftover double paint is the focus panel's resting title changing when the sync burst reorders rows after the immediate `heroOff` gate release (`HeroCommitGate.kt:201`). Waves: W1 hero-off ROWS gate (Kotlin `rowsReleased`, `rowsWait=` probe, `-debug.homeHeroOff`, test31 leg D) · W2 full-screen trailer never crops (BUG-94, decision 09-05) + first-poster bleed clip (BUG-92) · W3 folder backdrop crossfade collapse (BUG-95) · W4 `PinnedRowGeometry` makes the Large link frame fit the viewport (structural BUG-87 fix, allowed 09-05) · W5 belt first-reading grace + regime-scoped disarm reset (BUG-87/89) · W6 detail stutter glass flatten while scrolling (BUG-41). Then Codex → merge → device pass **with Show Hero OFF** → rc2 (build 118) → DM (SlopMonster) → beta.18 only after his hero-off photo passes. Plan + photo record: `docs/steven-beta18-batch-plan-2026-09-05.md`; approved plan file `~/.claude/plans/lets-make-a-detailed-cozy-comet.md`.
- **[MERGED 2026-09-05, device pass owed] FEAT-32 trailer bridge → submodule branch `claude/feat32-trailer-bridge` (4 commits, tip `1d4d8f9d`, off `146d2233`) fast-forwarded into `tvos-shared-extraction` and the outer pointer bumped (`c47dddf`).** Steven's 09-05 ask: the description page now enters the full-screen trailer the way official Nuvio does (0.6 s leave: chrome fade, title into a bottom-left caption with the Back hint, dim to black, cover cuts in from black; return: hard cut to the still landing at 1.06 and settling before the chrome fades in). `TrailerBridge.swift` (phase machine + pure choreography, 11 unit tests), `DetailView` presents the cover from a separate item set after the leave, `TrailerBridgeCoverObserver` (a child VC inside the cover reports dismissal START; SwiftUI reports a Back dismissal only at its END, and `onExitCommand` on cover content never fires on tvOS), the cover stays opaque and carries its own copy of the enlarged still composed with scrim + scroll dim (a clear presentation background let one Menu press dismiss the cover AND pop the page, 1 run in 3). Codex clean at round 4 (4 findings fixed); NuvioTVTests 110; Debug + Release; test51 (auto-play driven, `debug_bridge` trace oracle) passed on an earlier variant and SKIPS on the merged tip because YouTube answers every extraction client with `LOGIN_REQUIRED` from this Mac since ~18:20 ET. Owed: device pass (watch the return: one enlarged still, no black beat, no ghost caption, no doubled artwork) + one end-to-end test51 run once extraction recovers. Tracker FEAT-32; memory `trailer-bridge-feat32`.
- **[MERGED 2026-09-05, DEVICE PASS PASSED, rc1 SENT] Steven beta.17 batch → submodule branch `claude/steven-beta17` (15 commits, `cf2f674e..2a5874db`) fast-forwarded into `tvos-shared-extraction`, outer pointer bumped.** Waves H/G/F from his 09-04 list (hero commit gate, rows geometry, focus/trailer/detail), 11 Codex rounds + 1 internal review, every finding fixed; round 10's P2 (empty add-on list with a failed Cinemeta seed never opened the rows gate) fixed as `8619c690`, round 11 clean; the round-9 chip (`pullFromServer` set `initialized` without the `initializedState` mirror) fixed as `918cd2a1` via `AddonBootstrapState`. Christian's device pass in Steven's configuration PASSED on the Living Room ATV. Build number bumped to 117 (`146d2233`); beta.18-rc1 cut unsigned (tag `tvos-v0.3.0-beta.18-rc1`, IPA at https://litter.catbox.moe/mqr4in.ipa, litterbox 72 h ~09-08 17:00 ET; catbox proper stored 0-byte objects three times that day and dedups by hash, verify `content-length` after every upload). DM SENT to Steven 09-05 4:54 PM ET (`docs/comms-dm-drafts-2026-09-05.md`, SlopMonster 5/5): link, commit check `146d2233`, hero photo ask, per-item list, top bar (FEAT-30) told as IN PROGRESS at Christian's request, Open Sans on the list. His replies: bounce at Medium too; Medium-only "row above the last does not stay on screen" (BUG-89 note); two hero-diagnostics photos (images, unread); new asks FEAT-32 (built above) and FEAT-33 (60 fps collection animation; his reference video is itself 30 fps, hardware probe needed). **Next: his Hero Paint Diagnostics photo on build 117 decides beta.18** (`gate=released:all`, one `commit first=1`, zero `headChanged=1`/`hashChanged=1`/`same=1`). Plan + full record: `docs/steven-beta17-batch-plan-2026-09-04.md`.
- **[MERGED 2026-09-02, device pass owed (non-default scope only)] Upstream batch 8 → submodule branch `claude/upstream-batch8` (tip `3b140cc7`, off `a87ab03f`) fast-forwarded into `tvos-shared-extraction` (now `3b140cc7`, pushed) and the outer pointer bumped.** The 09-02 daily check's one item — `58864ec1` auto-play source loading scope (#1825) — PORTED into `shared/.../streams/StreamsRepository.kt` (applied verbatim; region was byte-identical to upstream's parent) + `StreamAutoPlayLoadingPolicy.kt` + its 4 tests. **Correction to the daily note:** that block is inert on tvOS (`manualSelection = true` always), so the real tvOS fix is in `NextEpisodeAutoPlay.swift`: the up-next engine now runs the full selection as soon as every IN-SCOPE source has finished (via the shared policy, made public, fed by new `StreamsUiState.installedAddonIds`), and the selector finally gets the true installed-addon set instead of every group. Codex settled r3 (2 fixes, r3 clean); gates jvm 597 / tvOS 617 / Debug+Release sim green. Device pass owed only with a non-default auto-play source synced from mobile (tvOS has no Settings row for it). See the OUTCOME ADDENDUM in `docs/upstream-port-plan-2026-09-02.md`.
- **[MERGED 2026-09-02, DEVICE PASS PASSED 2026-09-02] Upstream batch 7 → submodule branch `claude/upstream-batch7` (tip `a87ab03f`, off `3c39c677`) fast-forwarded into `tvos-shared-extraction` (now `a87ab03f`, pushed) and the outer pointer bumped.** Every still-unported upstream item as of the 09-01 check closed in one session — see the OUTCOME ADDENDUM in `docs/upstream-port-plan-2026-09-01.md`. (1) ARM→Simkl skip-intro ID resolution (`f212242a`+`6de79141`) PORTED — new `SimklIdResolver.kt`, ARM code gone, anime episode→TVDB S/E mapping for IntroDB; `getSkipIntervalsForContentId`'s Swift-facing signature deliberately unchanged (its anime branches have no IMDB id to hint with; adding params would rewrite the exported ObjC selector). (2) Addon season posters (specials-aware, `22096a1e` end-state) + certification fallback (`0504af98`) PORTED both halves — `MetaDetails.seasonPosters` + `EpisodesSection.swift` fallback (first Int-keyed Kotlin map consumed from Swift). (3) Multiline TTML regex (`50ef6a84`) PORTED as parity — **K/N probe NEGATIVE**: the old `setOf(RegexOption…)` form already passed on `tvosSimulatorArm64Test`, so the 08-31 "possible live subtitle bug" is retired; separate fork-own finding: no tvOS path reaches the shared TTML parser at all (`SubtitleVTT.swift` drops TTML tracks — follow-up). (4) Delayed addon manifest states (`085e8dc6`, #1819) — confirmed REAL on tvOS (Search "No results." / Discover "Install and enable an add-on…" during the manifest window, never self-correcting; Home stuck on "Setting up your catalogs…" with no retry on total manifest failure) and FIXED both layers: public predicates in `AddonModels.kt`, hand-ported `SearchRepository` (deliberate deviation: pending flag NOT added to the Discover reuse guard), `SearchViewModel` manifest-aware signature + re-search, Discover/Home error + focusable Retry. (5) Up-next dismissal (`4026ec92`, #858) — state half was already correct (`cancelled` sticky); gesture half BUILT: Menu dismisses the chip first on both engines (mpv `pressesBegan`, native `NativePlayerHostController.onMenuPress`), plus a "Dismiss" contextual action. (6) `c4934bce` pause-description CLOSED not applicable. Gates: jvmTest 593 / tvosSimulatorArm64Test 613 / composeApp iosSim 425 / Debug + Release sim builds green (Release caught a compile error Debug missed); Codex settled r5 (8 fixes r1–r4 incl. three latent-in-upstream skip bugs, 1 documented decline). Sim smoke: Home/Search/Discover/Detail normal on FA87; season selector via test33 PASSED (AX-tree assertion). **Device pass PASSED 2026-09-02 on the Living Room ATV (all four items: anime skip chip + IMDb IntroDB; addon season posters incl. specials + rating chip; Wi-Fi-off cold start → error + Retry on Home/Search/Discover, Retry recovers; Menu dismisses the up-next chip on both engines).** Batch complete end to end; ready for the next beta cut.
- **[MERGED 2026-08-28, device pass owed] Upstream 6-item batch → submodule branch `claude/upstream-batch6` (tip `437a331f`, PUSHED).** All 6 items closed in one session — visionos client swap (byte-checked), trailer dedup (only `distinctBy` was actually missing; the language work was already superseded by BUG-63/67), CJK name-fallback chain + new `TmdbMetadataServiceTest` (15 tests), `aggregate_credits`, Simkl JsonNull, and the scrobble guard — where the "tvOS call-site investigation" resolved as: both engines funnel through shared `isProgressComplete()` with `isEnded=false` hardcoded, so upstream's isEnded-only guard was inert on tvOS; the guard now also covers the fraction path + `shouldStoreProgress()` + both Swift Trakt drivers. Codex settled round 5 (4 fixes, 3 documented declines). Gates: jvm 529 / tvOS-native 541 / sim build green. 10 new upstream-report candidates logged. Full outcome + device-pass checklist in `docs/upstream-port-plan-2026-08-28.md` addendum.
- **[NEW 2026-08-26, updated 08-28] BUG-75 CLOSED both halves + housekeeping:** the reporter CONFIRMED the workaround fixed CW parity in-thread 08-27 (`p6ausa5`), AND the `claude/bug75-ports` code fix turned out to be **already released in beta.15 build 113** (`c77e9caa` verified an ancestor of `c37d4784` on 08-28 — the cloud sweeps' "built-but-unmerged, merge call owed" framing was stale; that action item is retired). Four upstream-report candidates still unfiled (Simkl precedence, FNV size-prefixing, CatalogRepository harder guard, TMDB `putIfAbsent`→`getOrPut` KMP-compat fix — see 2026-08-30 run above). Also 08-28: both stranded 08-27 sweeps absorbed into outer `main` (evening one hand-folded — 5th classifier-blocked `git merge`, first in an interactive session); GitHub issues #1 (BUG-69) + #2 (BUG-70) closed as completed 08-27 23:59Z → **0 open issues**; release notes de-jargoned (sanitizer in `release-beta.sh` `dc8281c2` + all 16 published releases backfilled on both repos). **The classifier-block class itself is now CLOSED, cloud-confirmed:** a checked-in `.claude/settings.json` (`b31b44d`) allowlists the sweep §6 git ops so cloud clones inherit deterministic permissions instead of hitting the nondeterministic auto-mode classifier, and the first post-fix scheduled sweep — the 08-28 morning quiet run (`a042416` @ 12:06Z) — **merged itself into outer `main` unaided** (`--no-ff` merge `3af2857` @ 12:07Z, authored and pushed by the cloud session; `origin/main` moved from a sweep for the first time). Zero new stranding; its designated branch `claude/kind-hypatia-3se944` was verified an ancestor of `main` and deleted, so `origin` again carries only `main`. If a sweep is *ever* refused again despite the allowlist, that is a new finding class — the environment's restriction would not be settings-driven; the hand-fold fallback stays documented in `docs/beta-feedback-sweep-instructions.md` §6.
- **[MERGED 2026-08-28, device pass owed] HIGH subtitle/player-engine batch → submodule branch `claude/subtitle-engine` (tip `716c18ea`, PUSHED).** The carried 08-25 8-commit batch, ported as end-states (33c6cdc2's JVM-only intermediate and 5a60a95d's parser/utils states skipped as superseded): StartupMode removed (legacy key kept in `syncKeys` so sync purges the orphan — no schema bump; ⚠️ the old `grep -rl "StartupMode"` check now false-positives on `legacyAddonSubtitleStartupModeKey` — use `grep -rl AddonSubtitleStartupMode shared/src` → empty), ASS/TTML parser restored, parallel per-addon subtitle fetch with incremental emission (`completedRequest` invariant preserved on every terminal path), forced-subtitle selection + `subtitleIsForced` persistence, compose runtime waves, tvOS Swift migration incl. the `subsFetchDone` partial-HLS-master fix. **Wave D (Android ExoPlayer sidecar) DEFERRED by product decision — zero tvOS value; future mobile-parity pass.** Also fixed pre-existing composeApp iOS compile break (`StreamsScreen` missing BUG-74's `IncompatibleContentId` branch). Codex settled round 4 (3 fixes incl. Castellano→es-419 misread and forced-track restore hijack; 6 documented declines, all unshipped-compose-runtime or deliberate-upstream). Gates: jvm 515 / tvOS-native 527 / composeApp iosSim 420 / sim build green. Details + device checklist in `docs/upstream-port-plan-2026-08-28.md` addendum.

**PORTED 2026-08-24 (same-day, sync-reliability batch — branch `claude/sync-reliability` off `claude/beta15` @ `207b1298`, tip `9221cc5d`, unpushed, merge decision owed):** the 2026-08-24 MEDIUM audit item done end-to-end — all 7 upstream sync fixes ported into `shared/` as logic ports (429/Retry-After `BackendRateLimit.kt` + SupabaseProvider wiring; terminal watch-progress write dedup; addon push debounce; `TvOsPluginRepository` 6h refresh throttle; pull-first conditional credential seeding; home-catalog push cache + legacy-read deletion, history-verified safe; SyncManager Activity/Full split with 2-min foreground gate, 15-min periodic, 10s full-pull suppression) plus the tvOS-native visibility half in `ContentView.swift`/`ProfilesViewModel.swift` — **tvOS never started periodic sync polling before this; it does now (profile entry + foreground, stopped on background/profile-picker exit)**. Codex 6 rounds → clean (5 real fixes incl. a P1 failed-scrobble-push/dedup interaction that upstream `d0c7bff7` still carries — upstream-report candidate — and clock-rollback guards; 2 documented parity tradeoffs). jvm + tvOS native tests green, sim build green, FA87 smoke passed. The search-tab spot-check also resolved same-day: tvOS `SearchViewModel` is `@StateObject` in a persistent `Tab` subtree — state survives switches, no port needed. Outcome details in `docs/upstream-port-plan-2026-08-24.md`.
- **[MEDIUM, PARKED by product decision 2026-08-20] Supporter perks v1** (upstream `bd88760e`, `38e6ea28`, `b80ee5ab`, `52e28562`, now also `fbb64124` cached-asset follow-up). Mobile monetization feature — membership tiers, theme accents, custom profile backgrounds, membership status card, "Support Nuvio" entry point. Entirely in `composeApp/`, nothing in `shared/` — not a mechanical port. Christian decided to park it as a backlog item: build nothing until it's re-raised; zero drift risk by waiting.
- **[LOW, DEFERRED by product decision 2026-08-20] Subtitle minimum font size** (upstream commit `d50f84fc`). Upstream lowers the iOS mobile subtitle-size floor from 12sp to 6sp. tvOS uses a different native subtitle renderer (`iosApp/NuvioTV/Screens/SubtitleVTT.swift`, not the KMP `PlayerEngine.ios.kt` this fix touches), and 10-foot UI arguably wants a *larger* minimum, not smaller. Don't port as-is — decide tvOS's own range next time player styling gets a pass.
- **[SHIPPED in beta.14 — build 111, 2026-08-22] Trailer Location (FEAT-25 round one)** — poster-vs-hero playback option; gated (Codex 5 rounds clean, sim green, device pass 3–8 passed) and fast-forwarded into NuvioMobile `claude/beta14` AND `tvos-shared-extraction`, both at `aa57a8d6`, then FEAT-26 (bigger season posters, `f539eafd`) → `65c486f7`, then the BUG-38 folder-page hero (`c747efd8`+`23304095`, settled by the reporter's Collections JSON) → `23304095`, released as `tvos-v0.3.0-beta.14` (build 111) from `2e732534` on 2026-08-22, then `tvos-v0.3.0-beta.14.5` (build 112) from `386eabfd` the same day with BUG-38 round three (Home hero follows focused collection folders; folder page logo-only). Record + three follow-ups (CW/Upcoming mute, latch-flip collapse animation, refused-slot retry) in `docs/trailer-playback-location-plan.md`; beta.14 entry in `docs/beta14-implementation-plan-2026-08-21.md`.

**Built 2026-08-20 (beta.13.5 batch, in progress):** **SDH subtitle stripping** (upstream `87585259`..`1cc1b768`, PR #1751) — ported end-to-end with full upstream parity: `SubtitleSdhFilter` into `shared/` commonMain (+ unit test), `stripSdh` through `SubtitleStyleState`/`PlayerSettingsStorage` (apple + android actuals, sync payload)/`PlayerSettingsRepository`, tvOS `MPVPlayerView.applySubtitleStyle()` `sub-filter-sdh(-harder)` properties, `PlaybackSettingsPane` toggle + xcstrings, and the composeApp mobile half (PlaybackSettingsPage toggle, strings.xml, `PlayerEngine.android.kt` cue filtering) applied from upstream. Also new (fork-own, GitHub issue #2): addon stream `proxyHeaders` threading into the tvOS players (upstream mobile had it via `MPVPlayerBridge` `http-header-fields`; tvOS dropped headers entirely).

**Resolved earlier (kept for the daily check's diffing):**
- **Addon delete-confirmation** and **Simkl anime-movie fix comparison** — resolved 2026-08-24 (verified by reading current file contents, not just commit messages). Addon-removal landed as `23079774` on `claude/beta15` (`AddonsView.swift` `addonPendingRemoval` + `.alert`). The Simkl comparison closed with no port needed: tvOS's `shared/` divergence already matches, and slightly improves on, upstream's final fix (`15df9555`). See `docs/upstream-port-plan-2026-08-24.md`.
- **Self-hosted server discovery** and **TMDB Discover exclusion filters UI half** — both shipped in beta.13 (see "Built 2026-08-19" below); the daily check should no longer list them as open.

**Built 2026-08-19 (beta.13 Waves 11/12; see `docs/beta13-release-plan.md`):** the other two decision items. **Self-hosted server discovery** (upstream `ddc28dc8`/`cc20e716`) — domain ported into `shared/` (ServerConfiguration/Discovery/Storage, cached SupabaseProvider + reset, AuthRepository prepareForServerSwitch/reinitialize, ServerConnectionController in shared `features/auth`), tvOS SwiftUI flow (ServerConnectionView, Welcome + Settings entry points, QR gating on `tv_login`, `<backend>/tv-login` redirect base like NuvioMedia/NuvioTV) and composeApp parity (upstream dialogs/AuthScreen/strings verbatim). **TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — mobile editor hunks applied verbatim (+ar/hu translations), plus a tvOS-native filter editor (shared `TmdbSourceFilterEditor` + `TmdbFilterEditorView` behind Edit Filters on collection folder grids) and `CollectionSyncService.startObserving()` on tvOS so edits push.

**Ported 2026-08-13:** addon cache-control / force-refresh (upstream `981b8bc7` → fork `a040293a`, incl. Android OkHttp cache + Compose call sites this fork's composeApp consumes, and tvOS Swift wiring) and TMDB exclusion-filter shared plumbing (upstream `0fc4616b` → fork `7dac9a67`). See the addendum in `docs/upstream-port-plan-2026-08-13.md` — including the empirical finding that tvOS's Ktor Darwin client never *serves* from NSURLCache (the header still matters for CDN/addon-server caches), and the three deliberate fork divergences kept in the cache-control port.

**Ported by 2026-08-19 (as part of the fork's own beta.13 work, outer commit `50af095`):** Simkl anime library filtering (upstream `96618a86` — `mediaCategory` field added to `LibraryModels.kt`/`SimklLibraryProjection.kt`/`LibraryDisplaySettings.kt`), PIN-verify-refreshes-profile-first (upstream `5327166f` — `ProfileRepository.kt` `verifyPin()` now calls `pullProfiles()` before `rememberVerifiedPin()`), and `shared/AppLanguage.kt` `ARABIC` entry (upstream `3c0ab547`). All three verified landed by reading current file contents, not just trusting commit messages — see `docs/upstream-port-plan-2026-08-19.md`.

As of 2026-08-19, `upstream/cmp-rewrite` has **not moved** since 2026-08-17 — still pinned at `bbac53b2`, zero new commits two checks in a row. `upstream/copilot/refactor-project-structure` remains stale/abandoned (the fork's own `origin/copilot/refactor-project-structure` mirror was deleted 2026-08-21 in a branch cleanup — only the upstream ref exists now), and the `upstream/simkl` ref has been deleted from the remote (it was already fully merged into `cmp-rewrite` with zero unique commits, so this is harmless). Submodule pointer drift from prior runs remains resolved — pinned pointer matches actual HEAD (`5cb2f8b9`).

For full day-by-day history of what's been checked and what landed, see `docs/upstream-port-plan-*.md` (2026-07-25 onward) — each file's "Verification" section confirms whether prior items landed.

## Repo layout

- Outer repo (this folder) = fork wrapper: docs, scaffolding, design assets, the `NuvioMobile` submodule.
- `NuvioMobile/` = the actual KMP + native tvOS app (submodule, tracks `tvos-shared-extraction` branch on `origin` = `youngchris29-art/NuvioMobile`).
- `NuvioMobile/shared/` = Compose-free Kotlin business logic shared across platforms — this is what upstream ports land in.
- `NuvioMobile/iosApp/NuvioTV/` = native SwiftUI tvOS frontend.

## Copy rule (standing, set 2026-09-03)

Every draft of user-facing prose presented to Christian for posting or publishing (Reddit comments and thread blocks, DMs to testers, README/INSTALL edits, release-notes highlights, GitHub issue/PR text, upstream reports, any `docs/comms-*.md`) goes through the SlopMonster loop before it is shown to him:

1. **Lint**: `python3 scripts/deslop/deslop.py <file>` (or `--text "…"`). The vendored copy in `scripts/deslop/` is the same scorer as the `slopmonster` skill, which is checked in at `.claude/skills/slopmonster/` so cloud sessions can invoke `/slopmonster` too; either works.
2. **Rewrite** until it scores 5/5: kill the flagged vocabulary and shapes, keep every fact, keep the register.
3. **Cleanse** with the rival model (`~/.claude/skills/slopmonster/tools/cleanse.sh`, needs the `codex` CLI) when it is available. Treat its output as a rewrite, not an edit: diff every hunk and restore any dropped fact, changed claim, invented opinion or edited boilerplate. Skip the cleanse where `codex` is absent (cloud sessions) or the piece is short with a single hit, and say so.
4. **Re-lint**. Present the draft only at 5/5, and state the score with the draft.

Don't game the scorer: no comment or period tricks to split windows. Real lists of three that trip the rule are reshaped, not padded with fake items. Legal & DMCA text and quoted third-party wording are never rewritten.
