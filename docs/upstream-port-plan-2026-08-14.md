# Upstream port check — 2026-08-14

## Summary

**No new commits.** `upstream/cmp-rewrite` (`NuvioMedia/NuvioMobile`) is still pinned at `0d6e30e4` ("bump version", 2026-08-13) — same HEAD as the 2026-08-13 check. Re-fetched today and confirmed via `git log --oneline 0d6e30e4..upstream/cmp-rewrite` → empty.

`upstream/copilot/refactor-project-structure` also unchanged (still at `cbc9fc4f`, 2026-03-24) — confirmed stale/abandoned, no action.

Fork state at check time: outer `main` HEAD `726c4c7a` ("docs: 08-13 ports landed — cache-control fix (a040293a) + TMDB filter plumbing (7dac9a67)"), submodule `NuvioMobile` HEAD `e0db05e0` on `tvos-shared-extraction` ("feat(diagnostics): add bare vidhub:// variant to the VidHub bisect knob" — fork-native diagnostics work, not an upstream port; the outer repo's pinned submodule pointer (`7dac9a67`) is one fork-native commit behind this — cosmetic drift, not upstream-check-related).

## Verification: did the 2026-08-13 open items land / change?

Both actionable items from the 2026-08-13 check **landed the same day** (see that doc's addendum) and remain landed:

- **Addon cache-control / force-refresh** (upstream `981b8bc7`) — ported as `a040293a`. Confirmed present in `git log` on `tvos-shared-extraction`.
- **TMDB Discover exclusion filters — shared plumbing** (upstream `0fc4616b`) — ported as `7dac9a67`. Confirmed present.

Two items remain open, both unchanged since 2026-08-13:

1. **Subtitle minimum font size** (upstream `d50f84fc`) — deliberate no-port, decision needed. Re-checked `shared/src/commonMain/kotlin/com/nuvio/app/features/player/SubtitleAudioModels.kt`: `fontSizeSp` is still an unbounded `Int` (currently defaults to 18, no `subtitleFontSizeRangeSp`-style clamp). Still needs a tvOS-specific decision (likely a *larger* floor than mobile's, not smaller) next time player styling gets a pass — not a mechanical port since tvOS renders subtitles via `iosApp/NuvioTV/Screens/SubtitleVTT.swift`, not the KMP `PlayerEngine.ios.kt` upstream's fix touches.

2. **TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — still not ported. Re-checked: `CollectionEditorScreen.kt` exists only under `composeApp/` (mobile UI) and a stale worktree (`.claude/worktrees/trusting-aryabhata-0539ea/`, appears to be leftover from an earlier Claude Code session — not part of the tracked tree). No `CollectionEditorScreen` or equivalent SwiftUI exclusion controls exist under `iosApp/NuvioTV/`. Fields remain reachable only via imported JSON (additive/default-null, no risk to existing collections). tvOS has no dedicated collection-editor screen yet, so this needs new UI design, not a mechanical port.

## Action items for Claude Code

No new work generated by this check. Carried-forward backlog, unchanged:

1. **[LOW / decision needed] Subtitle minimum font size** (upstream `d50f84fc`) — decide tvOS's own `subtitleFontSizeRangeSp`-equivalent (likely larger floor, not smaller) next time player styling gets attention. Touch `iosApp/NuvioTV/Screens/SubtitleVTT.swift`.
2. **[LOW] TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — design new tvOS SwiftUI exclusion controls (no existing collection-editor screen to extend) for `withoutGenres`/`withoutKeywords`/`withoutCompanies`/`withoutWatchProviders`, which are already wired into the shared query builder as of `7dac9a67`. Mobile-side reference: upstream's `CollectionEditorScreen.kt` + strings hunks (not directly portable — SwiftUI needs its own layout).

## Minor housekeeping note (not upstream-related)

Outer repo's pinned `NuvioMobile` submodule pointer (`7dac9a67`) is one commit behind the submodule's actual checked-out HEAD (`e0db05e0`, the VidHub bisect-knob diagnostics commit). Not an upstream-port issue — just an outer-repo commit that hasn't been made yet to bump the pointer. Flagging so it doesn't get confused with upstream drift in a future check.

## Next scheduled check

Re-fetch `upstream/cmp-rewrite`, diff past `0d6e30e4`. Backlog unchanged: subtitle-floor decision (item 1) and TMDB exclusion-filter UI half (item 2). No urgency on either — both are LOW priority with no user-facing regression.
