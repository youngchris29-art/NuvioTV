# DM draft: u/mrStevenx3, reply to his 2026-09-07 photos + answers

**Status: SENT 2026-09-07 9:59 PM ET (Reddit chat), on Christian's explicit go. Verified from a fresh ego process: one new message from u/youngchris2989 at 9:59 PM, composer empty after send, no duplicate. `fillInput` on the textbox ref + page-side `btn.click()` on `button[aria-label="Send message"]` through the shadow DOM; no wedge this time.** Replies to his 9:19 PM ET message (build 9b02bb16 confirmed, Saga photo, two Row Settle Diagnostics photos hero ON/OFF, two-line synopsis with the hero off, "text too large", trailer bridge now "only a fade"). Record and decoded diagnostics in `docs/steven-beta18-batch-plan-2026-09-05.md` (09-07 entry); photos in `docs/research/steven-beta18-photos/2026-09-07-*.jpg`.

What it commits to: measuring the hardware rest offset the `UNEXPECTED-WITH-FIT` lines expose and aiming the rest there instead of correcting; checking why the hero-off panel gives 2 synopsis lines at his shape (plan said 3); explaining (not promising to restore) that the caption scale-in removed in rc5 W2 is the motion he read as the trailer "zoom". What it asks for: the Row Settle pane scrolled to its tail after the Large walk; whether the two-step Down happens on every detail row; whether the double hero still shows with the hero off. Text size: acknowledged only, no promise.

SlopMonster loop: lint 5/5 on the first draft (236 words). Codex cleanse via `cleanse.sh`, diffed hunk by hunk: nine wording tightenings, no fact lost, no claim changed, nothing invented. Christian then cut the simulator sentence and the "I'll bring it back" promise. Final lint 5/5.

---

Hey! Thanks, that's exactly what I needed.

The Saga photo settles it: the names are there. Good.

The two row photos are the useful ones. They show that the frame now fits your Apple TV screen with the hero on or off. But the TV still puts each row 12 to 44 points lower than the app expects, so the app pulls it up. That's the bounce. The next fix is to measure where your TV actually puts the row and aim there, instead of correcting afterwards. One more photo would help: the same panel, scrolled to the end of the list. The bottom lines record the walk back up and the last row.

With the hero off, you get two lines. I expected three at your poster size. I'll check why. I've noted the text size too.

The trailer: the zoom you liked came from the caption growing into place as the page went dark. I took that motion out when I switched the caption to the logo.

I still need answers to two questions. On the description page, does one Down press jump two steps on every row, or only some? And with the hero off, do you still ever see the artwork paint twice at the top?

Merci!
