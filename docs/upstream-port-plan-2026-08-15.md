# Upstream port check — 2026-08-15

## Summary

**No new commits.** `upstream/cmp-rewrite` (`NuvioMedia/NuvioMobile`) is still pinned at `0d6e30e4` ("bump version", 2026-08-13) — unchanged since the 2026-08-13 and 2026-08-14 checks. `git log --oneline 0d6e30e4..upstream/cmp-rewrite` → empty.

`upstream/copilot/refactor-project-structure` unchanged (`cbc9fc4f`, 2026-03-24) — still stale/abandoned.

New this check: also diffed `upstream/simkl` (`e4911b77`, 2026-07-30) against `cmp-rewrite` — it's fully merged in (merge-base == simkl HEAD), zero unique commits. No action.

Fork state at check time: outer `main` HEAD `726c4c7a` (unchanged since 08-13), submodule `NuvioMobile` HEAD `e0db05e0` on `tvos-shared-extraction` ("feat(diagnostics): add bare vidhub:// variant to the VidHub bisect knob" — fork-native, not an upstream port). Outer repo's pinned submodule pointer (`7dac9a67`) is still one fork-native commit behind actual submodule HEAD — same cosmetic drift noted 08-14, still not fixed.

## Verification: did the 2026-08-14 open items land / change?

No — both remain open and unchanged:

1. **Subtitle minimum font size** (upstream `d50f84fc`) — re-checked `shared/src/commonMain/kotlin/com/nuvio/app/features/player/SubtitleAudioModels.kt`: `fontSizeSp: Int = 18` still unbounded, no `subtitleFontSizeRangeSp`-style clamp. Still a deliberate no-port pending a tvOS-specific decision.
2. **TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — re-checked: `CollectionEditorScreen.kt` still exists only under `composeApp/` (mobile) and the stale worktree `.claude/worktrees/trusting-aryabhata-0539ea/`. No SwiftUI equivalent under `iosApp/NuvioTV/`. Shared query-builder fields (`withoutGenres`/`withoutKeywords`/`withoutCompanies`/`withoutWatchProviders`) remain wired but unreachable from tvOS UI.

## Action items for Claude Code

No new work generated by this check. Carried-forward backlog, unchanged since 2026-08-13:

1. **[LOW / decision needed] Subtitle minimum font size** (upstream `d50f84fc`) — decide tvOS's own `subtitleFontSizeRangeSp`-equivalent (likely a larger floor than mobile's 6sp, not smaller) next time player styling gets attention. Touch `iosApp/NuvioTV/Screens/SubtitleVTT.swift`.
2. **[LOW] TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — design new tvOS SwiftUI exclusion controls (no existing collection-editor screen to extend) for `withoutGenres`/`withoutKeywords`/`withoutCompanies`/`withoutWatchProviders`, already wired into the shared query builder (`7dac9a67`). Mobile reference: upstream's `CollectionEditorScreen.kt` (not directly portable — SwiftUI needs its own layout).

## Minor housekeeping note (not upstream-related)

Outer repo's pinned `NuvioMobile` submodule pointer (`7dac9a67`) is still one commit behind the submodule's checked-out HEAD (`e0db05e0`). Cosmetic only — bump the pointer whenever convenient, no upstream-port implications.

## Next scheduled check

Re-fetch `upstream/cmp-rewrite`, diff past `0d6e30e4`. Backlog unchanged: subtitle-floor decision (item 1) and TMDB exclusion-filter UI half (item 2). Both LOW priority, no user-facing regression, no urgency.
