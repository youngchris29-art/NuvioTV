# Nuvio tvOS — Claude Code project notes

## Upstream sync status (auto-checked daily against NuvioMedia/NuvioMobile)

A scheduled task diffs this fork's `NuvioMobile` submodule (`upstream` remote = `github.com/NuvioMedia/NuvioMobile`, branch `cmp-rewrite`) against upstream daily and logs actionable ports to `docs/upstream-port-plan-YYYY-MM-DD.md`. Latest run: **2026-08-28**, see `docs/upstream-port-plan-2026-08-28.md`. Upstream has **not moved** since 2026-08-27 — still pinned at `1b84ee47`, zero new commits. This run re-verified (by reading current `shared/` file contents, not trusting prior notes) that all 6 items from the 2026-08-27 batch are **still unported**, and that the carried 2026-08-25 HIGH subtitle/player-engine batch (`StartupMode` still present in `shared/`) is also still unstarted. The A2 crash-cluster fixes (reported ported 2026-08-26) were spot-checked and confirmed still present, no regression. Same 6-item batch as 08-27: (1) **[HIGH, possible live break]** `InAppYouTubeExtractor` preferred client swap `android_vr`→`visionos` (upstream `c72a3825`) — YouTube may have blocked/deprecated `android_vr`, meaning in-app trailer playback could be degraded right now; bundled with `isDefaultAudioTrack` dub-track skip logic; (2) **[MEDIUM]** trailer dedup + language-preference tiebreaker (`HeroTrailerSelector` `distinctBy`, `MetaTrailer.iso6391`, TMDB trailer-list sort) — same upstream commit as #1, port together; (3) **[MEDIUM]** TMDB CJK/Romaji cast-crew name fallback, 3-commit chain (`c66e80e6`→`e6314ba2`→`754605cb`) — person bio+name, filmography, network-browse pages, port as one batch; (4) **[MEDIUM]** `tv/{id}/aggregate_credits` instead of `tv/{id}/credits` for full-series cast/crew (`0aac4dbe`); (5) **[LOW]** Simkl `stringValue()` JsonNull guard (`e39084d6`), mechanical; (6) **[MEDIUM]** scrobble guard for short placeholder/error-clip durations (`31f5d0e6`) — shared `isProgressComplete()` half is a clean port and `WatchProgressRules.kt` picks it up for free, but tvOS's native player completion call site (near `MPVPlayerView.swift`) needs its own guard — upstream's fix lives in Compose-only `MainAppContent.kt` with no tvOS equivalent, so this needs investigation, not just a mechanical port. All confirmed unported by reading current `shared/` file contents on `claude/beta15`, not commit messages. Full detail, batching suggestion, and verification checklist in `docs/upstream-port-plan-2026-08-27.md`.

Prior run: **2026-08-26**, see `docs/upstream-port-plan-2026-08-26.md`. Upstream moved `f62de092` → `582ae863` (16 new commits + 2 merges). Same "path-only looks like `composeApp/`-only, but `shared/` is an extraction of it" pattern — three commits have confirmed-unported `shared/` targets. **ALL THREE RESOLVED SAME-DAY (2026-08-26 evening session, submodule branch `claude/bug75-ports` off `claude/beta15`):** (1) Simkl list-mutation anime-type PORTED (response-first repair precedence — deliberate divergence from upstream `5003d298`, matches the fork's own history path; upstream-report candidate); (2) Discover catalog persistence PORTED (storage + registry + both profile-switch fan-outs wired); (3) season-guard CLOSED NOT APPLICABLE (tvOS has no preferred-episode concept — `EpisodesSection.swift` verified). The same session ported the carried 08-25 MEDIUM crash-cluster four (atomicfu map incl. a fork-only 13th site; FNV signature HARDENED with size-prefixed collections — upstream-report candidate; trySend; regex-escape) and `MetaVideo.rating`; `96fb98c4` SKIPPED as already-superseded by the fork's harder guard. AND it fixed **BUG-75** (the afternoon sweep's Reddit CW/Simkl report, renumbered from "BUG-72" after colliding with the local 08-23 DM-batch IDs): cross-platform tracking-source sync (`tracking_source_shared` namespace + ordered sync step, dead outbox removed), shared provider-aware CW row builder for Home + Top Shelf (limit-300 parity), coordinator armed at launch + wipe cascade. Gates: 505 jvm + 516 tvOS-native tests green, sim build green, clean cold launch on FA87; Codex loop **CLEAN at 13 rounds** (10 on 08-26, +internal 8-angle review during the quota gap, rounds 11–13 on 08-27 — failed-pull restore, preferences mutation lock, paired edit-record reads); interactive sim smoke moved to the owed device-pass punch list. Outer `main` UNFROZEN (three sweep branches merged, 26f20d8 → pushed). Original findings for the record: **New this run, three items:** (1) **[MEDIUM]** Simkl list-mutation anime-type fix (`5003d298`) — `SimklResolvedListMutation` needs `resolvedMediaType`/`animeType` fields (the history/scrobble path already has this from an earlier port; only the list-add/move path is missing it), confirmed via direct read of `shared/.../simkl/SimklMutationReceipt.kt`; (2) **[MEDIUM]** Discover catalog-selection persistence (`ab57cf1b`) — `resolveDiscoverCatalog()` + `DiscoverSelectionStorage` expect/actual don't exist in `shared/.../search/SearchRepository.kt`, confirmed via grep, so tvOS's Discover tab forgets the last-picked catalog on every cold launch; also needs a `ProfilesViewModel.swift` wiring check for `SearchRepository.reset()` on profile switch; (3) **[LOW, needs investigation first]** season-switch episode-highlight guard (`aa67e069`, `preferredEpisodeNumberForSeason`) — `shared/.../details/SeriesSeasonSupport.kt` lacks the guard function, but no matching "preferred episode to highlight" concept was found anywhere in tvOS's Swift code by name, so it's unclear if tvOS has this bug or just lacks the feature — check `DetailView.swift`/`DetailViewModel.swift` before deciding to port. Also confirmed **not** needed: tvOS's `MPVPlayerView.swift` already defaults `controlsVisible = false` (upstream `582ae863`'s fix is already tvOS's behavior). Carried unstarted from 2026-08-25: the HIGH subtitle/player-engine batch and MEDIUM crash-cluster fixes — see that day's doc.

**beta.15: RELEASED 2026-08-27 (`tvos-v0.3.0-beta.15`, build 113, from `c37d4784` on `tvos-shared-extraction`; both repos, marked Latest, thread block updated)** — device pass PASSED 08-27 (§J), BUG-76 + swatch-focus fixed and device-confirmed, three upstream issues filed (#1804-#1806). Originally: BUILT 2026-08-23 — plan + build log in `docs/beta15-implementation-plan-2026-08-23.md`. A1 addon-removal alert + A2 Simkl library cross-reference delta LANDED (`23079774`; the daily check should drop both from open items), subtitle sync offset in the player Subtitles tab for both engines with per-title persistence (`552900f1`), Settings revamped onto native `List`/`Toggle`/`Menu{Picker}` (`e2855c70`, `4a74ab9a`, `88582a6e`). Branch tip `c0632c41`, final Codex clean; device passes + release wave owed (punch list in the plan doc §H).

**Current open action items:**

- **[MERGED 2026-08-28, device pass owed] Upstream 6-item batch → submodule branch `claude/upstream-batch6` (tip `437a331f`, PUSHED).** All 6 items closed in one session — visionos client swap (byte-checked), trailer dedup (only `distinctBy` was actually missing; the language work was already superseded by BUG-63/67), CJK name-fallback chain + new `TmdbMetadataServiceTest` (15 tests), `aggregate_credits`, Simkl JsonNull, and the scrobble guard — where the "tvOS call-site investigation" resolved as: both engines funnel through shared `isProgressComplete()` with `isEnded=false` hardcoded, so upstream's isEnded-only guard was inert on tvOS; the guard now also covers the fraction path + `shouldStoreProgress()` + both Swift Trakt drivers. Codex settled round 5 (4 fixes, 3 documented declines). Gates: jvm 529 / tvOS-native 541 / sim build green. 10 new upstream-report candidates logged. Full outcome + device-pass checklist in `docs/upstream-port-plan-2026-08-28.md` addendum.
- **[NEW 2026-08-26, updated 08-28] BUG-75 CLOSED both halves + housekeeping:** the reporter CONFIRMED the workaround fixed CW parity in-thread 08-27 (`p6ausa5`), AND the `claude/bug75-ports` code fix turned out to be **already released in beta.15 build 113** (`c77e9caa` verified an ancestor of `c37d4784` on 08-28 — the cloud sweeps' "built-but-unmerged, merge call owed" framing was stale; that action item is retired). Three upstream-report candidates still unfiled (Simkl precedence, FNV size-prefixing, CatalogRepository harder guard). Also 08-28: both stranded 08-27 sweeps absorbed into outer `main` (evening one hand-folded — 5th classifier-blocked `git merge`, first in an interactive session); GitHub issues #1 (BUG-69) + #2 (BUG-70) closed as completed 08-27 23:59Z → **0 open issues**; release notes de-jargoned (sanitizer in `release-beta.sh` `dc8281c2` + all 16 published releases backfilled on both repos). **The classifier-block class itself is now CLOSED, cloud-confirmed:** a checked-in `.claude/settings.json` (`b31b44d`) allowlists the sweep §6 git ops so cloud clones inherit deterministic permissions instead of hitting the nondeterministic auto-mode classifier, and the first post-fix scheduled sweep — the 08-28 morning quiet run (`a042416` @ 12:06Z) — **merged itself into outer `main` unaided** (`--no-ff` merge `3af2857` @ 12:07Z, authored and pushed by the cloud session; `origin/main` moved from a sweep for the first time). Zero new stranding; its designated branch `claude/kind-hypatia-3se944` was verified an ancestor of `main` and deleted, so `origin` again carries only `main`. If a sweep is *ever* refused again despite the allowlist, that is a new finding class — the environment's restriction would not be settings-driven; the hand-fold fallback stays documented in `docs/beta-feedback-sweep-instructions.md` §6.
- **[MERGED 2026-08-28, device pass owed] HIGH subtitle/player-engine batch → submodule branch `claude/subtitle-engine` (tip `716c18ea`, PUSHED).** The carried 08-25 8-commit batch, ported as end-states (33c6cdc2's JVM-only intermediate and 5a60a95d's parser/utils states skipped as superseded): StartupMode removed (legacy key kept in `syncKeys` so sync purges the orphan — no schema bump; ⚠️ the old `grep -rl "StartupMode"` check now false-positives on `legacyAddonSubtitleStartupModeKey` — use `grep -rl AddonSubtitleStartupMode shared/src` → empty), ASS/TTML parser restored, parallel per-addon subtitle fetch with incremental emission (`completedRequest` invariant preserved on every terminal path), forced-subtitle selection + `subtitleIsForced` persistence, compose runtime waves, tvOS Swift migration incl. the `subsFetchDone` partial-HLS-master fix. **Wave D (Android ExoPlayer sidecar) DEFERRED by product decision — zero tvOS value; future mobile-parity pass.** Also fixed pre-existing composeApp iOS compile break (`StreamsScreen` missing BUG-74's `IncompatibleContentId` branch). Codex settled round 4 (3 fixes incl. Castellano→es-419 misread and forced-track restore hijack; 6 documented declines, all unshipped-compose-runtime or deliberate-upstream). Gates: jvm 515 / tvOS-native 527 / composeApp iosSim 420 / sim build green. Details + device checklist in `docs/upstream-port-plan-2026-08-28.md` addendum.
- **[LOW priority, spot-check only, carried 2026-08-23] Player pause-description staleness.** Upstream `c4934bce` fixed the Compose player showing a stale pause/episode-description after switching episodes. tvOS uses a native player (`MPVPlayerView`), not the Compose files upstream touched, so this isn't a direct port — just something to verify next time the tvOS player/pause-overlay UI gets touched.

**PORTED 2026-08-24 (same-day, sync-reliability batch — branch `claude/sync-reliability` off `claude/beta15` @ `207b1298`, tip `9221cc5d`, unpushed, merge decision owed):** the 2026-08-24 MEDIUM audit item done end-to-end — all 7 upstream sync fixes ported into `shared/` as logic ports (429/Retry-After `BackendRateLimit.kt` + SupabaseProvider wiring; terminal watch-progress write dedup; addon push debounce; `TvOsPluginRepository` 6h refresh throttle; pull-first conditional credential seeding; home-catalog push cache + legacy-read deletion, history-verified safe; SyncManager Activity/Full split with 2-min foreground gate, 15-min periodic, 10s full-pull suppression) plus the tvOS-native visibility half in `ContentView.swift`/`ProfilesViewModel.swift` — **tvOS never started periodic sync polling before this; it does now (profile entry + foreground, stopped on background/profile-picker exit)**. Codex 6 rounds → clean (5 real fixes incl. a P1 failed-scrobble-push/dedup interaction that upstream `d0c7bff7` still carries — upstream-report candidate — and clock-rollback guards; 2 documented parity tradeoffs). jvm + tvOS native tests green, sim build green, FA87 smoke passed. The search-tab spot-check also resolved same-day: tvOS `SearchViewModel` is `@StateObject` in a persistent `Tab` subtree — state survives switches, no port needed. Outcome details in `docs/upstream-port-plan-2026-08-24.md`.
- **[MEDIUM, PARKED by product decision 2026-08-20] Supporter perks v1** (upstream `bd88760e`, `38e6ea28`, `b80ee5ab`, `52e28562`, now also `fbb64124` cached-asset follow-up). Mobile monetization feature — membership tiers, theme accents, custom profile backgrounds, membership status card, "Support Nuvio" entry point. Entirely in `composeApp/`, nothing in `shared/` — not a mechanical port. Christian decided to park it as a backlog item: build nothing until it's re-raised; zero drift risk by waiting.
- **[LOW, DEFERRED by product decision 2026-08-20] Subtitle minimum font size** (upstream commit `d50f84fc`). Upstream lowers the iOS mobile subtitle-size floor from 12sp to 6sp. tvOS uses a different native subtitle renderer (`iosApp/NuvioTV/Screens/SubtitleVTT.swift`, not the KMP `PlayerEngine.ios.kt` this fix touches), and 10-foot UI arguably wants a *larger* minimum, not smaller. Don't port as-is — decide tvOS's own range next time player styling gets a pass.
- **[SHIPPED in beta.14 — build 111, 2026-08-22] Trailer Location (FEAT-25 round one)** — poster-vs-hero playback option; gated (Codex 5 rounds clean, sim green, device pass 3–8 passed) and fast-forwarded into NuvioMobile `claude/beta14` AND `tvos-shared-extraction`, both at `aa57a8d6`, then FEAT-26 (bigger season posters, `f539eafd`) → `65c486f7`, then the BUG-38 folder-page hero (`c747efd8`+`23304095`, settled by the reporter's Collections JSON) → `23304095`, released as `tvos-v0.3.0-beta.14` (build 111) from `2e732534` on 2026-08-22, then `tvos-v0.3.0-beta.14.5` (build 112) from `386eabfd` the same day with BUG-38 round three (Home hero follows focused collection folders; folder page logo-only). Record + three follow-ups (CW/Upcoming mute, latch-flip collapse animation, refused-slot retry) in `docs/trailer-playback-location-plan.md`; beta.14 entry in `docs/beta14-implementation-plan-2026-08-21.md`.

**Built 2026-08-20 (beta.13.5 batch, in progress):** **SDH subtitle stripping** (upstream `87585259`..`1cc1b768`, PR #1751) — ported end-to-end with full upstream parity: `SubtitleSdhFilter` into `shared/` commonMain (+ unit test), `stripSdh` through `SubtitleStyleState`/`PlayerSettingsStorage` (apple + android actuals, sync payload)/`PlayerSettingsRepository`, tvOS `MPVPlayerView.applySubtitleStyle()` `sub-filter-sdh(-harder)` properties, `PlaybackSettingsPane` toggle + xcstrings, and the composeApp mobile half (PlaybackSettingsPage toggle, strings.xml, `PlayerEngine.android.kt` cue filtering) applied from upstream. Also new (fork-own, GitHub issue #2): addon stream `proxyHeaders` threading into the tvOS players (upstream mobile had it via `MPVPlayerBridge` `http-header-fields`; tvOS dropped headers entirely).

**Resolved earlier (kept for the daily check's diffing):**
- **Addon delete-confirmation** and **Simkl anime-movie fix comparison** — resolved 2026-08-24 (verified by reading current file contents, not just commit messages). Addon-removal landed as `23079774` on `claude/beta15` (`AddonsView.swift` `addonPendingRemoval` + `.alert`). The Simkl comparison closed with no port needed: tvOS's `shared/` divergence already matches, and slightly improves on, upstream's final fix (`15df9555`). See `docs/upstream-port-plan-2026-08-24.md`.
- **Self-hosted server discovery** and **TMDB Discover exclusion filters UI half** — both shipped in beta.13 (see "Built 2026-08-19" below); the daily check should no longer list them as open.

**Built 2026-08-19 (beta.13 Waves 11/12; see `docs/beta13-release-plan.md`):** the other two decision items. **Self-hosted server discovery** (upstream `ddc28dc8`/`cc20e716`) — domain ported into `shared/` (ServerConfiguration/Discovery/Storage, cached SupabaseProvider + reset, AuthRepository prepareForServerSwitch/reinitialize, ServerConnectionController in shared `features/auth`), tvOS SwiftUI flow (ServerConnectionView, Welcome + Settings entry points, QR gating on `tv_login`, `<backend>/tv-login` redirect base like NuvioMedia/NuvioTV) and composeApp parity (upstream dialogs/AuthScreen/strings verbatim). **TMDB Discover exclusion filters — UI half** (upstream `0fc4616b`) — mobile editor hunks applied verbatim (+ar/hu translations), plus a tvOS-native filter editor (shared `TmdbSourceFilterEditor` + `TmdbFilterEditorView` behind Edit Filters on collection folder grids) and `CollectionSyncService.startObserving()` on tvOS so edits push.

**Ported 2026-08-13:** addon cache-control / force-refresh (upstream `981b8bc7` → fork `a040293a`, incl. Android OkHttp cache + Compose call sites this fork's composeApp consumes, and tvOS Swift wiring) and TMDB exclusion-filter shared plumbing (upstream `0fc4616b` → fork `7dac9a67`). See the addendum in `docs/upstream-port-plan-2026-08-13.md` — including the empirical finding that tvOS's Ktor Darwin client never *serves* from NSURLCache (the header still matters for CDN/addon-server caches), and the three deliberate fork divergences kept in the cache-control port.

**Ported by 2026-08-19 (as part of the fork's own beta.13 work, outer commit `50af095`):** Simkl anime library filtering (upstream `96618a86` — `mediaCategory` field added to `LibraryModels.kt`/`SimklLibraryProjection.kt`/`LibraryDisplaySettings.kt`), PIN-verify-refreshes-profile-first (upstream `5327166f` — `ProfileRepository.kt` `verifyPin()` now calls `pullProfiles()` before `rememberVerifiedPin()`), and `shared/AppLanguage.kt` `ARABIC` entry (upstream `3c0ab547`). All three verified landed by reading current file contents, not just trusting commit messages — see `docs/upstream-port-plan-2026-08-19.md`.

As of 2026-08-19, `upstream/cmp-rewrite` has **not moved** since 2026-08-17 — still pinned at `bbac53b2`, zero new commits two checks in a row. `upstream/copilot/refactor-project-structure` remains stale/abandoned (the fork's own `origin/copilot/refactor-project-structure` mirror was deleted 2026-08-21 in a branch cleanup — only the upstream ref exists now), and the `upstream/simkl` ref has been deleted from the remote (it was already fully merged into `cmp-rewrite` with zero unique commits, so this is harmless). Submodule pointer drift from prior runs remains resolved — pinned pointer matches actual HEAD (`5cb2f8b9`).

For full day-by-day history of what's been checked and what landed, see `docs/upstream-port-plan-*.md` (2026-07-25 onward) — each file's "Verification" section confirms whether prior items landed.

## Repo layout

- Outer repo (this folder) = fork wrapper: docs, scaffolding, design assets, the `NuvioMobile` submodule.
- `NuvioMobile/` = the actual KMP + native tvOS app (submodule, tracks `tvos-shared-extraction` branch on `origin` = `youngchris29-art/NuvioMobile`).
- `NuvioMobile/shared/` = Compose-free Kotlin business logic shared across platforms — this is what upstream ports land in.
- `NuvioMobile/iosApp/NuvioTV/` = native SwiftUI tvOS frontend.
